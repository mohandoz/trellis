# Build Reference

This reference doc lists the build steps for the project.

1. Install dependencies with the package manager.
2. Run the test suite.
3. Package the artifact.

It carries no constraint vocabulary and no import directives, so it passes the
audit gate cleanly and is eligible for bulk archiving.
