# Legacy Adapter Notes

We decided to keep the legacy adapter in place. Do not remove it; never delete
the compatibility shim.

Rationale: downstream consumers still depend on the old interface, and we chose
stability over cleanup for this release.
