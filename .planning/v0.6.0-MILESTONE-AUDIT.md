---
milestone: v0.6.0
milestone_name: Safe Brownfield Adoption
audited: 2026-05-29T04:30:00Z
status: passed
note_postaudit: "Both substantive findings closed during audit: the clean-git-repo BLOCKER (commit 86ca62b) and the snapshot .git WARNING (commit a1ff4ca). Remaining tech_debt is cosmetic/deferred-hardening only."
scores:
  requirements: 23/23
  phases: 4/4
  integration: 5/5   # all cross-phase connections WIRED (1 broken flow found + FIXED this audit)
  flows: 5/5         # the 5 ROADMAP E2E criteria verified in Phase 24 (suite 449/0)
gaps:
  requirements: []   # all 23 v0.6.0 requirements satisfied (3-source cross-referenced)
  integration: []    # the clean-git-repo blocker found by the integration checker was FIXED (commit 86ca62b) + regression-tested
  flows: []
tech_debt:
  - phase: 22-conjure-adopt-cli-core-rollback
    items:
      - "RESOLVED (commit a1ff4ca): snapshot_create now excludes .git + node_modules via `tar --exclude` (portable, preserves symlinks/perms/timestamps; cp -a fallback). Verified on a live git repo: zero rollback 'Permission denied' lines, tracked-file sha256 zero-diff preserved, git fsck clean + HEAD unchanged, suite 449/0. (was: copied .git wholesale → rollback stderr spam on read-only objects.)"
      - "SAFE-05 rollback-recovery race: a SIGKILL in the snapshot_path-flush window leaves [r]ollback refuse-closed (safe, no corruption). Proper fix needs a snapshot-completion marker. -> hardening backlog (manual UAT 2026-05-29 confirmed all 3 recovery branches otherwise work)."
  - phase: 22-23-24-nyquist
    items:
      - "Nyquist VALIDATION.md frontmatter for phases 22/23/24 still reads nyquist_compliant: false / wave_0_complete: false. Actual test coverage IS comprehensive + green (test-first Wave 0 blocks, every task has an automated verify, suite 449/0, interactive paths PTY-verified) — only the frontmatter flag was never flipped post-execution. PARTIAL (stale flag, not a coverage gap). -> run /gsd-validate-phase 22|23|24 to flip, or accept as cosmetic."
  - phase: requirements-traceability
    items:
      - "REQUIREMENTS.md body references ADOPT-07 (--json) + ADOPT-08 (--quick) but they are absent from the traceability table. Intentionally v2-deferred (not part of the v0.6.0 23-requirement scope). Cosmetic traceability sync only."
---

# v0.6.0 "Safe Brownfield Adoption" — Milestone Audit

**Audited:** 2026-05-29 (re-audit after all 4 phases complete; supersedes the 2026-05-28 partial audit)
**Status:** `tech_debt` — all 23 requirements satisfied, no blockers (one found + fixed this audit), minor tracked debt.

## Summary

All 4 phases (21–24) complete and verified (`passed`). All 23 v0.6.0 requirements satisfied (3-source cross-referenced: REQUIREMENTS.md traceability + phase VERIFICATION.md + SUMMARY frontmatter). Full test suite **449/0**; CI shellcheck gate clean. The integration checker's live E2E smoke test found ONE real blocker — `conjure adopt` refused a clean committed git repo (the headline user flow) — which was **fixed during this audit** (commit `86ca62b`) and locked with a regression test. Remaining items are minor, tracked tech debt (none blocking).

## Requirements Coverage — 23/23 satisfied

| Group | IDs | Phase | Status |
|-------|-----|-------|--------|
| ADOPT | 01,02,03,04,05,06 | 22 (03→21) | satisfied |
| SAFE | 01,02,03,04,05,06,07 | 22 (03→21) | satisfied |
| INV | 01,02,03,04 | 21 | satisfied |
| RESTR | 01,02,03,04,05,06 | 23 | satisfied |

(ADOPT-07/08 are v2-deferred — not in the v0.6.0 scope.)

## Cross-Phase Integration — 5/5 wired

All connections verified WIRED by the integration checker (read code + live smoke test):
1. adopt.sh orchestrates the Phase 21 libs (not reimplemented).
2. cli/conjure → adopt.sh env contract.
3. restructure skill drives the Phase 22 seam (`--update-manifest`/`--apply-step`); `[Read,Bash]` chokepoint holds.
4. gate helpers call `conjure audit` + caps.sh.
5. adopt scaffolds the restructure skill via init-project.sh.

**Broken flow found + FIXED this audit:** `conjure adopt` refused a clean git repo (log_init wrote RESTRUCTURE-LOG.md before precondition_git checked `git status --porcelain`). Fixed in `86ca62b` — precondition_git now filters conjure's own artifacts; a genuinely dirty user tree still exits 2. Regression test added (clean-git-repo pipeline; the gap was that every other pipeline test used a non-git target so the gate self-skipped).

## E2E Flows — 5/5 (Phase 24, suite 449/0)

C1 dry-run <30s + zero writes · C2 rollback zero-diff · C3 idempotent re-run · C4 SIGKILL recovery · C5 symlink-skip + @import-block. All green against the 500-file `_brownfield-argus` fixture.

## Nyquist Coverage

| Phase | VALIDATION.md | nyquist_compliant | Status |
|-------|---------------|-------------------|--------|
| 21 | exists | true | COMPLIANT |
| 22 | exists | false (stale flag) | PARTIAL |
| 23 | exists | false (stale flag) | PARTIAL |
| 24 | exists | false (stale flag) | PARTIAL |

Coverage is real + green for all phases; only the 22/23/24 frontmatter flags weren't flipped post-execution. Run `/gsd-validate-phase 22|23|24` to flip, or accept as cosmetic.

## Tech Debt (tracked, non-blocking)

See frontmatter `tech_debt`. Highlights: snapshot copies `.git/` (rollback stderr noise, deferred — touches the safety primitive); SAFE-05 flush-window rollback race (refuse-closed, deferred hardening); nyquist frontmatter flags stale; ADOPT-07/08 traceability cosmetic.
