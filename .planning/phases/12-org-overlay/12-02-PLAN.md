---
phase: 12-org-overlay
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - cli/conjure
  - scripts/audit-setup.sh
autonomous: true
requirements:
  - OVLY-01
  - OVLY-03
  - OVLY-04
  - OVLY-05

must_haves:
  truths:
    - "conjure init --overlay <url> exits 0, calls init-overlay.sh after base kit and profile"
    - "conjure refresh-overlay calls refresh-overlay.sh (cmd_refresh_overlay dispatcher exists)"
    - "refresh-overlay) case is present in the dispatch table and routes to cmd_refresh_overlay"
    - "conjure audit reports overlay URL + pinned SHA + upstream SHA comparison"
    - "conjure audit exits 0 when git ls-remote fails (D-06 graceful degradation)"
    - "conjure audit reports DRIFT warning when pinned SHA != upstream SHA"
    - "usage() string mentions refresh-overlay and --overlay=<git-url>"
  artifacts:
    - path: "cli/conjure"
      provides: "CLI entry points for --overlay init and refresh-overlay"
      contains: "cmd_refresh_overlay"
    - path: "scripts/audit-setup.sh"
      provides: "overlay presence + drift check audit section"
      contains: ".conjure-org-overlay"
  key_links:
    - from: "cli/conjure cmd_init"
      to: "scripts/init-overlay.sh"
      via: "bash $CONJURE_HOME/scripts/init-overlay.sh $overlay $target"
      pattern: "init-overlay\\.sh.*overlay.*target"
    - from: "cli/conjure cmd_refresh_overlay"
      to: "scripts/refresh-overlay.sh"
      via: "bash $CONJURE_HOME/scripts/refresh-overlay.sh"
      pattern: "refresh-overlay\\.sh"
    - from: "scripts/audit-setup.sh"
      to: ".claude/.conjure-org-overlay"
      via: "grep '^url=' and grep '^sha=' reads"
      pattern: "conjure-org-overlay"
---

<objective>
Wire the two overlay worker scripts into the CLI and audit system:
- Extend cmd_init in cli/conjure with --overlay=* flag parsing and a call to
  scripts/init-overlay.sh after base kit + profile overlay
- Add cmd_refresh_overlay function and refresh-overlay) dispatch case to cli/conjure
- Update usage() string to show --overlay and refresh-overlay
- Add overlay presence + drift check section to scripts/audit-setup.sh (after existing
  conflict-marker check at line 145)

Purpose: Makes overlay functionality user-accessible via the conjure CLI and surfaced
by conjure audit. Plan 01 worker scripts do the real work; this plan is pure wiring.

Output: Modified cli/conjure and scripts/audit-setup.sh. No new files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/12-org-overlay/12-CONTEXT.md
@.planning/phases/12-org-overlay/12-RESEARCH.md
@.planning/phases/12-org-overlay/12-PATTERNS.md
@.planning/phases/12-org-overlay/12-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend cli/conjure — overlay flag + cmd_refresh_overlay + dispatch</name>
  <files>cli/conjure</files>
  <read_first>
    - cli/conjure lines 1-110 (cmd_init full body: local declaration at line 55, arg-parser loop lines 56-65, profile overlay block lines 83-86, mutate_summary/close at lines 108-110)
    - cli/conjure lines 253-325 (cmd_refresh_graph dispatcher at 253-255, dispatch table at 310-324, usage() at lines 27-50)
    - .planning/phases/12-org-overlay/12-PATTERNS.md section "cli/conjure" (five exact location patches: arg parser, cmd_init body, cmd_refresh_overlay function, dispatch table, usage string)
    - .planning/phases/12-org-overlay/12-RESEARCH.md section "Pitfall 6" (both function AND dispatch case must be added)
  </read_first>
  <action>
    Make exactly five edits to cli/conjure. Read the file first to confirm current line numbers before editing.

    Edit 1 — cmd_init local declaration (line 55 area):
    Change: local mode="existing" profile="" dryrun=0 target="$(pwd)"
    To: local mode="existing" profile="" overlay="" dryrun=0 target="$(pwd)"

    Edit 2 — cmd_init arg-parser (line 59-60 area, after --profile=* case):
    Add new case arm inside the while/case block:
      --overlay=*)          overlay="${1#--overlay=}" ;;

    Edit 3 — cmd_init body (after profile overlay block, around line 86-87):
    Insert after the "fi" that closes the profile overlay if-block:
      # Apply org overlay if specified (OVLY-01)
      if [ -n "$overlay" ]; then
        CONJURE_HOME="$CONJURE_HOME" DRY_RUN="$dryrun" \
          bash "$CONJURE_HOME/scripts/init-overlay.sh" "$overlay" "$target"
      fi

    Edit 4 — add cmd_refresh_overlay function (after cmd_refresh_graph function at line 255):
    cmd_refresh_overlay() {
      bash "$CONJURE_HOME/scripts/refresh-overlay.sh" "$@"
    }

    Edit 5 — dispatch table (after refresh-graph) line, around line 316):
    Add:
      refresh-overlay)  shift; cmd_refresh_overlay "$@"  ;;

    Edit 6 — usage() string:
    - Extend the conjure init usage line to show [--overlay=<git-url>]:
        conjure init [new|existing|migrate] [--profile=<stack>] [--overlay=<git-url>] [--dry-run] [target]
    - Add a new usage line for refresh-overlay:
        conjure refresh-overlay [target]

    After all edits: shellcheck must pass on cli/conjure.
  </action>
  <verify>
    <automated>shellcheck cli/conjure && grep -c 'cmd_refresh_overlay' cli/conjure</automated>
  </verify>
  <acceptance_criteria>
    - cli/conjure contains "overlay=\"\"" in cmd_init local declaration
    - cli/conjure contains "--overlay=*)" in cmd_init arg-parser case block
    - cli/conjure contains "overlay=\"\${1#--overlay=}\"" (flag stripping)
    - cli/conjure contains "init-overlay.sh.*overlay.*target" (dispatch to worker)
    - cli/conjure contains "cmd_refresh_overlay()" function definition
    - cli/conjure contains "bash.*refresh-overlay.sh" inside cmd_refresh_overlay
    - cli/conjure contains "refresh-overlay)" in dispatch table case block
    - cli/conjure contains "--overlay=<git-url>" in usage() string
    - cli/conjure contains "refresh-overlay" in usage() string
    - shellcheck cli/conjure exits 0
    - grep -c 'cmd_refresh_overlay' cli/conjure returns 2 (function definition + dispatch case)
  </acceptance_criteria>
  <done>cli/conjure accepts --overlay=<url> in cmd_init, calls init-overlay.sh after profile overlay, has cmd_refresh_overlay dispatcher, has refresh-overlay) dispatch case, usage reflects both additions, and shellcheck passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add overlay audit section to scripts/audit-setup.sh</name>
  <files>scripts/audit-setup.sh</files>
  <read_first>
    - scripts/audit-setup.sh lines 1-20 (TARGET variable set at line 8; ok/warn/err/note helpers at lines 14-17; PASS/WARN/FAIL counters)
    - scripts/audit-setup.sh lines 132-155 (conflict-marker check — exact section to insert AFTER; note the closing "fi" at line 145 followed immediately by the summary block)
    - .planning/phases/12-org-overlay/12-PATTERNS.md section "scripts/audit-setup.sh" (complete overlay section code with D-05 + D-06 semantics)
    - .planning/phases/12-org-overlay/12-RESEARCH.md "Pitfall 3" (git ls-remote exit code: must use || true and check empty output, not exit code)
  </read_first>
  <action>
    Insert the overlay presence + drift check section into scripts/audit-setup.sh immediately
    after the closing "fi" of the conflict-marker block (currently around line 145) and before
    the "# Summary" comment block.

    The new section to insert (verbatim per D-05, D-06, and PATTERNS.md):

    # Org overlay presence and drift check (OVLY-04)
    OVERLAY_MARKER="$TARGET/.claude/.conjure-org-overlay"
    if [ ! -f "$OVERLAY_MARKER" ]; then
      ok "no org overlay configured"
    else
      OVERLAY_URL="$(grep '^url=' "$OVERLAY_MARKER" | cut -d= -f2-)"
      PINNED_SHA="$(grep '^sha=' "$OVERLAY_MARKER" | cut -d= -f2)"
      note "[overlay] url: $OVERLAY_URL"
      note "[overlay] pinned: $PINNED_SHA"
      UPSTREAM_SHA="$(git ls-remote "$OVERLAY_URL" HEAD 2>/dev/null | awk '{print $1}')" || true
      if [ -z "$UPSTREAM_SHA" ]; then
        warn "[overlay] drift check skipped (git ls-remote failed)"
      elif [ "$PINNED_SHA" = "$UPSTREAM_SHA" ]; then
        ok "[overlay] up to date ($PINNED_SHA)"
      else
        warn "[overlay] DRIFT — pinned=$PINNED_SHA upstream=$UPSTREAM_SHA — run: conjure refresh-overlay"
      fi
    fi

    Key implementation details:
    - OVERLAY_MARKER uses $TARGET (already set on line 8 — do not redefine it)
    - "|| true" on the git ls-remote line prevents set -uo pipefail from aborting (Pitfall 3)
    - Use "grep '^sha=' … | cut -d= -f2" (not f2-) for SHA — SHA has no = characters
    - Use "grep '^url=' … | cut -d= -f2-" (f2-) for URL — URL may contain = characters
    - The "git ls-remote" call does NOT need git to be installed as a prerequisite in audit-setup.sh because git is already a global Conjure prerequisite; if git is absent the || true handles any failure
    - Do NOT change the exit-code logic at the bottom of audit-setup.sh (lines 200+)

    After the edit: shellcheck must pass on scripts/audit-setup.sh.
  </action>
  <verify>
    <automated>shellcheck scripts/audit-setup.sh</automated>
  </verify>
  <acceptance_criteria>
    - scripts/audit-setup.sh contains "conjure-org-overlay" (overlay section present)
    - scripts/audit-setup.sh contains "git ls-remote.*HEAD.*|| true" (graceful fail — D-06)
    - scripts/audit-setup.sh contains "drift check skipped" (D-06 warning message)
    - scripts/audit-setup.sh contains "DRIFT" (D-05 drift warning output)
    - scripts/audit-setup.sh contains "up to date" (D-05 up-to-date output)
    - scripts/audit-setup.sh contains "cut -d= -f2-" (URL with potential = preserved)
    - The overlay section appears AFTER "conflict markers" in the file (grep -n shows overlay section line > conflict marker section line)
    - The overlay section appears BEFORE "# Summary" in the file
    - shellcheck scripts/audit-setup.sh exits 0
  </acceptance_criteria>
  <done>scripts/audit-setup.sh has an overlay section that reads the marker file, runs git ls-remote for drift detection, gracefully skips on network failure, and reports DRIFT/up-to-date/not-configured status using the existing ok/warn/note helpers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cli/conjure → scripts/init-overlay.sh | CLI passes user-supplied URL and target path to worker |
| scripts/audit-setup.sh → git ls-remote | Audit initiates outbound network call to overlay repo URL stored in marker file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-05 | Information Disclosure | audit-setup.sh logs OVERLAY_URL (may contain embedded token in URL) | mitigate | note "[overlay] url: $OVERLAY_URL" is informational for legitimate use; document in README that users should use SSH URLs or credential helpers rather than embedding tokens in URLs; no automatic masking required for CLI tools |
| T-12-06 | Denial of Service | git ls-remote in audit hangs on unresponsive host | accept | git ls-remote has a default server timeout (~30s); CI context makes this acceptable; no additional timeout wrapper needed for v0.4.0 |
| T-12-07 | Tampering | overlay URL in .conjure-org-overlay marker could be modified by attacker to point to malicious repo | accept | .conjure-org-overlay is a project file; attacker who can modify it already has write access to the project; no additional protection warranted |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | No package manager installs in this plan; all tools are POSIX + git builtins |
</threat_model>

<verification>
After both tasks:
1. shellcheck cli/conjure — must exit 0
2. shellcheck scripts/audit-setup.sh — must exit 0
3. grep 'refresh-overlay' cli/conjure — must show 3+ matches (function, body, dispatch)
4. grep 'overlay=' cli/conjure — must show --overlay= in usage and arg parser
5. grep 'init-overlay.sh' cli/conjure — must show call with $overlay and $target
6. grep 'conjure-org-overlay' scripts/audit-setup.sh — must exist
7. grep 'git ls-remote' scripts/audit-setup.sh — must show '|| true' on same line
8. grep -n 'Summary' scripts/audit-setup.sh vs grep -n 'conjure-org-overlay' — overlay section must appear before Summary line
</verification>

<success_criteria>
cli/conjure routes conjure init --overlay and conjure refresh-overlay to the correct worker
scripts. conjure audit reports overlay presence, pinned SHA, and drift status, exiting 0
even when git ls-remote fails. Both files are shellcheck-clean.
</success_criteria>

<output>
Create .planning/phases/12-org-overlay/12-02-SUMMARY.md when done
</output>
