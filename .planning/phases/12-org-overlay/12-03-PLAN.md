---
phase: 12-org-overlay
plan: 03
type: execute
wave: 3
depends_on:
  - 12-01
  - 12-02
files_modified:
  - tests/run.sh
autonomous: true
requirements:
  - OVLY-01
  - OVLY-02
  - OVLY-03
  - OVLY-04
  - OVLY-05

must_haves:
  truths:
    - "bash tests/run.sh exits 0 with FAIL: 0 after OVLY tests are added"
    - "OVLY-01 through OVLY-05 are exercised by named test assertions in tests/run.sh"
    - "Mock overlay repo is a local file:// git repo — no network required"
    - "OVLY-04 DRIFT test verifies that conjure audit prints 'DRIFT' when pinned SHA differs"
    - "OVLY-04 graceful-fail test verifies audit exits 0 (not 128) when git ls-remote fails"
    - "OVLY-05 static grep verifies neither worker script contains credential keywords"
    - "All OVLY sandbox dirs are cleaned up with rm -rf at end of test block"
  artifacts:
    - path: "tests/run.sh"
      provides: "OVLY regression test blocks"
      contains: "OVLY org-overlay tests"
  key_links:
    - from: "tests/run.sh OVLY block"
      to: "scripts/init-overlay.sh"
      via: "bash scripts/init-overlay.sh $OVLY_URL $OVLY_TARGET"
      pattern: "init-overlay\\.sh.*OVLY"
    - from: "tests/run.sh OVLY block"
      to: "scripts/audit-setup.sh"
      via: "bash scripts/audit-setup.sh $OVLY_TARGET"
      pattern: "audit-setup\\.sh.*OVLY"
---

<objective>
Add OVLY-01 through OVLY-05 regression test blocks to tests/run.sh, covering all 12
test assertions from the VALIDATION.md per-task map. The tests use a local file:// git
repo as the mock overlay (no network, no external services).

Purpose: Closes the feedback loop so every OVLY requirement is machine-verified by
bash tests/run.sh. The 12-VALIDATION.md documents exactly which assertions are required.

Output: tests/run.sh extended with OVLY test section inserted before the final Summary
block. bash tests/run.sh exits 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/12-org-overlay/12-CONTEXT.md
@.planning/phases/12-org-overlay/12-RESEARCH.md
@.planning/phases/12-org-overlay/12-PATTERNS.md
@.planning/phases/12-org-overlay/12-VALIDATION.md
@.planning/phases/12-org-overlay/12-01-SUMMARY.md
@.planning/phases/12-org-overlay/12-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add OVLY regression blocks to tests/run.sh</name>
  <files>tests/run.sh</files>
  <read_first>
    - tests/run.sh final 40 lines (locate the "# Summary" / "PASS: $PASS FAIL: $FAIL" block and the CLEANUP SKILL sandbox — insert OVLY block BEFORE the Summary block)
    - tests/run.sh lines 760-820 (MKTPL-SETUP + MKTPL-01 tests — exact structural template; sandbox setup, RC capture, pass/fail assertions)
    - tests/run.sh lines 890-960 (SKILL-SETUP pattern — git init + commit + CONJURE_HOME-based invocation style)
    - .planning/phases/12-org-overlay/12-PATTERNS.md section "tests/run.sh" (complete OVLY-SETUP, OVLY-01 through OVLY-05 test patterns, cleanup)
    - .planning/phases/12-org-overlay/12-VALIDATION.md (12 assertion IDs to confirm all are present)
    - scripts/init-overlay.sh (to confirm actual exit-code behavior before writing assertions)
    - scripts/refresh-overlay.sh (to confirm actual exit-code behavior)
    - scripts/audit-setup.sh (to confirm actual DRIFT/skip/ok output strings)
  </read_first>
  <action>
    Insert the OVLY test section into tests/run.sh immediately before the final Summary
    block (the "═══" separator + PASS/FAIL echo + [ "$FAIL" -eq 0 ] lines at end of file).
    Insert after the "# CLEANUP SKILL sandbox" + rm -rf line.

    The OVLY block structure (12 assertions per VALIDATION.md):

    Section header:
      echo
      echo "▸ OVLY org-overlay tests (OVLY-01 through OVLY-05)"

    OVLY-SETUP (local git repo as mock overlay — file:// URL, no network):
      Create OVLY_REPO with git init, user config, skills/org-skill/SKILL.md,
      agents/org-agent.md, initial commit. Capture OVLY_EXPECTED_SHA.
      Create OVLY_TARGET with mkdir -p .claude/.
      Set OVLY_URL="file://$OVLY_REPO".

    OVLY-01 and OVLY-02 assertions (7 assertions):
      a. init-overlay.sh exits 0 (OVLY-01)
      b. overlay skill file present in .claude/skills/org-skill/SKILL.md (OVLY-01)
      OVLY-01c (DRY_RUN honored): invoke init-overlay.sh with DRY_RUN=1 on a fresh target,
        verify exit 0, verify no files written to .claude/, verify mutate_summary reports
        0 mutations skipped — confirm DRY_RUN is propagated through lib/mutate.sh correctly
      c. .conjure-org-overlay marker exists (OVLY-02)
      d. marker url= matches OVLY_URL (OVLY-02)
      e. marker sha= matches OVLY_EXPECTED_SHA (OVLY-02)

    OVLY-03 assertions (3 assertions):
      f. refresh-overlay exits 1 with "No org overlay configured" when no marker (OVLY-03)
        — use a fresh NO_MARKER_DIR, run refresh-overlay.sh on it, check RC=1 and message
      g. refresh-overlay exits 0 with valid marker in OVLY_TARGET (OVLY-03)
      h. overlay file still present after refresh (OVLY-03)

    OVLY-04 assertions (3 assertions):
      i. conjure audit reports overlay status (contains 'up to date' OR 'overlay') when SHA matches (OVLY-04)
        — add a minimal CLAUDE.md to OVLY_TARGET so audit has something to check
        — run: bash "$CONJURE_HOME/scripts/audit-setup.sh" "$OVLY_TARGET"
      j. conjure audit reports DRIFT when pinned SHA is overwritten with fake value (OVLY-04)
        — overwrite marker sha= with deadbeef… SHA, re-run audit, grep for 'DRIFT'
        — restore marker afterward
      k. conjure audit exits 0 (not 128) and prints 'drift check skipped' on invalid URL (OVLY-04)
        — overwrite marker with url=file:///nonexistent-overlay-repo, run audit, check RC != 128 and message

    OVLY-05 assertions (2 assertions):
      l. grep -qE 'password|credential|token' scripts/init-overlay.sh — must NOT match (OVLY-05)
      m. grep -qE 'password|credential|token' scripts/refresh-overlay.sh — must NOT match (OVLY-05)

    Cleanup:
      rm -rf "$OVLY_REPO" "$OVLY_TARGET"
      rm -rf "$NO_MARKER_DIR" (if not already cleaned inline)

    Implementation notes:
    - Use CONJURE_HOME="$CONJURE_HOME" prefix on all worker-script invocations (same as MKTPL tests)
    - Capture stdout+stderr with 2>&1 and redirect to /dev/null for non-assertion runs
    - Use "|| SOME_RC=$?" pattern (not set -e override) to capture non-zero exits
    - The OVLY-04 up-to-date test uses "up to date" OR "overlay" as grep pattern because
      audit may print "[overlay] up to date" or just mention overlay presence
    - The OVLY-04 invalid-URL test: check RC != 128 (not RC = 0) because audit may still
      exit 1 for other checks in the target; the key invariant is it does not exit 128
    - After OVLY-04c test, restore marker to clean state before cleanup

    Count: 13 pass/fail calls total (assertions a through m with the no-marker message check
    bringing OVLY-03 to 3 assertions, for a total matching VALIDATION.md count of 12-13).
  </action>
  <verify>
    <automated>bash tests/run.sh</automated>
  </verify>
  <acceptance_criteria>
    - tests/run.sh contains "OVLY org-overlay tests" section header
    - tests/run.sh contains "OVLY-01" string in pass/fail assertion labels
    - tests/run.sh contains "OVLY-02" string in pass/fail assertion labels
    - tests/run.sh contains "OVLY-03" string in pass/fail assertion labels
    - tests/run.sh contains "OVLY-04" string in pass/fail assertion labels
    - tests/run.sh contains "OVLY-05" string in pass/fail assertion labels
    - tests/run.sh contains "file://" (local mock overlay — no network dependency)
    - tests/run.sh contains "DRIFT" as a grep target (OVLY-04 drift test)
    - tests/run.sh contains "drift check skipped" as a grep target (OVLY-04 graceful fail)
    - tests/run.sh contains "No org overlay configured" as a grep target (OVLY-03)
    - tests/run.sh contains "rm -rf.*OVLY" (sandbox cleanup)
    - bash tests/run.sh exits 0 with FAIL: 0 in the summary line
  </acceptance_criteria>
  <done>
    tests/run.sh contains OVLY-01 through OVLY-05 regression blocks. bash tests/run.sh
    exits 0 with FAIL: 0. All 12+ test assertions from VALIDATION.md are covered. No
    network access required (file:// URLs throughout). Sandbox is fully cleaned up.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tests/run.sh → file:// local git repo | Tests invoke init-overlay.sh with a local file:// URL; no external network |
| tests/run.sh → scripts/*.sh | Tests invoke worker scripts directly via CONJURE_HOME path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-08 | Tampering | test-created temp dirs left on disk if tests fail mid-run | accept | Tests use mktemp -d and rm -rf cleanup; partial cleanup on failure is acceptable in test context |
| T-12-09 | Information Disclosure | OVLY_EXPECTED_SHA leaks into test output | accept | SHA values in test output are from a local throw-away repo; no secrets involved |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | No package manager installs; pure bash test additions |
</threat_model>

<verification>
After the task:
1. bash tests/run.sh — must exit 0
2. bash tests/run.sh 2>&1 | grep 'FAIL:' — must show FAIL: 0
3. bash tests/run.sh 2>&1 | grep -c 'OVLY' — must be >= 12 (one per assertion)
4. grep 'file://' tests/run.sh — must show OVLY_URL assignment (no network mock)
5. grep 'DRIFT' tests/run.sh — must exist in OVLY-04 test
6. grep 'drift check skipped' tests/run.sh — must exist in OVLY-04c test
</verification>

<success_criteria>
bash tests/run.sh exits 0 with FAIL: 0. All five OVLY requirement groups are represented
by named pass/fail assertions. The mock overlay uses file:// URLs so tests run offline.
The VALIDATION.md wave-0 gap list is fully resolved.
</success_criteria>

<output>
Create .planning/phases/12-org-overlay/12-03-SUMMARY.md when done
</output>
