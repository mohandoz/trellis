---
phase: 08-nyquist-compliance-backfill
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/06-cost-estimator/06-VALIDATION.md
  - .planning/phases/07-skill-firing-telemetry/07-VALIDATION.md
autonomous: true
requirements:
  - TECH-02e
  - TECH-02f

must_haves:
  truths:
    - "A contributor can copy-paste any verify block from 06-VALIDATION.md and confirm cost estimator behavior without reading source code"
    - "A contributor can copy-paste any verify block from 07-VALIDATION.md and confirm skill-firing telemetry behavior without reading source code"
    - "06-VALIDATION.md covers: cost section header present, cost label has ±20% band and pricing date, no network calls in default path, --exact advisory when ANTHROPIC_API_KEY absent"
    - "07-VALIDATION.md covers: hook file exists, no network egress patterns in hook, opt-in gate (CONJURE_TELEMETRY unset), DO_NOT_TRACK suppression, JSONL write with required fields, UserPromptExpansion path, retire-list section renders, TELEMETRY.md present with required fields"
  artifacts:
    - path: ".planning/phases/06-cost-estimator/06-VALIDATION.md"
      provides: "Executable verify commands for Phase 06 cost estimator"
      contains: "<!-- Covers: TECH-02e"
    - path: ".planning/phases/07-skill-firing-telemetry/07-VALIDATION.md"
      provides: "Executable verify commands for Phase 07 skill-firing telemetry"
      contains: "<!-- Covers: TECH-02f"
  key_links:
    - from: ".planning/phases/06-cost-estimator/06-VALIDATION.md"
      to: "scripts/audit-setup.sh"
      via: "CONJURE_COST=1 invocations"
      pattern: "CONJURE_COST=1"
    - from: ".planning/phases/07-skill-firing-telemetry/07-VALIDATION.md"
      to: "templates/hooks-nodejs/skill-telemetry.mjs"
      via: "node invocations with piped JSON payloads"
      pattern: "skill-telemetry.mjs"
---

<objective>
Create VALIDATION.md files for Phase 06 (cost estimator) and Phase 07 (skill-firing telemetry). Each file contains standalone, copy-paste-runnable shell verify blocks that confirm the phase's shipped behavior.

Purpose: Close the Nyquist compliance gap for TECH-02e and TECH-02f (per D-01 through D-04 in 08-CONTEXT.md). Phase 07 is the most verification-dense phase (7 behaviors), requiring careful standalone block design.
Output: Two VALIDATION.md files covering cost estimation output format, network-free guarantees, opt-in telemetry behavior, JSONL record format, and retire-list rendering.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-nyquist-compliance-backfill/08-CONTEXT.md
@.planning/ROADMAP.md
@tests/run.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 06-VALIDATION.md for Phase 06 (Cost Estimator)</name>
  <files>.planning/phases/06-cost-estimator/06-VALIDATION.md</files>
  <read_first>
    - tests/run.sh lines 378-444 (cost estimator test section — exact env vars, commands, grep patterns)
    - scripts/audit-setup.sh (understand CONJURE_COST and CONJURE_EXACT env var integration points, and grep for network call patterns)
    - lib/prices.json (understand what model/pricing keys are present)
  </read_first>
  <action>
    Create .planning/phases/06-cost-estimator/06-VALIDATION.md following D-03 and D-04.

    Header line (per D-04):
    <!-- Covers: TECH-02e | COST-01, COST-02, COST-03 -->

    Title: # Phase 06 VALIDATION

    Write exactly 4 ## Verify sections. All blocks that touch the filesystem use their own inline tmpdir setup.

    Section 1 — "Verify cost section header appears when CONJURE_COST=1 (COST-01)" (mirrors run.sh lines 384-392):
    Setup: CONJURE_HOME=$(pwd); TMPDIR=$(mktemp -d); trap ... EXIT; cp -r tests/fixtures/python-fastapi/. "$TMPDIR/"
    Command: `CONJURE_COST=1 bash scripts/audit-setup.sh "$TMPDIR" 2>&1 | grep '── Cost Estimate ──'`
    Expected: line containing `── Cost Estimate ──`

    Section 2 — "Verify cost label has ±20% band and pricing date (COST-02)" (mirrors run.sh lines 395-413):
    Setup: same pattern — fresh TMPDIR with python-fastapi fixture.
    Commands:
      `CONJURE_COST=1 bash scripts/audit-setup.sh "$TMPDIR" 2>&1 | grep -E 'Estimate: \$[0-9]+\.[0-9]{2} ±20%'`
      `CONJURE_COST=1 bash scripts/audit-setup.sh "$TMPDIR" 2>&1 | grep 'prices:'`
    Expected: first grep matches `Estimate: $X.XX ±20%`; second grep matches `prices:` with a date.

    Section 3 — "Verify no network calls in default audit path (COST-03)" (mirrors run.sh lines 423-428):
    Static grep — no tmpdir needed:
    `grep -v '^#' scripts/audit-setup.sh | grep -cE 'curl|fetch|http[s]?:' || true`
    Expected: `0` (zero network call patterns)

    Section 4 — "Verify --exact advisory when ANTHROPIC_API_KEY is absent (COST-03)" (mirrors run.sh lines 431-442):
    Setup: fresh TMPDIR with python-fastapi fixture.
    Command: `CONJURE_COST=1 CONJURE_EXACT=1 ANTHROPIC_API_KEY="" bash scripts/audit-setup.sh "$TMPDIR" 2>&1 | grep 'ANTHROPIC_API_KEY not set'`
    Expected: line containing `ANTHROPIC_API_KEY not set`

    Do NOT include prose background, failure modes, or explanatory paragraphs.
  </action>
  <verify>
    <automated>
      grep -c '<!-- Covers: TECH-02e' .planning/phases/06-cost-estimator/06-VALIDATION.md &&
      grep -c '## Verify' .planning/phases/06-cost-estimator/06-VALIDATION.md | grep -E '^4$' &&
      grep -c 'Expected:' .planning/phases/06-cost-estimator/06-VALIDATION.md | grep -E '^[4-9]$' &&
      grep -c 'CONJURE_COST=1' .planning/phases/06-cost-estimator/06-VALIDATION.md | grep -E '^[1-9]'
    </automated>
  </verify>
  <acceptance_criteria>
    - File exists at .planning/phases/06-cost-estimator/06-VALIDATION.md
    - First line matches: <!-- Covers: TECH-02e | COST-01, COST-02, COST-03 -->
    - File contains exactly 4 ## Verify sections
    - File contains exactly 4 **Expected:** lines (or more if section 2 has two sub-patterns counted separately — at minimum 4)
    - grep -c 'CONJURE_COST=1' >= 3 (cost flag used across multiple sections)
    - grep -c '── Cost Estimate ──' >= 1 (COST-01 header pattern present)
    - grep -c '±20%' >= 1 (COST-02 band pattern present)
    - grep -c 'ANTHROPIC_API_KEY' >= 1 (COST-03 advisory pattern present)
    - grep -c "grep -v '\^#'.*grep -cE 'curl" >= 1 OR grep -c 'no network' >= 1 (COST-03 network-free check present)
    - Each filesystem-touching block has its own TMPDIR=$(mktemp -d) line
  </acceptance_criteria>
  <done>06-VALIDATION.md exists with 4 standalone verify sections covering COST-01, COST-02, COST-03 behaviors. Each section is independently copy-paste-runnable from repo root.</done>
</task>

<task type="auto">
  <name>Task 2: Create 07-VALIDATION.md for Phase 07 (Skill-Firing Telemetry)</name>
  <files>.planning/phases/07-skill-firing-telemetry/07-VALIDATION.md</files>
  <read_first>
    - tests/run.sh lines 448-603 (telemetry test section — hook existence, egress grep, CONJURE_TELEMETRY=1 JSONL write, DO_NOT_TRACK suppression, UserPromptExpansion path, retire-list, TELEMETRY.md checks)
    - templates/hooks-nodejs/skill-telemetry.mjs (understand the hook's input format — what JSON payload keys it expects from stdin)
  </read_first>
  <action>
    Create .planning/phases/07-skill-firing-telemetry/07-VALIDATION.md following D-03 and D-04.

    Phase 07 has the most testable behaviors (7 claims). Write exactly 7 ## Verify sections.

    Header line (per D-04):
    <!-- Covers: TECH-02f | TLMY-01, TLMY-02, TLMY-02b, TLMY-03, TLMY-04, TLMY-05 -->

    Title: # Phase 07 VALIDATION

    Section 1 — "Verify skill-telemetry.mjs hook file exists (TLMY-01)" (mirrors run.sh lines 451-455):
    Static check — no tmpdir needed:
    `[ -f templates/hooks-nodejs/skill-telemetry.mjs ] && echo "PASS: exists" || echo "FAIL: missing"`
    Expected: `PASS: exists`

    Section 2 — "Verify hook contains no network egress patterns (TLMY-03)" (mirrors run.sh lines 458-468):
    Static grep — no tmpdir needed:
    `grep -cE 'curl|fetch|http|socket|XMLHttpRequest|require\(.https.\)|require\(.http.\)|import.*https|import.*http|net\.Socket' templates/hooks-nodejs/skill-telemetry.mjs || true`
    Expected: `0` (zero egress patterns)

    Section 3 — "Verify hook exits 0 silently when CONJURE_TELEMETRY is unset (TLMY-01)" (mirrors run.sh lines 498-511):
    TMPDIR=$(mktemp -d); trap ... EXIT
    `printf '{}' | CONJURE_TELEMETRY="" node templates/hooks-nodejs/skill-telemetry.mjs >/dev/null 2>&1; echo "exit: $?"`
    `[ -f "$TMPDIR/.claude/telemetry/skill-events.jsonl" ] && echo "FAIL: file written" || echo "PASS: no file"`
    Expected: `exit: 0` and `PASS: no file`

    Section 4 — "Verify DO_NOT_TRACK=1 suppresses JSONL writes (TLMY-01)" (mirrors run.sh lines 513-526):
    TMPDIR=$(mktemp -d); trap ... EXIT
    Build PAYLOAD with hook_event_name, tool_name (Skill), tool_input.skill_name, session_id, cwd set to $TMPDIR.
    `printf '%s' "$PAYLOAD" | DO_NOT_TRACK=1 CONJURE_TELEMETRY=1 node templates/hooks-nodejs/skill-telemetry.mjs >/dev/null 2>&1; echo "exit: $?"`
    `[ -f "$TMPDIR/.claude/telemetry/skill-events.jsonl" ] && echo "FAIL: file written" || echo "PASS: suppressed"`
    Expected: `exit: 0` and `PASS: suppressed`

    Section 5 — "Verify hook writes JSONL with required fields when CONJURE_TELEMETRY=1 (TLMY-02)" (mirrors run.sh lines 529-562):
    TMPDIR=$(mktemp -d); trap ... EXIT
    `SKILL_PAYLOAD='{"hook_event_name":"PreToolUse","tool_name":"Skill","tool_input":{"skill_name":"test-skill"},"session_id":"sess-001","cwd":"'"$TMPDIR"'"}'`
    `printf '%s' "$SKILL_PAYLOAD" | CONJURE_TELEMETRY=1 node templates/hooks-nodejs/skill-telemetry.mjs >/dev/null 2>&1`
    `cat "$TMPDIR/.claude/telemetry/skill-events.jsonl"`
    Expected: JSON line containing `skill_invoke`, `test-skill`, `session_id`, `project_cwd`

    Section 6 — "Verify UserPromptExpansion path writes skill_typed event (TLMY-02b)" (mirrors run.sh lines 565-587):
    TMPDIR=$(mktemp -d); trap ... EXIT
    First write a PreToolUse/Skill record to ensure the JSONL file exists before the UPE append:
    `SKILL_PAYLOAD='{"hook_event_name":"PreToolUse","tool_name":"Skill","tool_input":{"skill_name":"test-skill"},"session_id":"sess-001","cwd":"'"$TMPDIR"'"}'`
    `printf '%s' "$SKILL_PAYLOAD" | CONJURE_TELEMETRY=1 node templates/hooks-nodejs/skill-telemetry.mjs >/dev/null 2>&1`
    Then build and fire the UPE payload:
    `UPE_PAYLOAD='{"hook_event_name":"UserPromptExpansion","command_name":"/test-skill","session_id":"sess-002","cwd":"'"$TMPDIR"'"}'`
    `printf '%s' "$UPE_PAYLOAD" | CONJURE_TELEMETRY=1 node templates/hooks-nodejs/skill-telemetry.mjs >/dev/null 2>&1`
    `tail -1 "$TMPDIR/.claude/telemetry/skill-events.jsonl"`
    Expected: JSON line containing `skill_typed`, `test-skill`, `project_cwd`

    Section 7 — "Verify retire-list section renders when CONJURE_RETIRE=1 (TLMY-04)" (mirrors run.sh lines 589-601):
    TMPDIR=$(mktemp -d); trap ... EXIT; cp -r tests/fixtures/python-fastapi/. "$TMPDIR/"
    `CONJURE_HOME=$(pwd) CONJURE_RETIRE=1 bash scripts/audit-setup.sh "$TMPDIR" 2>&1 | grep '── Skill Retire-List ──'`
    Expected: line containing `── Skill Retire-List ──`

    Note: TELEMETRY.md check (TLMY-05) is covered as a static file check. Add it as part of Section 1 or as a standalone section — choose whichever keeps section count at 7. If included in Section 1, adjust the section name to cover both existence checks. Recommended: keep Section 1 for hook file only; add Section 8 for TELEMETRY.md — this brings total to 8. Alternatively merge hook + TELEMETRY.md into one section titled "Verify shipped artifacts exist (TLMY-01, TLMY-05)". Either is acceptable as long as TELEMETRY.md fields (session_id, project_cwd, DO_NOT_TRACK) are verified.

    For the TELEMETRY.md section (wherever it lands):
    `[ -f TELEMETRY.md ] && echo "PASS: exists" || echo "FAIL: missing"`
    `grep -c 'session_id\|project_cwd\|DO_NOT_TRACK' TELEMETRY.md`
    Expected: `PASS: exists` and count >= 3

    Do NOT include prose background, failure modes, or explanatory paragraphs.
    Inline PAYLOAD variable construction must use single-quote-safe JSON — use double quotes for JSON keys and values, single quotes for the outer shell variable assignment where possible.
  </action>
  <verify>
    <automated>
      grep -c '<!-- Covers: TECH-02f' .planning/phases/07-skill-firing-telemetry/07-VALIDATION.md &&
      grep -c '## Verify' .planning/phases/07-skill-firing-telemetry/07-VALIDATION.md | grep -E '^[7-9]$' &&
      grep -c 'Expected:' .planning/phases/07-skill-firing-telemetry/07-VALIDATION.md | grep -E '^[7-9]$' &&
      grep -c 'TLMY-0' .planning/phases/07-skill-firing-telemetry/07-VALIDATION.md | grep -E '^[1-9]' &&
      grep -c 'skill-telemetry.mjs' .planning/phases/07-skill-firing-telemetry/07-VALIDATION.md | grep -E '^[1-9]'
    </automated>
  </verify>
  <acceptance_criteria>
    - File exists at .planning/phases/07-skill-firing-telemetry/07-VALIDATION.md
    - First line matches: <!-- Covers: TECH-02f | TLMY-01, TLMY-02, TLMY-02b, TLMY-03, TLMY-04, TLMY-05 -->
    - File contains 7 or 8 ## Verify sections (grep -c '## Verify' in range 7-8)
    - File contains matching number of **Expected:** lines
    - grep -c 'CONJURE_TELEMETRY' >= 3 (opt-in env var used across multiple sections)
    - grep -c 'DO_NOT_TRACK' >= 2 (suppression check present in at least two places: verify block and TELEMETRY.md check)
    - grep -c 'skill_invoke' >= 1 (TLMY-02 JSONL field pattern present)
    - grep -c 'skill_typed' >= 1 (TLMY-02b UserPromptExpansion event type present)
    - grep -c '── Skill Retire-List ──' >= 1 (TLMY-04 retire-list pattern present)
    - grep -c 'TELEMETRY.md' >= 1 (TLMY-05 doc check present)
    - grep -c 'egress\|network\|curl\|fetch' >= 1 (TLMY-03 no-egress check present)
    - Each node invocation block has its own TMPDIR=$(mktemp -d) and uses cwd=$TMPDIR in the payload
  </acceptance_criteria>
  <done>07-VALIDATION.md exists with 7+ standalone verify sections covering TLMY-01 through TLMY-05 behaviors. Each section is independently copy-paste-runnable from repo root.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author → documentation | VALIDATION.md commands are run by contributors in their local shell |
| verify block → node runtime | Sections pipe JSON payloads to skill-telemetry.mjs via stdin |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03 | Information Disclosure | 07-VALIDATION.md node invocations | accept | All node calls use local tmpdir for cwd; hook is designed local-only with no egress (TLMY-03 verifies this) |
| T-08-04 | Tampering | 06-VALIDATION.md audit-setup.sh invocations | accept | CONJURE_COST=1 path is read-only cost estimation; tmpdir setup prevents fixture mutation |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No package manager installs in this plan |
</threat_model>

<verification>
After both tasks complete:

```bash
# Confirm both VALIDATION.md files exist
ls -la .planning/phases/06-cost-estimator/06-VALIDATION.md
ls -la .planning/phases/07-skill-firing-telemetry/07-VALIDATION.md

# Confirm header coverage annotations
head -1 .planning/phases/06-cost-estimator/06-VALIDATION.md
head -1 .planning/phases/07-skill-firing-telemetry/07-VALIDATION.md

# Confirm section counts
grep -c '## Verify' .planning/phases/06-cost-estimator/06-VALIDATION.md
grep -c '## Verify' .planning/phases/07-skill-firing-telemetry/07-VALIDATION.md

# Confirm all 6 VALIDATION.md files exist across all plans
ls .planning/phases/0{1,2,4,5,6,7}-*/??-VALIDATION.md

# CI passes with new files present
bash tests/run.sh 2>&1 | tail -5
```
</verification>

<success_criteria>
- 06-VALIDATION.md and 07-VALIDATION.md both exist in their respective phase directories
- Both files start with the correct <!-- Covers: TECH-02x | ... --> header annotation
- 06-VALIDATION.md has 4 ## Verify sections; 07-VALIDATION.md has 7-8 ## Verify sections
- Every filesystem-touching section has its own inline tmpdir setup
- Every section has an **Expected:** line with a grep-friendly partial pattern
- All 6 VALIDATION.md files (across plans 01, 02, 03) exist: ls .planning/phases/0{1,2,4,5,6,7}-*/??-VALIDATION.md returns 6 files
- CI (bash tests/run.sh) exits 0 with all new files present
</success_criteria>

<output>
Create .planning/phases/08-nyquist-compliance-backfill/08-03-SUMMARY.md when done.
</output>
