---
phase: 08-nyquist-compliance-backfill
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-pre-flight-cross-platform-hooks/01-VALIDATION.md
  - .planning/phases/02-dry-run-enforcement-chokepoint/02-VALIDATION.md
autonomous: true
requirements:
  - TECH-02a
  - TECH-02b

must_haves:
  truths:
    - "A contributor can copy-paste any verify block from 01-VALIDATION.md and confirm preflight behavior without reading source code"
    - "A contributor can copy-paste any verify block from 02-VALIDATION.md and confirm dry-run enforcement without reading source code"
    - "01-VALIDATION.md covers: preflight exits 0 in normal env, blocks when node missing, emits OS-aware fix-it hint, settings template uses node not bash hooks, init-project.sh sources hooks-nodejs with no chmod"
    - "02-VALIDATION.md covers: --dry-run creates no filesystem artifacts, [dry-run] prefix lines appear in output, mutation count > 0 in summary line, lib/mutate.sh respects DRY_RUN"
  artifacts:
    - path: ".planning/phases/01-pre-flight-cross-platform-hooks/01-VALIDATION.md"
      provides: "Executable verify commands for Phase 01 pre-flight and cross-platform hooks"
      contains: "<!-- Covers: TECH-02a"
    - path: ".planning/phases/02-dry-run-enforcement-chokepoint/02-VALIDATION.md"
      provides: "Executable verify commands for Phase 02 dry-run enforcement"
      contains: "<!-- Covers: TECH-02b"
  key_links:
    - from: ".planning/phases/01-pre-flight-cross-platform-hooks/01-VALIDATION.md"
      to: "scripts/preflight.sh"
      via: "verify block invocations"
      pattern: "bash scripts/preflight.sh"
    - from: ".planning/phases/02-dry-run-enforcement-chokepoint/02-VALIDATION.md"
      to: "lib/mutate.sh"
      via: "source + DRY_RUN=1 invocations"
      pattern: "source lib/mutate.sh"
---

<objective>
Create VALIDATION.md files for Phase 01 (pre-flight + cross-platform hooks) and Phase 02 (dry-run enforcement chokepoint). Each file contains standalone, copy-paste-runnable shell verify blocks that confirm the phase's shipped behavior without requiring the reader to understand source code.

Purpose: Close the Nyquist compliance gap for TECH-02a and TECH-02b so a contributor can verify pre-flight and dry-run behavior without reading tests/run.sh (per D-01 through D-04 in 08-CONTEXT.md).
Output: Two VALIDATION.md files, each with 4-5 independent ## Verify sections.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-nyquist-compliance-backfill/08-CONTEXT.md
@.planning/ROADMAP.md
@tests/run.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 01-VALIDATION.md for Phase 01 (Pre-flight + Cross-Platform Hooks)</name>
  <files>.planning/phases/01-pre-flight-cross-platform-hooks/01-VALIDATION.md</files>
  <read_first>
    - tests/run.sh lines 104-183 (preflight test section and template lint section — exact commands and assertions to mirror)
    - scripts/preflight.sh (understand what it checks and outputs)
    - templates/settings.json.tmpl (verify node hook wiring pattern exists)
    - scripts/init-project.sh (verify hooks-nodejs sourcing, no chmod on .mjs)
  </read_first>
  <action>
    Create .planning/phases/01-pre-flight-cross-platform-hooks/01-VALIDATION.md following the document structure mandated by D-03 and D-04.

    Header line (first line of file, per D-04):
    <!-- Covers: TECH-02a | SAFE-03, SAFE-04 -->

    Title: # Phase 01 VALIDATION

    Write exactly 5 ## Verify sections, each with its own inline tmpdir (where filesystem access is needed) and an **Expected:** line using grep-friendly partial patterns (not verbatim full output), per D-02 and D-03.

    Section 1 — "Verify preflight exits 0 in normal environment" (mirrors run.sh line 109):
    Run `CONJURE_HOME=$(pwd) bash scripts/preflight.sh` from repo root. No tmpdir needed (read-only).
    Expected: exit code 0 (exit: 0 or no error output).

    Section 2 — "Verify preflight blocks when node is missing" (mirrors run.sh lines 117-128):
    Build STRIPPED_PATH by filtering directories where node is executable out of $PATH (the same tr/grep/sed idiom from run.sh lines 119-121). Then run `PATH="$STRIPPED_PATH" bash scripts/preflight.sh; echo "exit: $?"`.
    Expected: `exit: [^0]` (non-zero exit — any digit 1-9).

    Section 3 — "Verify preflight emits OS-aware package manager fix-it hint" (mirrors run.sh lines 131-145):
    Same STRIPPED_PATH construction. Capture output of `PATH="$STRIPPED_PATH" bash scripts/preflight.sh 2>&1 || true`, then pipe through `grep -E 'brew|apt|winget'`.
    Expected: line containing `brew` (macOS) or `apt` or `winget` (Linux/Windows).

    Section 4 — "Verify settings.json.tmpl uses node hook wiring (not bash)" (mirrors run.sh lines 165-173):
    Two greps (no tmpdir needed):
    - `grep -c 'node .claude/hooks/' templates/settings.json.tmpl`
    - `grep -v '^#' templates/settings.json.tmpl | grep -c 'bash .claude/hooks/' || true`
    Expected: first count >= 1; second count = 0.

    Section 5 — "Verify init-project.sh sources hooks-nodejs and has no chmod on hook files" (mirrors run.sh lines 175-183):
    Two greps (no tmpdir needed):
    - `grep 'hooks-nodejs' scripts/init-project.sh`
    - `grep -v '^#' scripts/init-project.sh | grep -c 'chmod.*hooks' || true`
    Expected: hooks-nodejs line present; chmod count = 0.

    Do NOT include any prose background, failure modes section, or explanatory paragraphs — only the header comment, title, and ## Verify sections with code blocks and Expected lines (per D-03).
  </action>
  <verify>
    <automated>
      grep -c '<!-- Covers: TECH-02a' .planning/phases/01-pre-flight-cross-platform-hooks/01-VALIDATION.md &&
      grep -c '## Verify' .planning/phases/01-pre-flight-cross-platform-hooks/01-VALIDATION.md | grep -E '^5$' &&
      grep -c 'Expected:' .planning/phases/01-pre-flight-cross-platform-hooks/01-VALIDATION.md | grep -E '^5$' &&
      grep -c 'SAFE-03' .planning/phases/01-pre-flight-cross-platform-hooks/01-VALIDATION.md | grep -E '^[1-9]' &&
      grep -c 'SAFE-04' .planning/phases/01-pre-flight-cross-platform-hooks/01-VALIDATION.md | grep -E '^[1-9]'
    </automated>
  </verify>
  <acceptance_criteria>
    - File exists at .planning/phases/01-pre-flight-cross-platform-hooks/01-VALIDATION.md
    - First line matches pattern: <!-- Covers: TECH-02a | SAFE-03, SAFE-04 -->
    - File contains exactly 5 ## Verify sections (grep -c '## Verify' == 5)
    - File contains exactly 5 **Expected:** lines (grep -c 'Expected:' == 5)
    - No prose section titled "Background", "Failure Modes", or similar — only header comment + title + ## Verify sections
    - Every code block uses `bash` fenced block markers
    - At least one block contains the STRIPPED_PATH node-strip idiom (grep -E 'tr.*:.*\\n.*node')
    - grep -c 'TMPDIR.*mktemp' finds >= 0 (blocks that need tmpdir have it; static-file blocks do not)
    - grep -c 'bash scripts/preflight.sh' >= 3 (preflight invoked in multiple sections)
  </acceptance_criteria>
  <done>01-VALIDATION.md exists with 5 standalone verify sections covering SAFE-03 and SAFE-04 behaviors, each section independently runnable from repo root.</done>
</task>

<task type="auto">
  <name>Task 2: Create 02-VALIDATION.md for Phase 02 (Dry-Run Enforcement Chokepoint)</name>
  <files>.planning/phases/02-dry-run-enforcement-chokepoint/02-VALIDATION.md</files>
  <read_first>
    - tests/run.sh lines 186-221 (dry-run enforcement section — exact commands, assertions, and cleanup idiom)
    - lib/mutate.sh (understand mutate_mkdir, mutate_write function signatures and DRY_RUN behavior)
  </read_first>
  <action>
    Create .planning/phases/02-dry-run-enforcement-chokepoint/02-VALIDATION.md following D-03 and D-04.

    Header line (per D-04):
    <!-- Covers: TECH-02b | SAFE-01, SAFE-02, D-04, D-05 -->

    Title: # Phase 02 VALIDATION

    Write exactly 4 ## Verify sections. Each section that touches the filesystem uses its own inline tmpdir with `TMPDIR=$(mktemp -d); trap 'rm -rf "$TMPDIR"' EXIT` setup.

    Section 1 — "Verify --dry-run creates no filesystem artifacts (SAFE-01)" (mirrors run.sh lines 196-202):
    Setup: TMPDIR=$(mktemp -d); trap ... EXIT; printf '# Test project\n' > "$TMPDIR/CLAUDE.md"
    Command: `CONJURE_HOME=$(pwd) cli/conjure init --dry-run "$TMPDIR" >/dev/null 2>&1 || true`
    Check: `[ -d "$TMPDIR/.claude" ] && echo "FAIL: .claude created" || echo "PASS: no .claude"`
    Expected: `PASS: no .claude`

    Section 2 — "Verify [dry-run] prefix lines appear in output (D-04)" (mirrors run.sh lines 204-209):
    Setup: new TMPDIR with minimal CLAUDE.md.
    Command: `CONJURE_HOME=$(pwd) cli/conjure init --dry-run "$TMPDIR" 2>&1 | grep '\[dry-run\]' | head -3`
    Expected: one or more lines containing `[dry-run]`

    Section 3 — "Verify mutation count > 0 in dry-run summary line (D-05)" (mirrors run.sh lines 210-215):
    Setup: new TMPDIR with minimal CLAUDE.md.
    Command: `CONJURE_HOME=$(pwd) cli/conjure init --dry-run "$TMPDIR" 2>&1 | grep -E '\[dry-run\] [1-9][0-9]* mutations skipped'`
    Expected: line matching `[dry-run] N mutations skipped` where N >= 1

    Section 4 — "Verify lib/mutate.sh DRY_RUN=1 suppresses mkdir and write (SAFE-02)":
    Source lib/mutate.sh then call DRY_RUN=1 mutate_mkdir and DRY_RUN=1 mutate_write. Check that neither creates artifacts.
    Setup: TMPDIR=$(mktemp -d); trap ... EXIT; source lib/mutate.sh
    Commands:
      `DRY_RUN=1 mutate_mkdir "$TMPDIR/would-not-exist"`
      `[ -d "$TMPDIR/would-not-exist" ] && echo "FAIL: dir created" || echo "PASS: mkdir suppressed"`
      `DRY_RUN=1 mutate_write "$TMPDIR/would-not-exist.txt" "content"`
      `[ -f "$TMPDIR/would-not-exist.txt" ] && echo "FAIL: file written" || echo "PASS: write suppressed"`
    Expected: `PASS: mkdir suppressed` and `PASS: write suppressed`

    Important: each section gets its OWN tmpdir setup block — do not share state between sections (per D-02). The trap reset idiom (`trap - EXIT`) is not needed between sections since each section is meant to be run independently (copy-paste), not sequentially in a single shell session.

    Do NOT include prose background, failure modes, or explanatory paragraphs.
  </action>
  <verify>
    <automated>
      grep -c '<!-- Covers: TECH-02b' .planning/phases/02-dry-run-enforcement-chokepoint/02-VALIDATION.md &&
      grep -c '## Verify' .planning/phases/02-dry-run-enforcement-chokepoint/02-VALIDATION.md | grep -E '^4$' &&
      grep -c 'Expected:' .planning/phases/02-dry-run-enforcement-chokepoint/02-VALIDATION.md | grep -E '^4$' &&
      grep -c 'TMPDIR.*mktemp' .planning/phases/02-dry-run-enforcement-chokepoint/02-VALIDATION.md | grep -E '^[4-9]$|^[1-9][0-9]+$'
    </automated>
  </verify>
  <acceptance_criteria>
    - File exists at .planning/phases/02-dry-run-enforcement-chokepoint/02-VALIDATION.md
    - First line matches: <!-- Covers: TECH-02b | SAFE-01, SAFE-02, D-04, D-05 -->
    - File contains exactly 4 ## Verify sections
    - File contains exactly 4 **Expected:** lines
    - Each section that runs CLI or sources lib/mutate.sh has its own TMPDIR=$(mktemp -d) setup
    - grep -c 'PASS: no .claude' == 1 (SAFE-01 assertion present)
    - grep -c '\[dry-run\]' in file >= 2 (D-04 pattern present in at least two places)
    - grep -c 'mutations skipped' == 1 (D-05 assertion present)
    - grep -c 'source lib/mutate.sh' == 1 (SAFE-02 direct test present)
    - grep -c 'PASS: mkdir suppressed' == 1 and grep -c 'PASS: write suppressed' == 1
  </acceptance_criteria>
  <done>02-VALIDATION.md exists with 4 standalone verify sections covering SAFE-01, SAFE-02, D-04, D-05 behaviors. Each section is independently copy-paste-runnable from repo root.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author → documentation | VALIDATION.md commands are run by contributors in their local shell |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | VALIDATION.md tmpdir commands | accept | Commands use mktemp -d + trap cleanup; no persistent side-effects; documentation-only files |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No package manager installs in this plan |
</threat_model>

<verification>
After both tasks complete:

```bash
# Confirm both VALIDATION.md files exist
ls -la .planning/phases/01-pre-flight-cross-platform-hooks/01-VALIDATION.md
ls -la .planning/phases/02-dry-run-enforcement-chokepoint/02-VALIDATION.md

# Confirm header coverage annotations
head -1 .planning/phases/01-pre-flight-cross-platform-hooks/01-VALIDATION.md
head -1 .planning/phases/02-dry-run-enforcement-chokepoint/02-VALIDATION.md

# Confirm section counts
grep -c '## Verify' .planning/phases/01-pre-flight-cross-platform-hooks/01-VALIDATION.md
grep -c '## Verify' .planning/phases/02-dry-run-enforcement-chokepoint/02-VALIDATION.md

# CI passes (no size cap violations, no broken references)
bash tests/run.sh 2>&1 | tail -5
```
</verification>

<success_criteria>
- 01-VALIDATION.md and 02-VALIDATION.md both exist in their respective phase directories
- Both files start with the correct <!-- Covers: TECH-02x | ... --> header annotation
- 01-VALIDATION.md has 5 ## Verify sections; 02-VALIDATION.md has 4 ## Verify sections
- Every section has a standalone tmpdir setup where filesystem interaction occurs
- Every section has an **Expected:** line with a grep-friendly partial pattern
- CI (bash tests/run.sh) exits 0 with the new files present
</success_criteria>

<output>
Create .planning/phases/08-nyquist-compliance-backfill/08-01-SUMMARY.md when done.
</output>
