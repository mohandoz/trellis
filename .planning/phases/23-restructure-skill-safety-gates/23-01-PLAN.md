---
phase: 23-restructure-skill-safety-gates
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/run.sh
  - tests/fixtures/_restructure-gates/with-invariant.md
  - tests/fixtures/_restructure-gates/missing-invariant.md
  - tests/fixtures/_restructure-gates/INVARIANTS.txt
  - tests/fixtures/_restructure-gates/reflowed-invariant.md
  - tests/fixtures/_restructure-gates/with-import.md
  - tests/fixtures/_restructure-gates/oversized.md
  - tests/fixtures/_restructure-gates/decision-doc.md
  - tests/fixtures/_restructure-gates/clean-doc.md
autonomous: true
requirements: [RESTR-01, RESTR-02, RESTR-03, RESTR-04, RESTR-05, RESTR-06]
nyquist_compliant: true
wave_0: true

must_haves:
  truths:
    - "Running bash tests/run.sh emits a `▸ Phase 23 — restructure gate helpers` section"
    - "Before any gate helper or SKILL.md exists, the Phase 23 section reports graceful RED (fail with a 'Wave 1/2 must create ...' message), never a crash"
    - "The full suite still exits 0 is NOT expected in Wave 0 — it exits non-zero with the new Phase 23 reds, but every PRE-EXISTING (Phase ≤22) assertion stays green (no regression)"
    - "Synthetic gate fixtures exist for: invariant-present, invariant-missing, reflowed/case-mangled-but-complete, @import, oversized (>200 lines), decision-vocabulary, clean reference-doc"
  artifacts:
    - path: "tests/run.sh"
      provides: "Phase 23 graceful-red test block gating every Wave 1+ verification"
      contains: "▸ Phase 23 — restructure gate helpers"
    - path: "tests/fixtures/_restructure-gates/INVARIANTS.txt"
      provides: "Canonical-token invariant list used by the verify-invariants gate tests"
  key_links:
    - from: "tests/run.sh"
      to: "templates/skills/restructure/gates/verify-invariants.sh"
      via: "P23_RESTR_OK presence guard + helper invocation"
      pattern: "P23_RESTR_OK"
    - from: "tests/run.sh"
      to: "scripts/init-project.sh existing"
      via: "sandbox scaffold drive for criterion 1"
      pattern: "init-project.sh.*existing"
---

<objective>
Establish the Phase 23 Wave 0 test-first foundation: a graceful-red `▸ Phase 23 — restructure gate helpers` block in `tests/run.sh` plus the synthetic gate fixtures, mirroring the Phase 22 Wave 0 idiom (tests/run.sh:2371-2391 setup + :2749-2883 block). Every later verification (the gate helpers in Wave 1, the SKILL.md + scaffold edit in Wave 2) runs against a concrete red→green signal that already exists.

Per VALIDATION.md the entire deterministic surface is covered here: the four gate helpers' unit contracts, the scaffold-install (criterion 1), the invariant BLOCK (criterion 4), the audit BLOCK (criterion 5), archive-last sequencing (criterion 6), the non-TTY exit-2 approval guard (criterion 3 partial), and the `--apply-step` routing (RESTR-02). The interactive `/dev/tty` approve/skip/edit loop (criterion 2) is the ONE manual/expect item — only its non-TTY exit-2 half is automated here.

Purpose: lock the Nyquist contract — every Wave 1+ task verifies against an assertion that already exists and currently reports a clear, guarded RED. Mirrors the established Phase 22 precedent (STATE.md: "graceful-red test block gates every later verification before scripts/adopt.sh exists").
Output: a new test section in tests/run.sh + a `tests/fixtures/_restructure-gates/` fixture set.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-restructure-skill-safety-gates/23-CONTEXT.md
@.planning/phases/23-restructure-skill-safety-gates/23-RESEARCH.md
@.planning/phases/23-restructure-skill-safety-gates/23-PATTERNS.md
@.planning/phases/23-restructure-skill-safety-gates/23-VALIDATION.md

# The exact Phase 22 Wave 0 idiom this block mirrors:
@tests/run.sh
@tests/fixtures/_adopt-restructure-steps/adopt-manifest.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Phase 23 graceful-red test block + presence guards in tests/run.sh</name>
  <files>tests/run.sh</files>
  <read_first>
    - tests/run.sh:2360-2391 (Phase 22 Wave 0 header comment + the `P22_ADOPT_SH`/`P22_ADOPT_OK`/`P22_FIXTURE`/`p22_sha` presence-guard + sha helper idiom to mirror).
    - tests/run.sh:2749-2883 (the Phase 22 op-executor section: `if [ "$P22_ADOPT_OK" -ne 1 ]; then fail "... Wave 1 must create ..."; else ...; fi` structure, mktemp sandboxes, set/reset EXIT-trap discipline, jq-assert idioms).
    - tests/run.sh:2884-2925 (the `▸ Phase 22 — snapshot self-copy` tail section + the `# End Phase 22 test block` marker at line 2913 + the `for _s in $GH_HIDE_STUBS` cleanup at 2916 + the PASS/FAIL Summary at 2919 — the new block must be inserted BEFORE the `# End Phase 22 test block` marker, OR immediately after it but BEFORE the cleanup/Summary).
    - tests/run.sh:15-16 (`pass()`/`fail()` helpers — increment $PASS/$FAIL).
    - tests/run.sh:2696-2729 (a non-TTY exit-2 assert idiom — capture rc from a piped/`</dev/null` drive, expect 2).
    - tests/fixtures/_adopt-restructure-steps/adopt-manifest.json (the manifest fixture reused for the group-by + apply-step integration drive — classification + restructure_steps[] shape).
    - scripts/init-project.sh:58-64 (the scaffold loop the criterion-1 scaffold test will drive via `init-project.sh existing`).
    - .planning/phases/23-restructure-skill-safety-gates/23-VALIDATION.md (lines 41-54: the Per-Task Verification Map — every assertion this block must contain).
  </read_first>
  <action>
    Insert a new test section into tests/run.sh, placed BEFORE the `# End Phase 22 test block` marker (currently line 2913) so it lands inside the existing trap/cleanup scope and before the Summary. Open it with a header comment block mirroring tests/run.sh:2361-2369 and an `echo "▸ Phase 23 — restructure gate helpers"`.

    Add Phase 23 presence guards mirroring P22_ADOPT_OK (PATTERNS.md lines 265-270): set `P23_RESTR_DIR="$CONJURE_HOME/templates/skills/restructure"`, `P23_RESTR_OK=0`, then `[ -f "$P23_RESTR_DIR/SKILL.md" ] && P23_RESTR_OK=1`; and `P23_GATES_OK=0`, `[ -f "$P23_RESTR_DIR/gates/verify-invariants.sh" ] && P23_GATES_OK=1`. Every assertion sub-section guards on the relevant flag: `if [ "$P23_GATES_OK" -ne 1 ]; then fail "templates/skills/restructure/gates/verify-invariants.sh not found — Wave 1 must create the gate helpers first (RESTR-04)"; else <real asserts>; fi` — graceful RED, never a crash (this is the Nyquist contract: assertions EXIST in Wave 0 and report a guarded fail until Wave 1/2 turn them green).

    Write the deterministic assertions enumerated in VALIDATION.md's Per-Task Verification Map (lines 43-54), each citing its RESTR id / criterion. Use the canonical-token INVARIANTS.txt and the synthetic fixtures created in Task 2 (paths under `tests/fixtures/_restructure-gates/`). The assertion set, by helper:
      - verify-invariants (RESTR-04/Crit-4): `gates/verify-invariants.sh <with-invariant.md> <INVARIANTS.txt>` → rc 0 + pass; `<missing-invariant.md> <INVARIANTS.txt>` → rc 2 + the stderr lists the missing token(s); `<reflowed-invariant.md> <INVARIANTS.txt>` (case/whitespace-mangled but content-complete) → rc 0 (D-07 normalized-substring match).
      - audit-staged (RESTR-05/Crit-5): `gates/audit-staged.sh <with-import.md>` → exit 2 (the @import block, per O-1); `<clean-doc.md>` (a ≤100-line, @import-free file) → exit 0; `<oversized.md>` (>200 lines, hence >CLAUDE_MD_CAP=100) → exit 2 (cap-breach block per O-1). Export `CONJURE_HOME="$CONJURE_HOME"` when invoking (the helper must find audit-setup.sh — PATTERNS.md lines 164-171).
      - decision-scan (RESTR-06/Crit-6): `gates/decision-scan.sh <decision-doc.md>` → exit/stdout signal "individual"; `<clean-doc.md>` → signal "bulk". Assert on the documented signal (define the convention in Wave 1 — e.g. rc 1/exit-code or a stdout token; here assert whatever the Wave-1 contract specifies, written as a pending-but-present assertion).
      - extract-invariants (RESTR-04): `gates/extract-invariants.sh <source-claude>` writes a non-empty `INVARIANTS.candidates` (or the documented output path) under a temp `.conjure-adopt-state/` and exits 0; assert the candidates file is non-empty and contains expected tokens (e.g. `exit 2`).
    Add the integration + scaffold assertions:
      - criterion 1 scaffold (RESTR-01/02): mktemp a target, `cp -r "$P22_FIXTURE/." "$TGT/"` (reuse P22_FIXTURE=brownfield-simple, tests/run.sh:2377), run `CONJURE_HOME="$CONJURE_HOME" bash "$CONJURE_HOME/scripts/init-project.sh" existing "$TGT" >/dev/null 2>&1 || true`, then assert: `.claude/skills/restructure/SKILL.md` exists; `grep -q 'allowed-tools:.*Read.*Bash' "$TGT/.claude/skills/restructure/SKILL.md"`; `[ "$(wc -l < "$TGT/.claude/skills/restructure/SKILL.md")" -le 200 ]` (criterion 1 cap); `.claude/skills/restructure/gates/verify-invariants.sh` present (whole-dir copy ships gates — A1 RESOLVED).
      - apply-step routing (RESTR-02): reuse the Phase 22 op-executor drive idiom (tests/run.sh:2755-2801) against `_adopt-restructure-steps/adopt-manifest.json` — `--apply-step step-1`, jq-assert `status==applied` + dest changed. (This re-exercises the shipped seam from the skill's perspective; it passes immediately since Phase 22 shipped it — but it documents the RESTR-02 contract the skill rides.)
      - group-by (RESTR-01): drive a per-class count over `_adopt-restructure-steps/adopt-manifest.json`. The fixture's `files[]` array has exactly TWO entries (core, reference-doc) while `.summary` carries the aggregate counts. Assert from `files[]`: `jq '[.files[]|select(.classification=="core")]|length'` → 1 and `jq '[.files[]|select(.classification=="reference-doc")]|length'` → 1 (these are the per-file buckets the skill groups by); `jq '[.files[]|select(.classification=="unknown")]|length'` → 0 (no unknown entry in files[]). If the summary-level count is needed, assert `jq '.summary.unknown'` → 1 separately — do NOT conflate `.summary.unknown` (1) with the `files[]` selection (0).
      - archive-last (Crit-6): assert the skill's proposed-step ordering places `op:archive` after `op:write`/`op:extract` — guarded on P23_RESTR_OK; in Wave 0 this is a graceful red until the skill's proposal logic exists (or assert against a Wave-2 fixture the skill emits).
      - non-TTY approval exit-2 (RESTR-03/Crit-3): drive the skill's approval entry (the helper/driver Wave 2 ships) with `< /dev/null` (non-TTY) and `[ -t 0 ]` false, capture rc, expect 2 (D-12, mirror tests/run.sh:2696-2729). Guarded on P23_RESTR_OK.
      - bulk summary log (RESTR-03/Crit-3): after a bulk apply drive, assert exactly ONE `RESTRUCTURE` summary line in RESTRUCTURE-LOG.md for the bucket (D-09). Guarded on P23_RESTR_OK.
    Each sandbox uses `mktemp -d` + `trap 'rm -rf "$VAR"' EXIT` set then `trap - EXIT` reset (or a shared EXIT cleanup), exactly per the Phase 22 sections. Do NOT write any hash/record file INTO a tree that a later `diff`/`cmp` compares (the 22-03-SUMMARY Rule-1 bug — keep record files in a separate mktemp).
    Set DRY_RUN, CONJURE_HOME, and CONJURE_FORCE_INTERACTIVE env vars explicitly per invocation as the Phase 22 block does. NEVER use `exit 1` anywhere in the block (use the pass/fail helpers; hard failures in helpers use exit 2). No `mapfile`/associative arrays/`local -n` (bash 3.2.57).
  </action>
  <acceptance_criteria>
    - `bash tests/run.sh 2>&1 | grep -c '▸ Phase 23 — restructure gate helpers'` → 1 (the section header is emitted).
    - `bash tests/run.sh 2>&1 | grep -iE 'Wave 1 must create|Wave 2 must create'` → non-empty (graceful-red guard messages present while the helpers/SKILL.md are absent).
    - `bash tests/run.sh` exits NON-zero in Wave 0 (the new Phase 23 asserts are red) BUT the pre-Phase-23 PASS count is preserved: `bash tests/run.sh 2>&1 | grep -c '  ✗ '` counts only NEW Phase 23 reds (no Phase ≤22 assertion flips to ✗ — diff the failing lines against a pre-change run to confirm zero regression).
    - `bash -n tests/run.sh` → parses clean (no syntax error).
    - `grep -v '^#' tests/run.sh | grep -c 'exit 1'` → unchanged from baseline (the new block adds zero `exit 1`).
    - The block is inserted before the `# End Phase 22 test block` marker (grep confirms the `▸ Phase 23` header appears at a line number < the `# End Phase 22 test block` line, OR before the `# Summary` line and the GH_HIDE_STUBS cleanup).
  </acceptance_criteria>
  <verify>
    <automated>bash -n tests/run.sh && bash tests/run.sh 2>&1 | grep -c '▸ Phase 23 — restructure gate helpers' | grep -qx 1 && bash tests/run.sh 2>&1 | grep -qiE 'Wave 1 must create|Wave 2 must create' && echo OK</automated>
  </verify>
  <done>The Phase 23 section is present in tests/run.sh, emits its header, reports graceful RED with "Wave N must create ..." messages while helpers/SKILL.md are absent, parses clean under `bash -n`, adds zero `exit 1`, and introduces no regression in pre-Phase-23 assertions.</done>
</task>

<task type="auto">
  <name>Task 2: Synthetic gate fixtures under tests/fixtures/_restructure-gates/</name>
  <files>tests/fixtures/_restructure-gates/with-invariant.md, tests/fixtures/_restructure-gates/missing-invariant.md, tests/fixtures/_restructure-gates/INVARIANTS.txt, tests/fixtures/_restructure-gates/reflowed-invariant.md, tests/fixtures/_restructure-gates/with-import.md, tests/fixtures/_restructure-gates/oversized.md, tests/fixtures/_restructure-gates/decision-doc.md, tests/fixtures/_restructure-gates/clean-doc.md</files>
  <read_first>
    - .planning/phases/23-restructure-skill-safety-gates/23-PATTERNS.md (lines 282-288: the synthetic-fixture `printf` heredoc idiom + the exact fixture contents sketch).
    - .planning/phases/23-restructure-skill-safety-gates/23-RESEARCH.md (lines 453-457: Wave 0 Gaps — the six fixture shapes needed; lines 313-314: canonical-token invariant extraction so INVARIANTS.txt holds tokens like `exit 2`, `@import`, `≤100`, command names, NOT full sentences).
    - .planning/phases/23-restructure-skill-safety-gates/23-VALIDATION.md (lines 62-64: O-1/O-2 resolutions — audit gate blocks on @import + cap breach >CLAUDE_MD_CAP (100; SKILL >200); invariants are canonical TOKENS).
    - lib/caps.sh (the cap literals: CLAUDE_MD_CAP=100, SKILL_MD_CAP=200 — so `oversized.md` must be >200 lines to trip the HARD cap that audit-staged blocks on).
    - tests/fixtures/_adopt-restructure-steps/adopt-manifest.json (the existing fixture being REUSED for group-by/apply-step — do not recreate it).
  </read_first>
  <action>
    Create the `tests/fixtures/_restructure-gates/` directory with these small, hand-authored fixtures (use the Write tool; these are committed test fixtures, not transient state). **The leading underscore is REQUIRED** (checker B2): `tests/run.sh` has generic loops at lines 326/368/390 iterating `tests/fixtures/[^_]*/` that run `audit-setup.sh` (expect rc 0) and `conjure init --dry-run` + `diff -r` (expect byte-identical) on every NON-underscore fixture dir. These Phase 23 fixtures (`with-import.md`, `oversized.md` >200 lines, etc.) are NOT valid harness profiles and WOULD regress those loops. STATE.md:73 documents the lesson: `_`-prefixed fixture dirs are excluded from the generic loops. So the dir MUST be `_restructure-gates/` and every path in this plan + 23-02 already uses that name.

    Fixture contents (canonical-token aligned, O-2):
      - `INVARIANTS.txt`: one canonical token per line — e.g. `exit 2`, `@import`, `≤100`, `mutate.sh`, `do not delete`. These are the tokens the verify-invariants gate substring-matches after normalization.
      - `with-invariant.md`: a short proposed CLAUDE.md whose prose CONTAINS every INVARIANTS.txt token (possibly reworded around them, but each token appears verbatim after normalization) — e.g. a paragraph stating "hooks must exit 2; never use @import; CLAUDE.md stays ≤100 lines; all writes route through mutate.sh; do not delete user files". → verify-invariants rc 0.
      - `missing-invariant.md`: a proposed CLAUDE.md that DROPS at least one token (e.g. omits `exit 2`) — → verify-invariants rc 2 + missing list.
      - `reflowed-invariant.md`: same content as with-invariant.md but case-mangled + whitespace-mangled (UPPERCASED words, tokens split across lines, multiple spaces) while still containing every token — proves D-07 normalized-substring match → rc 0.
      - `with-import.md`: a CLAUDE.md whose FIRST non-blank line is `@import ./extra.md` (the `^@` audit trigger) — → audit-staged rc 2.
      - `oversized.md`: a CLAUDE.md with >200 lines (generate via a `printf`/loop in the fixture or author 201+ trivial lines). Since 201 > CLAUDE_MD_CAP(100), it trips the audit-staged cap-breach block → exit 2. (>200 gives unambiguous margin above the 100 cap; the gate's block per O-1 is on @import OR line count > CLAUDE_MD_CAP=100.)
      - `decision-doc.md`: an archive-candidate doc containing decision vocabulary — e.g. "We decided to keep the legacy adapter; never remove it; rationale: downstream depends on it." (matches `we chose`/`decided`/`rationale`/`never`) → decision-scan signals individual.
      - `clean-doc.md`: a short (<100-line), @import-free reference doc with NO decision vocabulary — serves as both the audit-clean rc-0 case AND the decision-scan "bulk-eligible" case.
    Keep every fixture minimal (a handful of lines except oversized.md). Do NOT include real secrets or paths outside the fixture.
  </action>
  <acceptance_criteria>
    - All eight fixture files exist under the chosen `tests/fixtures/_restructure-gates/` (or `_restructure-gates/`) dir; Task 1's `<read_first>` fixture paths match the chosen name.
    - `grep -c . tests/fixtures/_restructure-gates/INVARIANTS.txt` ≥ 3 (at least three canonical tokens, one per line).
    - `head -1 tests/fixtures/_restructure-gates/with-import.md` (first non-blank) starts with `@` (the audit `^@` trigger).
    - `[ "$(wc -l < tests/fixtures/_restructure-gates/oversized.md)" -gt 200 ]` → true (trips the HARD cap).
    - `grep -iE 'decided|we chose|rationale|do not|never' tests/fixtures/_restructure-gates/decision-doc.md` → non-empty; the same grep on `clean-doc.md` → empty.
    - Every token in `INVARIANTS.txt`, after lowercase+whitespace-collapse, is a substring of the lowercase+whitespace-collapsed `with-invariant.md` AND `reflowed-invariant.md`; at least one token is NOT a substring of `missing-invariant.md` (this is the contract the Wave 1 verify-invariants gate will assert against).
    - The chosen fixture dir name is confirmed against tests/run.sh's generic fixture loops (not accidentally swept as a harness-audit fixture).
  </acceptance_criteria>
  <verify>
    <automated>test -f tests/fixtures/_restructure-gates/INVARIANTS.txt && [ "$(grep -c . tests/fixtures/_restructure-gates/INVARIANTS.txt)" -ge 3 ] && [ "$(wc -l < tests/fixtures/_restructure-gates/oversized.md)" -gt 200 ] && grep -iqE 'decided|we chose|rationale|do not|never' tests/fixtures/_restructure-gates/decision-doc.md && ! grep -iqE 'decided|we chose|rationale|do not|never' tests/fixtures/_restructure-gates/clean-doc.md && echo OK</automated>
  </verify>
  <done>All synthetic gate fixtures exist with the canonical-token contract: INVARIANTS.txt holds tokens (not sentences), with-invariant/reflowed-invariant contain every token, missing-invariant drops one, with-import trips `^@`, oversized exceeds 200 lines, decision-doc matches the 5-term scan, clean-doc matches neither. The fixture dir name does not collide with the generic harness-audit loops.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → production scripts | tests/run.sh invokes the (future) gate helpers + init-project.sh in mktemp sandboxes; untrusted only insofar as a buggy helper could write outside its sandbox |
| fixture files → gate helpers | the synthetic fixtures are the data the gates read; crafted to exercise both block and pass paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | Tampering | Wave 0 assertion writes a record file into a compared tree (the 22-03-SUMMARY Rule-1 bug class) | mitigate | Keep any sha/record file in a separate `mktemp`, never inside a tree a later `diff`/`cmp` compares; acceptance criterion forbids it |
| T-23-02 | Denial of Service | a graceful-red guard crashes instead of failing, aborting the whole suite | mitigate | Every Phase 23 sub-section guards on `P23_RESTR_OK`/`P23_GATES_OK` and emits `fail "... Wave N must create ..."`; `bash -n` + a Wave-0 run confirm no crash |
| T-23-03 | Tampering | fixture dir swept by the generic harness-audit/golden-EXPECT loop, producing spurious failures | mitigate | Name the dir to avoid the `tests/fixtures/[^_]*/` loop (leading `_` if needed); acceptance criterion verifies against the loop patterns |
| T-23-04 | Repudiation | a new `exit 1` slips into the test block, violating the project convention gate | accept | Low risk in a test file (uses pass/fail helpers, not exit); convention gate `grep -c 'exit 1'` is checked in the verify and stays at baseline |
| T-23-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — this phase installs ZERO external packages (RESEARCH Package Legitimacy Audit); no slopcheck required. No `[ASSUMED]`/`[SUS]` packages |
</threat_model>

<verification>
- `bash -n tests/run.sh` parses clean.
- `bash tests/run.sh 2>&1 | grep -c '▸ Phase 23 — restructure gate helpers'` == 1.
- The new section reports graceful RED (Wave 1/2 guard messages) and does NOT crash the suite.
- No pre-Phase-23 assertion regresses (diff the ✗ lines against a baseline run).
- `grep -v '^#' tests/run.sh | grep -c 'exit 1'` unchanged from baseline.
- All eight fixtures exist with the canonical-token contract (Task 2 acceptance criteria).
</verification>

<success_criteria>
The Phase 23 test block exists, emits its header, reports a clear graceful RED for every Wave 1/2 deliverable (the four gate helpers, the scaffold-install criterion 1, the non-TTY exit-2 guard, the apply-step routing, group-by, archive-last), and the synthetic fixtures are in place — so every later task verifies against a concrete red→green signal that already exists. Mirrors the Phase 22 Wave 0 precedent exactly.
</success_criteria>

<output>
Create `.planning/phases/23-restructure-skill-safety-gates/23-01-SUMMARY.md` when done.
</output>
