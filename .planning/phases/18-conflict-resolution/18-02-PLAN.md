---
phase: 18-conflict-resolution
plan: 02
type: execute
wave: 2
depends_on:
  - 18-01
files_modified:
  - cli/conjure
  - tests/run.sh
autonomous: true
requirements:
  - RESOLVE-01
  - RESOLVE-02

must_haves:
  truths:
    - "conjure resolve dispatches to scripts/resolve.sh (cmd_resolve in cli/conjure)"
    - "conjure resolve appears in usage/help output"
    - "RESOLVE regression tests pass: non-interactive guard, all-clear, keep action, apply action"
  artifacts:
    - path: "cli/conjure"
      provides: "cmd_resolve function and dispatch entry"
      contains: "cmd_resolve"
    - path: "tests/run.sh"
      provides: "RESOLVE regression tests section"
      contains: "RESOLVE-01"
  key_links:
    - from: "cli/conjure cmd_resolve"
      to: "scripts/resolve.sh"
      via: "bash exec"
      pattern: "bash.*scripts/resolve\\.sh"
---

<objective>
Wire cmd_resolve into cli/conjure (dispatch + usage) and add the RESOLVE regression
test section to tests/run.sh covering all four acceptance behaviors.

Purpose: Make `conjure resolve` a real CLI subcommand and verify all RESOLVE-01/RESOLVE-02
behaviors with automated regression tests mirroring the DRIFT-01/DRIFT-02 test pattern.

Output: cli/conjure updated with cmd_resolve; tests/run.sh updated with RESOLVE section
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-conflict-resolution/18-CONTEXT.md
@.planning/phases/18-conflict-resolution/18-01-SUMMARY.md
@cli/conjure
@scripts/resolve.sh
@tests/run.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add cmd_resolve and dispatch entry to cli/conjure</name>
  <files>cli/conjure</files>
  <read_first>
    - cli/conjure lines 27-52 — usage() function: add "conjure resolve [target]" line matching existing format
    - cli/conjure lines 160-173 — cmd_check() as immediate structural predecessor: same arg-parsing pattern (local target + while loop + shift); model cmd_resolve identically
    - cli/conjure lines 339-354 — dispatch case block: add "resolve)" entry adjacent to "check)" entry
    - .planning/phases/18-conflict-resolution/18-01-SUMMARY.md — confirm scripts/resolve.sh path and env vars it expects
  </read_first>
  <action>
    Add cmd_resolve function to cli/conjure, positioned after cmd_check (around line 174):

    cmd_resolve() {
      local target dryrun
      target="$(pwd)"
      dryrun=0
      while [ $# -gt 0 ]; do
        case "$1" in
          --dry-run)   dryrun=1 ;;
          --help|-h)   echo "Usage: conjure resolve [--dry-run] [target]"; return 0 ;;
          *)           target="$1" ;;
        esac
        shift
      done
      CONJURE_HOME="$CONJURE_HOME" DRY_RUN="$dryrun" \
        bash "$CONJURE_HOME/scripts/resolve.sh" "$target"
    }

    Add usage line in usage() function, after the "conjure check" line:
      conjure resolve [--dry-run] [target]

    Add dispatch entry in the case block, after "check)":
      resolve)  shift; cmd_resolve "$@" ;;

    Per CONTEXT.md D-implementation-structure: cmd_resolve delegates entirely to scripts/resolve.sh via bash exec with CONJURE_HOME and DRY_RUN forwarded. No logic in the cmd_ wrapper beyond arg parsing and env forwarding — mirrors cmd_check exactly.

    Do NOT change any other existing functions or the dispatch order for other commands.
  </action>
  <verify>
    <automated>
      bash -n cli/conjure
      grep -q 'cmd_resolve' cli/conjure
      grep -q 'resolve)' cli/conjure
      grep -q 'conjure resolve' cli/conjure
      CONJURE_HOME="$(pwd)" cli/conjure help resolve 2>&1 | grep -q "resolve"
    </automated>
  </verify>
  <acceptance_criteria>
    - bash -n cli/conjure passes
    - cmd_resolve function exists in cli/conjure
    - dispatch case includes "resolve)" entry
    - usage output includes "conjure resolve"
    - CONJURE_HOME="$(pwd)" cli/conjure resolve --help prints usage line and exits 0
    - DRY_RUN is forwarded to scripts/resolve.sh via env var
  </acceptance_criteria>
  <done>cli/conjure has a working cmd_resolve that delegates to scripts/resolve.sh with CONJURE_HOME and DRY_RUN forwarded, and "conjure resolve" appears in usage output.</done>
</task>

<task type="auto">
  <name>Task 2: Add RESOLVE regression tests to tests/run.sh</name>
  <files>tests/run.sh</files>
  <read_first>
    - tests/run.sh lines 1378-1466 — DRIFT detection test section: study the exact pattern (section banner, mktemp -d, trap EXIT, invoke via cli/conjure, check RC, pass/fail, rm -rf, trap - EXIT). All RESOLVE tests MUST follow this pattern exactly.
    - tests/run.sh lines 1460-1466 — Summary block at end: new RESOLVE section inserts BEFORE this summary block.
    - scripts/resolve.sh — confirm it sources lib/mutate.sh, uses find "$TARGET" -name '.conjure-conflict-*', and reads from stdin for prompt.
    - lib/merge.sh lines 59-73 — sidecar naming: .conjure-conflict-${encoded} where encoded = rel with / replaced by _; sits in sidecar_dir = dirname current_file. For top-level test: current_file = TARGET/foo.txt → sidecar = TARGET/.conjure-conflict-foo.txt
  </read_first>
  <action>
    Insert a new RESOLVE section in tests/run.sh, between the last DRIFT test cleanup block (trap - EXIT after DRIFT-02) and the summary block (echo "═══..."). The section must use the exact same idiom as DRIFT tests: mktemp -d, set trap EXIT, invoke via cli/conjure (not direct bash), capture RC with || RC=$?, pass/fail, rm -rf SANDBOX, trap - EXIT.

    Section header (2 lines):
      echo
      echo "▸ Conflict resolution tests (RESOLVE-01, RESOLVE-02)"

    Sub-case RESOLVE-01a — non-interactive guard:
      - RESOLVE_DIR="$(mktemp -d)"
      - trap 'rm -rf "$RESOLVE_DIR"' EXIT
      - Create a fake sidecar: printf 'upstream content\n' > "$RESOLVE_DIR/.conjure-conflict-foo.txt"
      - Create current file: printf 'my content\n' > "$RESOLVE_DIR/foo.txt"
      - Run: RESOLVE_RC=0; CONJURE_HOME="$CONJURE_HOME" cli/conjure resolve "$RESOLVE_DIR" </dev/null >/dev/null 2>&1 || RESOLVE_RC=$?
      - Assert RESOLVE_RC -eq 2: pass "resolve exits 2 when stdin is not a TTY (RESOLVE-01)" / fail ...
      - rm -rf "$RESOLVE_DIR"; trap - EXIT
      NOTE: </dev/null redirects stdin to a non-TTY (closed pipe), triggering the [ -t 0 ] guard. This is the established test pattern for non-interactive guards.

    Sub-case RESOLVE-02a — all-clear on empty dir:
      - RESOLVE_DIR="$(mktemp -d)"
      - trap 'rm -rf "$RESOLVE_DIR"' EXIT
      - No sidecars created
      - ALLCLEAR_OUT="$(CONJURE_HOME="$CONJURE_HOME" cli/conjure resolve "$RESOLVE_DIR" </dev/null 2>&1 || true)"
      NOTE: all-clear path does NOT need TTY — it exits before prompting. The non-interactive guard only fires when sidecars are present. BUT if the implementation checks [ -t 0 ] unconditionally at start (before sidecar discovery), the all-clear test will also get exit 2. Check scripts/resolve.sh: if the guard is at the top before the sidecar scan, test with a TTY-less stdin will hit exit 2. Since all tests run non-interactively, the all-clear test must be structured to avoid the guard. Two options: (a) run with a PTY via script(1) — too complex; (b) move the guard AFTER sidecar discovery in scripts/resolve.sh so all-clear path does not require TTY. The CONTEXT.md spec says "guard [ -t 0 ] and exits 2 when non-interactive" — this is for the prompting path. The all-clear path (zero sidecars) should NOT require TTY. Implement scripts/resolve.sh to check sidecar count FIRST; if zero, print "No conflicts remain" and exit 0 immediately without checking [ -t 0 ]. The non-interactive guard should appear AFTER the all-clear short-circuit.

      With that ordering: ALLCLEAR_RC=0; CONJURE_HOME="$CONJURE_HOME" cli/conjure resolve "$RESOLVE_DIR" </dev/null >/dev/null 2>&1 || ALLCLEAR_RC=$?
      - Assert ALLCLEAR_RC -eq 0: pass / fail
      - Assert output contains "No conflicts remain": capture and grep
      - rm -rf "$RESOLVE_DIR"; trap - EXIT

    Sub-case RESOLVE-02b — keep action (sandbox with real TTY required):
      Since tests run non-interactively, test the worker directly via bash with stdin piped. Bypass the CLI layer for action tests to avoid TTY requirement.
      - RESOLVE_DIR="$(mktemp -d)"
      - trap 'rm -rf "$RESOLVE_DIR"' EXIT
      - printf 'upstream content\n' > "$RESOLVE_DIR/.conjure-conflict-foo.txt"
      - printf 'my content\n' > "$RESOLVE_DIR/foo.txt"
      - Run worker directly with piped "k\n" input: KEEP_OUT="$(printf 'k\n' | CONJURE_HOME="$CONJURE_HOME" DRY_RUN=0 bash "$CONJURE_HOME/scripts/resolve.sh" "$RESOLVE_DIR" 2>&1 || true)"
      NOTE: Direct bash invocation bypasses [ -t 0 ] guard because stdin IS a pipe; but the guard will trigger exit 2. The keep/apply tests therefore MUST test via a mechanism that satisfies the TTY check. Solution: use bash -c with a /dev/tty redirect if available, OR structure the test as a subprocess using script(1) — both are too complex. Alternative: the guard in scripts/resolve.sh checks [ -t 0 ] which is true for a terminal. In a test with piped stdin this is false → guard fires. The only way to test keep/apply without a real TTY is to bypass the guard using an env var escape hatch (e.g. CONJURE_FORCE_INTERACTIVE=1 for testing). Add CONJURE_FORCE_INTERACTIVE support to scripts/resolve.sh: [ -t 0 ] || [ "${CONJURE_FORCE_INTERACTIVE:-0}" = "1" ] || { echo "..." >&2; exit 2; }. This is a well-established test-escape pattern for TTY guards and does not weaken the production path since the env var is unset by default.

      With CONJURE_FORCE_INTERACTIVE=1:
      - Keep test: printf 'k\n' | CONJURE_HOME="$CONJURE_HOME" DRY_RUN=0 CONJURE_FORCE_INTERACTIVE=1 bash "$CONJURE_HOME/scripts/resolve.sh" "$RESOLVE_DIR" >/dev/null 2>&1 || true
      - Assert sidecar removed: [ ! -f "$RESOLVE_DIR/.conjure-conflict-foo.txt" ] → pass/fail
      - Assert current file unchanged: grep -q 'my content' "$RESOLVE_DIR/foo.txt" → pass/fail
      - rm -rf "$RESOLVE_DIR"; trap - EXIT

    Sub-case RESOLVE-02c — apply action:
      - RESOLVE_DIR="$(mktemp -d)"
      - trap 'rm -rf "$RESOLVE_DIR"' EXIT
      - printf 'upstream content\n' > "$RESOLVE_DIR/.conjure-conflict-foo.txt"
      - printf 'my content\n' > "$RESOLVE_DIR/foo.txt"
      - printf 'a\n' | CONJURE_HOME="$CONJURE_HOME" DRY_RUN=0 CONJURE_FORCE_INTERACTIVE=1 bash "$CONJURE_HOME/scripts/resolve.sh" "$RESOLVE_DIR" >/dev/null 2>&1 || true
      - Assert sidecar removed: [ ! -f "$RESOLVE_DIR/.conjure-conflict-foo.txt" ] → pass/fail
      - Assert current file content matches sidecar: grep -q 'upstream content' "$RESOLVE_DIR/foo.txt" → pass/fail
      - rm -rf "$RESOLVE_DIR"; trap - EXIT

    IMPORTANT: The CONJURE_FORCE_INTERACTIVE=1 env var requires a corresponding change in scripts/resolve.sh. The test action for this plan must note: "scripts/resolve.sh must be updated simultaneously to support CONJURE_FORCE_INTERACTIVE=1 escape hatch — update the guard line before adding these tests."

    Update scripts/resolve.sh guard line from:
      [ -t 0 ] || { echo "conjure resolve: stdin is not a TTY — interactive mode required" >&2; exit 2; }
    to:
      [ -t 0 ] || [ "${CONJURE_FORCE_INTERACTIVE:-0}" = "1" ] || { echo "conjure resolve: stdin is not a TTY — interactive mode required" >&2; exit 2; }

    Verify ordering in scripts/resolve.sh: sidecar discovery and all-clear check MUST come BEFORE the non-interactive guard so the all-clear path is reachable without TTY. Guard ordering:
      1. source lib/mutate.sh
      2. find sidecars into tmpfile
      3. if tmpfile empty → print "No conflicts remain", exit 0  (no TTY needed)
      4. [ -t 0 ] || [ "${CONJURE_FORCE_INTERACTIVE:-0}" = "1" ] || exit 2  (guard for prompting path only)
      5. main prompt loop

    No heredocs in tests/run.sh — use printf '%s\n' for multiline strings. No fenced code blocks here.
    Follow the DRIFT test idiom exactly: each sub-case is self-contained (mktemp → setup → run → assert → rm -rf → trap -).
  </action>
  <verify>
    <automated>
      bash -n tests/run.sh
      bash tests/run.sh 2>&1 | grep -E "RESOLVE-01|RESOLVE-02" | grep -v "✗"
    </automated>
  </verify>
  <acceptance_criteria>
    - bash -n tests/run.sh passes (no syntax errors introduced)
    - tests/run.sh contains the RESOLVE section header "▸ Conflict resolution tests (RESOLVE-01, RESOLVE-02)"
    - RESOLVE-01a: piped-stdin invocation of `conjure resolve` exits 2 (non-interactive guard)
    - RESOLVE-02a: `conjure resolve` on empty dir (</dev/null) prints "No conflicts remain" and exits 0 — all-clear path bypasses TTY guard
    - RESOLVE-02b: keep action (CONJURE_FORCE_INTERACTIVE=1) removes sidecar, leaves current file unchanged
    - RESOLVE-02c: apply action (CONJURE_FORCE_INTERACTIVE=1) replaces current file with sidecar content, removes sidecar
    - Full test suite passes: bash tests/run.sh exits 0
    - scripts/resolve.sh is updated with CONJURE_FORCE_INTERACTIVE escape hatch AND guard placed after all-clear check
  </acceptance_criteria>
  <done>tests/run.sh has a complete RESOLVE section covering all four acceptance behaviors; full test suite passes with 0 failures; scripts/resolve.sh ordering and escape hatch are correct.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cli/conjure arg $1 → resolve | User-controlled target path forwarded to resolve.sh |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-04 | Tampering | CONJURE_FORCE_INTERACTIVE env var | accept | Test-only escape hatch; documented as testing aid; unset in production CI; no security boundary crossed since resolve only removes sidecar files in project tree |
| T-18-05 | Information Disclosure | resolve --help output | accept | No secrets in usage strings |
| T-18-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
1. bash -n cli/conjure (syntax)
2. bash -n tests/run.sh (syntax)
3. grep -q cmd_resolve cli/conjure
4. grep -q 'resolve)' cli/conjure
5. CONJURE_HOME="$(pwd)" cli/conjure resolve --help → exits 0, prints usage
6. bash tests/run.sh 2>&1 | tail -5 → FAIL: 0
7. bash tests/run.sh 2>&1 | grep -E "(RESOLVE-01|RESOLVE-02)" | grep "✓" | wc -l → should be ≥ 4
</verification>

<success_criteria>
- cli/conjure dispatches `resolve` to scripts/resolve.sh with CONJURE_HOME and DRY_RUN forwarded
- "conjure resolve" appears in usage() output
- RESOLVE-01: non-interactive guard test passes (exit 2 on piped stdin)
- RESOLVE-02: all-clear test passes (exit 0 + "No conflicts remain" on empty dir)
- RESOLVE-02: keep action test passes (sidecar removed, current file unchanged)
- RESOLVE-02: apply action test passes (current file updated, sidecar removed)
- Full test suite: bash tests/run.sh exits 0
</success_criteria>

<output>
Create `.planning/phases/18-conflict-resolution/18-02-SUMMARY.md` when done.
</output>
