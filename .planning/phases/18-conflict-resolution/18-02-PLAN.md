---
phase: 18-conflict-resolution
plan: 02
type: execute
wave: 2
depends_on:
  - 18-01
files_modified:
  - cli/conjure
  - tests/run.sh
autonomous: true
requirements:
  - RESOLVE-01
  - RESOLVE-02

must_haves:
  truths:
    - "conjure resolve dispatches to scripts/resolve.sh (cmd_resolve in cli/conjure)"
    - "conjure resolve appears in usage/help output"
    - "RESOLVE regression tests pass: non-interactive guard, all-clear, keep action, apply action"
  artifacts:
    - path: "cli/conjure"
      provides: "cmd_resolve function and dispatch entry"
      contains: "cmd_resolve"
    - path: "tests/run.sh"
      provides: "RESOLVE regression tests section"
      contains: "RESOLVE-01"
  key_links:
    - from: "cli/conjure cmd_resolve"
      to: "scripts/resolve.sh"
      via: "bash exec"
      pattern: "bash.*scripts/resolve\\.sh"
---

<objective>
Wire cmd_resolve into cli/conjure (dispatch + usage) and add the RESOLVE regression
test section to tests/run.sh covering all four acceptance behaviors.

Purpose: Make `conjure resolve` a real CLI subcommand and verify all RESOLVE-01/RESOLVE-02
behaviors with automated regression tests mirroring the DRIFT-01/DRIFT-02 test pattern.

Output: cli/conjure updated with cmd_resolve; tests/run.sh updated with RESOLVE section
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-conflict-resolution/18-CONTEXT.md
@.planning/phases/18-conflict-resolution/18-01-SUMMARY.md
@cli/conjure
@scripts/resolve.sh
@tests/run.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add cmd_resolve and dispatch entry to cli/conjure</name>
  <files>cli/conjure</files>
  <read_first>
    - cli/conjure lines 27-52 — usage() function: add "conjure resolve [target]" line matching existing format
    - cli/conjure lines 160-173 — cmd_check() as immediate structural predecessor: same arg-parsing pattern (local target + while loop + shift); model cmd_resolve identically
    - cli/conjure lines 339-354 — dispatch case block: add "resolve)" entry adjacent to "check)" entry
    - .planning/phases/18-conflict-resolution/18-01-SUMMARY.md — confirm scripts/resolve.sh path and env vars it expects
  </read_first>
  <action>
    Add cmd_resolve function to cli/conjure, positioned after cmd_check (around line 174):

    cmd_resolve() {
      local target dryrun
      target="$(pwd)"
      dryrun=0
      while [ $# -gt 0 ]; do
        case "$1" in
          --dry-run)   dryrun=1 ;;
          --help|-h)   echo "Usage: conjure resolve [--dry-run] [target]"; return 0 ;;
          *)           target="$1" ;;
        esac
        shift
      done
      CONJURE_HOME="$CONJURE_HOME" DRY_RUN="$dryrun" \
        bash "$CONJURE_HOME/scripts/resolve.sh" "$target"
    }

    Add usage line in usage() function, after the "conjure check" line:
      conjure resolve [--dry-run] [target]

    Add dispatch entry in the case block, after "check)":
      resolve)  shift; cmd_resolve "$@" ;;

    Per CONTEXT.md D-implementation-structure: cmd_resolve delegates entirely to scripts/resolve.sh via bash exec with CONJURE_HOME and DRY_RUN forwarded. No logic in the cmd_ wrapper beyond arg parsing and env forwarding — mirrors cmd_check exactly.

    Do NOT change any other existing functions or the dispatch order for other commands.
  </action>
  <verify>
    <automated>
      bash -n cli/conjure
      grep -q 'cmd_resolve' cli/conjure
      grep -q 'resolve)' cli/conjure
      grep -q 'conjure resolve' cli/conjure
      CONJURE_HOME="$(pwd)" cli/conjure help resolve 2>&1 | grep -q "resolve"
    </automated>
  </verify>
  <acceptance_criteria>
    - bash -n cli/conjure passes
    - cmd_resolve function exists in cli/conjure
    - dispatch case includes "resolve)" entry
    - usage output includes "conjure resolve"
    - CONJURE_HOME="$(pwd)" cli/conjure resolve --help prints usage line and exits 0
    - DRY_RUN is forwarded to scripts/resolve.sh via env var
  </acceptance_criteria>
  <done>cli/conjure has a working cmd_resolve that delegates to scripts/resolve.sh with CONJURE_HOME and DRY_RUN forwarded, and "conjure resolve" appears in usage output.</done>
</task>

<task type="auto">
  <name>Task 2: Add RESOLVE regression tests to tests/run.sh</name>
  <files>tests/run.sh</files>
  <read_first>
    - tests/run.sh lines 1378-1466 — DRIFT detection test section: study the exact pattern (section banner, mktemp -d, trap EXIT, invoke via cli/conjure, check RC, pass/fail, rm -rf, trap - EXIT). All RESOLVE tests MUST follow this pattern exactly.
    - tests/run.sh lines 1460-1466 — Summary block at end: new RESOLVE section inserts BEFORE this summary block.
    - scripts/resolve.sh — confirm it: (a) sources lib/mutate.sh, (b) runs find before the TTY guard, (c) exits 0 with "No conflicts remain" before the TTY guard when no sidecars are found, (d) has guard [ -t 0 ] || [ "${CONJURE_FORCE_INTERACTIVE:-0}" = "1" ] || exit 2 AFTER the all-clear check, (e) reads from stdin for the prompt loop. These properties were established in Plan 01 — do NOT modify scripts/resolve.sh here.
    - lib/merge.sh lines 59-73 — sidecar naming: .conjure-conflict-${encoded} where encoded = rel with / replaced by _; sits in sidecar_dir = dirname current_file. For top-level test: current_file = TARGET/foo.txt → sidecar = TARGET/.conjure-conflict-foo.txt
  </read_first>
  <action>
    Insert a new RESOLVE section in tests/run.sh, between the last DRIFT test cleanup block (trap - EXIT after DRIFT-02) and the summary block (echo "═══..."). The section must use the exact same idiom as DRIFT tests: mktemp -d, set trap EXIT, invoke via cli/conjure (not direct bash) where appropriate, capture RC with || RC=$?, pass/fail, rm -rf SANDBOX, trap - EXIT.

    Section header (2 lines):
      echo
      echo "▸ Conflict resolution tests (RESOLVE-01, RESOLVE-02)"

    Sub-case RESOLVE-01a — non-interactive guard:
      - RESOLVE_DIR="$(mktemp -d)"
      - trap 'rm -rf "$RESOLVE_DIR"' EXIT
      - Create a fake sidecar: printf 'upstream content\n' > "$RESOLVE_DIR/.conjure-conflict-foo.txt"
      - Create current file: printf 'my content\n' > "$RESOLVE_DIR/foo.txt"
      - Run: RESOLVE_RC=0; CONJURE_HOME="$CONJURE_HOME" cli/conjure resolve "$RESOLVE_DIR" </dev/null >/dev/null 2>&1 || RESOLVE_RC=$?
      - Assert RESOLVE_RC -eq 2: pass "resolve exits 2 when stdin is not a TTY (RESOLVE-01)" / fail ...
      - rm -rf "$RESOLVE_DIR"; trap - EXIT
      NOTE: </dev/null redirects stdin to a non-TTY (closed pipe), triggering the [ -t 0 ] guard. CONJURE_FORCE_INTERACTIVE is NOT set here, so the guard fires. This is the established test pattern for non-interactive guards.

    Sub-case RESOLVE-02a — all-clear on empty dir:
      - RESOLVE_DIR="$(mktemp -d)"
      - trap 'rm -rf "$RESOLVE_DIR"' EXIT
      - No sidecars created
      - ALLCLEAR_RC=0; ALLCLEAR_OUT="$(CONJURE_HOME="$CONJURE_HOME" cli/conjure resolve "$RESOLVE_DIR" </dev/null 2>&1)" || ALLCLEAR_RC=$?
      NOTE: The all-clear path does NOT require a TTY because scripts/resolve.sh (built in Plan 01) performs the all-clear check BEFORE the [ -t 0 ] guard. When no sidecars are found, the script prints "No conflicts remain" and exits 0 without ever reaching the TTY check. This is why </dev/null works here.
      - Assert ALLCLEAR_RC -eq 0: pass "resolve exits 0 on empty dir (RESOLVE-02)" / fail ...
      - Assert output contains "No conflicts remain": echo "$ALLCLEAR_OUT" | grep -q "No conflicts remain" → pass/fail
      - rm -rf "$RESOLVE_DIR"; trap - EXIT

    Sub-case RESOLVE-02b — keep action:
      Scripts/resolve.sh supports CONJURE_FORCE_INTERACTIVE=1 (established in Plan 01) to bypass the TTY check when stdin is a pipe, enabling action tests in non-interactive CI.
      - RESOLVE_DIR="$(mktemp -d)"
      - trap 'rm -rf "$RESOLVE_DIR"' EXIT
      - printf 'upstream content\n' > "$RESOLVE_DIR/.conjure-conflict-foo.txt"
      - printf 'my content\n' > "$RESOLVE_DIR/foo.txt"
      - printf 'k\n' | CONJURE_HOME="$CONJURE_HOME" DRY_RUN=0 CONJURE_FORCE_INTERACTIVE=1 bash "$CONJURE_HOME/scripts/resolve.sh" "$RESOLVE_DIR" >/dev/null 2>&1 || true
      - Assert sidecar removed: [ ! -f "$RESOLVE_DIR/.conjure-conflict-foo.txt" ] → pass "keep removes sidecar (RESOLVE-02)" / fail ...
      - Assert current file unchanged: grep -q 'my content' "$RESOLVE_DIR/foo.txt" → pass "keep leaves current file unchanged (RESOLVE-02)" / fail ...
      - rm -rf "$RESOLVE_DIR"; trap - EXIT

    Sub-case RESOLVE-02c — apply action:
      - RESOLVE_DIR="$(mktemp -d)"
      - trap 'rm -rf "$RESOLVE_DIR"' EXIT
      - printf 'upstream content\n' > "$RESOLVE_DIR/.conjure-conflict-foo.txt"
      - printf 'my content\n' > "$RESOLVE_DIR/foo.txt"
      - printf 'a\n' | CONJURE_HOME="$CONJURE_HOME" DRY_RUN=0 CONJURE_FORCE_INTERACTIVE=1 bash "$CONJURE_HOME/scripts/resolve.sh" "$RESOLVE_DIR" >/dev/null 2>&1 || true
      - Assert sidecar removed: [ ! -f "$RESOLVE_DIR/.conjure-conflict-foo.txt" ] → pass "apply removes sidecar (RESOLVE-02)" / fail ...
      - Assert current file content matches sidecar: grep -q 'upstream content' "$RESOLVE_DIR/foo.txt" → pass "apply updates current file (RESOLVE-02)" / fail ...
      - rm -rf "$RESOLVE_DIR"; trap - EXIT

    No heredocs in tests/run.sh — use printf '%s\n' for multiline strings. No fenced code blocks here.
    Follow the DRIFT test idiom exactly: each sub-case is self-contained (mktemp → setup → run → assert → rm -rf → trap -).
    Do NOT modify scripts/resolve.sh — it is owned by Plan 01 and already contains CONJURE_FORCE_INTERACTIVE support and correct guard ordering.
  </action>
  <verify>
    <automated>
      bash -n tests/run.sh
      bash tests/run.sh 2>&1 | grep -E "RESOLVE-01|RESOLVE-02" | grep -v "✗"
    </automated>
  </verify>
  <acceptance_criteria>
    - bash -n tests/run.sh passes (no syntax errors introduced)
    - tests/run.sh contains the RESOLVE section header "▸ Conflict resolution tests (RESOLVE-01, RESOLVE-02)"
    - RESOLVE-01a: piped-stdin (</dev/null, no CONJURE_FORCE_INTERACTIVE) invocation of `conjure resolve` with sidecars present exits 2 (non-interactive guard)
    - RESOLVE-02a: `conjure resolve` on empty dir (</dev/null) prints "No conflicts remain" and exits 0 — all-clear path bypasses TTY guard (no modification to scripts/resolve.sh needed here — Plan 01 already implements this ordering)
    - RESOLVE-02b: keep action (CONJURE_FORCE_INTERACTIVE=1) removes sidecar, leaves current file unchanged
    - RESOLVE-02c: apply action (CONJURE_FORCE_INTERACTIVE=1) replaces current file with sidecar content, removes sidecar
    - Full test suite passes: bash tests/run.sh exits 0
  </acceptance_criteria>
  <done>tests/run.sh has a complete RESOLVE section covering all four acceptance behaviors; full test suite passes with 0 failures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cli/conjure arg $1 → resolve | User-controlled target path forwarded to resolve.sh |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-05 | Tampering | CONJURE_FORCE_INTERACTIVE in test invocations | accept | Set only in test harness; unset in production CI; no security boundary crossed since resolve only removes sidecar files in project tree |
| T-18-06 | Information Disclosure | resolve --help output | accept | No secrets in usage strings |
| T-18-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
1. bash -n cli/conjure (syntax)
2. bash -n tests/run.sh (syntax)
3. grep -q cmd_resolve cli/conjure
4. grep -q 'resolve)' cli/conjure
5. CONJURE_HOME="$(pwd)" cli/conjure resolve --help → exits 0, prints usage
6. bash tests/run.sh 2>&1 | tail -5 → FAIL: 0
7. bash tests/run.sh 2>&1 | grep -E "(RESOLVE-01|RESOLVE-02)" | grep "✓" | wc -l → should be ≥ 4
</verification>

<success_criteria>
- cli/conjure dispatches `resolve` to scripts/resolve.sh with CONJURE_HOME and DRY_RUN forwarded
- "conjure resolve" appears in usage() output
- RESOLVE-01: non-interactive guard test passes (exit 2 on piped stdin with sidecars present)
- RESOLVE-02: all-clear test passes (exit 0 + "No conflicts remain" on empty dir via </dev/null — no TTY needed)
- RESOLVE-02: keep action test passes (sidecar removed, current file unchanged)
- RESOLVE-02: apply action test passes (current file updated, sidecar removed)
- Full test suite: bash tests/run.sh exits 0
- scripts/resolve.sh is NOT modified by this plan — all resolve.sh changes were owned by Plan 01
</success_criteria>

<output>
Create `.planning/phases/18-conflict-resolution/18-02-SUMMARY.md` when done.
</output>
