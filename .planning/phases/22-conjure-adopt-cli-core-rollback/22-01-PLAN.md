---
phase: 22-conjure-adopt-cli-core-rollback
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/run.sh
  - tests/fixtures/adopt-restructure-steps/adopt-manifest.json
autonomous: true
requirements: [ADOPT-01, ADOPT-02, ADOPT-04, ADOPT-05, ADOPT-06, SAFE-01, SAFE-02, SAFE-04, SAFE-05, SAFE-06, SAFE-07]
user_setup: []

must_haves:
  truths:
    - "A `▸ Phase 22` test block exists in tests/run.sh and runs (red) before adopt.sh is built"
    - "The suite degrades gracefully: missing scripts/adopt.sh fails its assertions, it does not crash the runner"
    - "A hand-authored synthetic restructure_steps[] manifest fixture exists with one write op + one archive op (D-08)"
    - "A git-init dirty-tree harness and a non-TTY SIGKILL recovery harness are present in the Phase 22 block"
  artifacts:
    - path: "tests/run.sh"
      provides: "Phase 22 inline test block (mirrors Phase 21 block at lines 1691-2358) covering all 5 ROADMAP criteria + SAFE-04/07 + D-08 + Pitfall 3"
      contains: "▸ Phase 22"
    - path: "tests/fixtures/adopt-restructure-steps/adopt-manifest.json"
      provides: "Synthetic manifest with populated restructure_steps[] (1 write + 1 archive) for --apply-step / --update-manifest tests"
      contains: "restructure_steps"
  key_links:
    - from: "tests/run.sh Phase 22 block"
      to: "scripts/adopt.sh"
      via: "subprocess invocation in sandbox_setup'd dir"
      pattern: "adopt\\.sh"
    - from: "tests/run.sh Phase 22 block"
      to: "tests/fixtures/brownfield-simple"
      via: "sandbox_setup fixture copy"
      pattern: "brownfield-simple"
---

<objective>
Wave 0 (test-first): build the Phase 22 test infrastructure that gates every later verification in this phase. Per the project's Nyquist convention, the assertions for all five ROADMAP success criteria + SAFE-04/SAFE-07 + the D-08 op-executor + Pitfall 3 (snapshot self-copy) must exist BEFORE `scripts/adopt.sh` is written, so Wave 1/Wave 2 executors run against a concrete red→green signal.

This plan creates NO production code. It creates: (1) a `▸ Phase 22` inline test block appended to `tests/run.sh` that mirrors the Phase 21 block style and fails gracefully while `scripts/adopt.sh` is absent; (2) a hand-authored synthetic `restructure_steps[]` manifest fixture (D-08) with one `write` op and one `archive` op; and (3) the two net-new test harnesses identified in PATTERNS.md "No Analog Found" — a `git init`-ed dirty-tree sandbox (criterion 3) and a background-launch + `kill -9` + re-run harness asserting the non-TTY `exit 2` + "last completed" recovery message (criterion 5).

Purpose: every downstream task can verify itself via `bash tests/run.sh 2>&1 | grep -E "Phase 22|✗"` instead of inventing ad-hoc checks. The block is the executable contract.
Output: `tests/run.sh` (Phase 22 block appended before the gh-stub cleanup at line ~2361), `tests/fixtures/adopt-restructure-steps/adopt-manifest.json`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-conjure-adopt-cli-core-rollback/22-CONTEXT.md
@.planning/phases/22-conjure-adopt-cli-core-rollback/22-VALIDATION.md
@.planning/phases/22-conjure-adopt-cli-core-rollback/22-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Synthetic restructure_steps[] manifest fixture (D-08)</name>
  <files>tests/fixtures/adopt-restructure-steps/adopt-manifest.json</files>
  <read_first>
    - tests/fixtures/adopt-restructure-steps/adopt-manifest.json (the file being created — confirm it does not yet exist)
    - adopt-manifest.schema.json (the draft-07 schema this fixture MUST validate against — note required top-level keys schema_version/summary/files and the restructure_steps[] item shape, currently `items: {}` empty at inventory time)
    - tests/run.sh lines 2270-2312 (Pattern-7 sample-JSON heredoc + `jq empty` / `jq -e` validation idiom — the analog for how fixtures are authored + validated)
    - .planning/phases/22-conjure-adopt-cli-core-rollback/22-RESEARCH.md §"Pattern 5" + §State-of-the-Art op-type table (D-08 scope: only `write`, `archive`, `extract`=write+archive)
  </read_first>
  <action>
    Hand-author a complete `adopt-manifest.json` under tests/fixtures/adopt-restructure-steps/ that is valid against adopt-manifest.schema.json AND has a populated `restructure_steps[]` array with exactly two ops per D-08: (1) a `write` op `{ "id": "step-1", "op": "write", "dest": "CLAUDE.md", "src": ".conjure-adopt-state/staging/CLAUDE.md", "status": "proposed" }` (D-07 staging-path reference) and (2) an `archive` op `{ "id": "step-2", "op": "archive", "src": "docs/OLD.md", "status": "proposed" }`. Include the schema-required top-level keys (`schema_version`, `summary` with its required sub-fields, `files[]` — a minimal valid set is fine since these tests exercise restructure_steps[], not classification). Keep it lean and jq-parseable. This fixture is the seam the Wave 2 `--apply-step`/`--update-manifest` executor (D-05/D-06) is tested against; do NOT add op types outside {write, archive, extract}. NOTE per RESEARCH.md line 220: never commit per-repo runtime artifacts (.conjure-adopt-state, backups, RESTRUCTURE-LOG.md) into the fixture — only the manifest belongs here.
  </action>
  <verify>
    <automated>jq -e '.schema_version and .summary and .files and (.restructure_steps | length == 2) and (.restructure_steps[0].op == "write") and (.restructure_steps[1].op == "archive")' tests/fixtures/adopt-restructure-steps/adopt-manifest.json</automated>
  </verify>
  <acceptance_criteria>
    - `jq empty tests/fixtures/adopt-restructure-steps/adopt-manifest.json` exits 0 (valid JSON)
    - `jq -e '.restructure_steps | length == 2'` exits 0
    - The write op has `src` pointing under `.conjure-adopt-state/staging/` and `status: "proposed"` (D-07)
    - The archive op has only `id`/`op`/`src`/`status` (no `dest`) and `status: "proposed"`
    - Fixture validates against adopt-manifest.schema.json required top-level keys (`jq -e '.schema_version and .summary and .files'` exits 0)
  </acceptance_criteria>
  <done>tests/fixtures/adopt-restructure-steps/adopt-manifest.json exists, is valid JSON, validates against the schema's required keys, and carries exactly one write + one archive proposed op.</done>
</task>

<task type="auto">
  <name>Task 2: Phase 22 test block in tests/run.sh — pipeline + safety assertions</name>
  <files>tests/run.sh</files>
  <read_first>
    - tests/run.sh lines 1691-2358 (the entire Phase 21 block — copy its exact section structure, `▸ Phase NN` headers, lib-present guards, and sub-shell run pattern)
    - tests/run.sh lines 14-16 (`t()`/`pass()`/`fail()` helpers — use these, do NOT invent new assertion functions)
    - tests/run.sh lines 2358-2370 (the append point: insert the Phase 22 block BEFORE the gh-stub cleanup `for _s in $GH_HIDE_STUBS` at ~2361; do NOT add a second PASS/FAIL summary)
    - tests/lib/sandbox.sh lines 46-63 (`sandbox_setup` — sets SANDBOX_DIR, copies fixture, registers its own EXIT trap; mind the trap set/reset discipline from tests/run.sh lines 248-249,1777,1846)
    - .planning/phases/22-conjure-adopt-cli-core-rollback/22-VALIDATION.md §"Per-Task Verification Map" (the authoritative list of observable signals — every row here becomes one or more assertions)
    - .planning/phases/22-conjure-adopt-cli-core-rollback/22-PATTERNS.md §"Phase 22 block in tests/run.sh" (section headers, DRY_RUN sub-shell grep pattern, trap caveat)
    - tests/fixtures/brownfield-simple (the fixture these tests run adopt against — note CLAUDE.md is 21 lines, .claude/skills/git/SKILL.md is the pre-existing file used for the idempotency byte-unchanged assertion)
  </read_first>
  <action>
    Append a `▸ Phase 22 — conjure adopt` block to tests/run.sh (before the gh-stub cleanup at ~line 2361) mirroring the Phase 21 block. Guard every adopt invocation behind a `[ -f scripts/adopt.sh ]` (or `cmd_adopt` presence) check so the suite fails the Phase 22 assertions as red — with a "Wave 1 must create scripts/adopt.sh first" `fail` message — rather than crashing while the script is absent (mirror the Phase 21 `P21_CAPS_OK` lib-present guard at lines 1696-1703). Write these sub-sections, each tagging its requirement IDs in the message:
    (a) `adopt.sh dry-run (ADOPT-02 / criterion 1)`: in a `sandbox_setup tests/fixtures/brownfield-simple` dir, run `DRY_RUN=1 ... adopt.sh "$sb"`; assert `git status --porcelain "$sb"` is empty AND `find "$sb" -name adopt-manifest.json` is empty (zero writes under target, Pitfall 1) AND stdout contains all five step labels `preconditions`/`snapshot`/`inventory`/`scaffold`/`audit` AND a `[dry-run] would` marker AND the printed manifest temp path is NOT the hardcoded `/tmp/adopt-manifest-dryrun.json`.
    (b) `adopt.sh live (ADOPT-01/04/05/06 / criterion 2)`: run live in a sandbox; assert a `.conjure-adopt-backups/*/CLAUDE.md` snapshot copy exists (SAFE-01), `adopt-manifest.json` is present under target, new `.claude/hooks/*` were scaffolded, the pre-existing `.claude/skills/git/SKILL.md` is byte-unchanged (sha256 before==after — ADOPT-04 never-overwrite), and the adoption report stdout contains a `CLAUDE.md: 21 → 21` line (ADOPT-06 before/after) and a `Next:`/restructure-skill pointer.
    (c) `adopt.sh dirty-tree (ADOPT-03/SAFE-06 / criterion 3)`: see Task 3 harness — git-init the sandbox, create an untracked file, run without `--force` and assert rc==2, run with `--force` and assert rc==0 AND `grep -q 'WARN.*uncommitted' "$sb/RESTRUCTURE-LOG.md"`.
    (d) `adopt.sh rollback (SAFE-02 / criterion 4)`: capture pre-adopt sha256 of every non-conjure file, run live, run `--rollback`, assert each file's sha256 equals the recorded before-hash AND `created[]` files are gone AND `grep -q '\[ROLLBACK\]' RESTRUCTURE-LOG.md` AND a `diff -r` of pre-adopt vs post-rollback (excluding `.conjure-adopt-backups`/`.conjure-archive-*`/`RESTRUCTURE-LOG.md`/`adopt-manifest.json` per D-03) is empty.
    (e) `adopt.sh state + log (SAFE-04/SAFE-07)`: after a live run assert `jq . "$sb/.conjure-adopt-state"` (or `.conjure-adopt-state/state.json`) parses, `.mutated[].before` is present, and RESTRUCTURE-LOG.md contains SNAPSHOT, INVENTORY, SCAFFOLD, AUDIT entries in order.
    Use the `t()`/`pass()`/`fail()` helpers and the DRY_RUN sub-shell + grep idiom from Phase 21 (run.sh lines 1730-1745). Do NOT change the suite tail or add a second summary.
  </action>
  <verify>
    <automated>bash tests/run.sh 2>&1 | grep -q "▸ Phase 22"</automated>
  </verify>
  <acceptance_criteria>
    - `bash tests/run.sh 2>&1 | grep -c "▸ Phase 22"` is >= 1 (block runs)
    - The runner does NOT crash with `scripts/adopt.sh` absent — Phase 22 assertions show as `✗ ... Wave 1 must create scripts/adopt.sh first` (graceful red), and Phase 21 / v0.5.0 blocks still report their prior pass counts
    - The block uses only `t`/`pass`/`fail` (grep shows no newly-defined assertion functions inside the Phase 22 block)
    - Sub-sections (a)-(e) each appear as a `▸ Phase 22 — ...` header tagging the requirement IDs from VALIDATION.md
    - The dry-run zero-writes assertion uses `git status --porcelain "$sb"` empty AND `find "$sb" -name adopt-manifest.json` empty (both checks present)
  </acceptance_criteria>
  <done>tests/run.sh has a graceful-red Phase 22 block covering dry-run zero-writes, live scaffold/idempotency/report, rollback sha256 + zero-diff, and state+log assertions, all using existing helpers and the Phase 21 sandbox pattern.</done>
</task>

<task type="auto">
  <name>Task 3: Dirty-tree git harness + SIGKILL recovery harness + apply-step assertions</name>
  <files>tests/run.sh</files>
  <read_first>
    - tests/run.sh (the Phase 22 block created in Task 2 — this task extends it; read it in full first)
    - tests/lib/sandbox.sh lines 46-63 (`sandbox_setup` — note it copies a fixture but does NOT `git init`; criterion 3 needs an untracked-file dirty tree, so this harness adds `git -C "$sb" init -q` + an untracked file)
    - scripts/resolve.sh lines 32-37 (the non-TTY `exit 2` guard the recovery test asserts against) and lines 52-80 (the `/dev/tty` prompt loop — informs what "non-interactive → exit 2 + last completed" looks like)
    - .planning/phases/22-conjure-adopt-cli-core-rollback/22-VALIDATION.md rows for "Crit 5 / SAFE-05", "D-08 / D-05", "D-08 / D-06", "Pitfall 3" (the exact observable signals these harnesses assert)
    - tests/fixtures/adopt-restructure-steps/adopt-manifest.json (Task 1 fixture — the input to the --apply-step / --update-manifest assertions)
  </read_first>
  <action>
    Extend the Phase 22 block with the net-new harnesses (PATTERNS.md "No Analog Found"):
    (a) `git-init dirty-tree harness` (criterion 3 wiring): after `sandbox_setup`, run `git -C "$sb" init -q` + commit the fixture, then `touch "$sb/UNTRACKED.txt"` to make the tree dirty for adopt's purposes; this harness feeds sub-section (c) from Task 2.
    (b) `SIGKILL recovery (SAFE-05 / criterion 5)`: launch `adopt.sh "$sb"` in the background, `kill -9` it after the snapshot step (poll for `.conjure-adopt-backups/*` existence to know snapshot completed, with a bounded timeout loop — no fixed long sleep), then re-run adopt.sh NON-interactively (no TTY) and assert it `exit 2` AND stdout/stderr contains `last completed:` + the snapshot step name + the three recovery flag names `--rollback`/`--resume`/`--start-fresh` (D-13). This non-TTY exit-2 form is the reliably-automatable signal; the interactive `[r]/[c]/[s]` prompt is deferred to the manual-verification table in VALIDATION.md.
    (c) `--apply-step / --update-manifest (D-05/D-06/D-08)`: copy tests/fixtures/adopt-restructure-steps/adopt-manifest.json into a sandbox target plus a staging file, run `adopt.sh --apply-step step-1 "$sb"`, assert the dest file changed via mutate_* AND `jq '.restructure_steps[] | select(.id=="step-1") | .status'` == `"applied"` AND RESTRUCTURE-LOG.md has a `RESTRUCTURE` entry; for `--update-manifest`, feed a valid step JSON and assert it is appended, then feed `{}` (no id/op) and assert rc==2.
    (d) `Pitfall 3 snapshot self-copy regression`: run a live adopt twice (delete `.conjure-adopt-state` between runs), then assert there is NO `.conjure-adopt-backups/*/.conjure-adopt-backups` nesting (`find "$sb/.conjure-adopt-backups" -path '*/.conjure-adopt-backups/*' -name '*' | head -1` is empty).
    All four guarded behind the `[ -f scripts/adopt.sh ]` presence check so they stay graceful-red until Wave 1/Wave 2 land. Use bounded polling loops (e.g. `for i in $(seq 1 50); do [ -d "$sb/.conjure-adopt-backups" ] && break; sleep 0.1; done`) — never a blind long sleep.
  </action>
  <verify>
    <automated>bash tests/run.sh 2>&1 | grep -Eq "Phase 22.*(SAFE-05|apply-step|self-copy|dirty)"</automated>
  </verify>
  <acceptance_criteria>
    - The dirty-tree harness calls `git -C "$sb" init` and creates an untracked file (grep tests/run.sh Phase 22 block for `git -C` + `init`)
    - The SIGKILL harness uses background launch + `kill -9` + bounded poll (no blind `sleep` > 1s; grep shows a `kill -9` and a bounded `for`/`seq` poll loop)
    - The SIGKILL re-run asserts `exit 2` (rc captured) AND a `last completed:` substring AND the three flag names
    - The apply-step assertion checks `status == "applied"` via jq AND a RESTRUCTURE log entry; the update-manifest assertion checks `{}` → rc==2
    - The Pitfall-3 assertion checks for absence of nested `.conjure-adopt-backups` after two adopts
    - With scripts/adopt.sh absent, all four are graceful-red (no runner crash)
  </acceptance_criteria>
  <done>The Phase 22 block additionally covers SIGKILL non-TTY recovery (exit 2 + last-completed), git-init dirty-tree, --apply-step/--update-manifest against the synthetic fixture, and the snapshot self-copy regression — all bounded, all graceful-red until production code lands.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture JSON → jq/test runner | Hand-authored manifest fixture is parsed by jq during tests; malformed content must fail loudly, never silently pass an assertion |
| test sandbox → host filesystem | Tests run adopt against a `mktemp -d` sandbox; a path-escape in a harness could touch the real repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | Tampering | test sandbox writing outside `$SANDBOX_DIR` | mitigate | All harnesses operate strictly under `sandbox_setup`'s `mktemp -d` `$SANDBOX_DIR`; assertions use `"$sb"`-rooted paths only; no harness writes to `$CONJURE_HOME` |
| T-22-02 | Denial of Service | SIGKILL harness hangs the suite | mitigate | Bounded poll loop (`for i in $(seq 1 50); ... sleep 0.1`) with a hard cap; background PID is `kill -9`'d explicitly; no unbounded `wait` |
| T-22-03 | Information Disclosure | committing per-repo runtime artifacts into the fixture | mitigate | Fixture contains only `adopt-manifest.json`; RESEARCH.md line 220 rule enforced — no `.conjure-adopt-state`/backups/log committed |
| T-22-SC | Tampering | npm/pip/cargo installs | accept | This phase installs zero external packages (RESEARCH.md Package Legitimacy Audit: none); no install task exists, so no slopcheck checkpoint is required |
</threat_model>

<verification>
- `bash tests/run.sh 2>&1 | grep -q "▸ Phase 22"` — the block runs
- `jq -e '.restructure_steps | length == 2' tests/fixtures/adopt-restructure-steps/adopt-manifest.json` — fixture valid
- Full suite still completes (Phase 21 + v0.5.0 blocks unaffected): `bash tests/run.sh; echo "exit:$?"` exits non-zero ONLY due to the new graceful-red Phase 22 assertions, not a runner crash
- `shellcheck tests/run.sh` clean (match the inline `# shellcheck disable=` style already used)
</verification>

<success_criteria>
- A `▸ Phase 22` test block exists in tests/run.sh covering all five ROADMAP criteria + SAFE-04/SAFE-07 + D-08 + Pitfall 3
- The synthetic restructure_steps[] manifest fixture exists and is schema-valid with 1 write + 1 archive op
- The git-init dirty-tree harness and the non-TTY SIGKILL recovery harness are present and bounded
- The suite is graceful-red (not crashing) with scripts/adopt.sh absent, giving Wave 1/Wave 2 a concrete red→green target
</success_criteria>

<output>
Create `.planning/phases/22-conjure-adopt-cli-core-rollback/22-01-SUMMARY.md` when done
</output>
