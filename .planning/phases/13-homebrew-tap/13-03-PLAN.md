---
phase: 13-homebrew-tap
plan: 03
type: execute
wave: 3
depends_on:
  - 13-01
  - 13-02
files_modified:
  - tests/run.sh
  - .planning/phases/13-homebrew-tap/13-VALIDATION.md
autonomous: true
requirements:
  - BREW-01
  - BREW-02
  - BREW-03
  - BREW-04

user_setup:
  - service: github
    why: "Homebrew tap requires mohandoz/homebrew-conjure repo and a PAT secret for the bump action to push formula updates"
    dashboard_config:
      - task: "Create mohandoz/homebrew-conjure GitHub repo with Formula/conjure.rb inside a Formula/ subdirectory"
        location: "https://github.com/new — repo name must be homebrew-conjure (the homebrew- prefix allows `brew tap mohandoz/conjure`)"
      - task: "Create a GitHub PAT with repo and workflow scopes; add it as repository secret HOMEBREW_TAP_GITHUB_TOKEN"
        location: "https://github.com/mohandoz/conjure/settings/secrets/actions"

must_haves:
  truths:
    - "bash tests/run.sh passes green for all four automated BREW assertions (BREW-01 syntax, BREW-02 env override, BREW-03 no HEAD/branch, BREW-04 workflow ref)"
    - "BREW-02 test sets CONJURE_HOME to a mktemp dir with a fake VERSION file and verifies cli/conjure reads it (D-03 prerequisite)"
    - "BREW-03 test greps Formula/conjure.rb for HEAD and branch tokens — both absent"
    - "BREW-04 test greps .github/workflows/release.yml for bump-homebrew-formula-action reference — present"
    - "BREW-01 automated test runs ruby -c on Formula/conjure.rb — exits 0"
    - "13-VALIDATION.md documents BREW-01 brew install and BREW-04 live tap bump as manual-only steps"
    - "Temp dir from BREW-02 test is cleaned up: mktemp -d + trap EXIT + explicit rm -rf + trap - EXIT"
  artifacts:
    - path: "tests/run.sh"
      provides: "BREW regression test block"
      contains: "BREW homebrew tests"
    - path: ".planning/phases/13-homebrew-tap/13-VALIDATION.md"
      provides: "Phase 13 validation contract"
      contains: "BREW-01"
  key_links:
    - from: "tests/run.sh BREW-02 test"
      to: "cli/conjure line 24"
      via: "CONJURE_HOME env var override — requires D-03 conditional to be applied"
      pattern: "CONJURE_HOME.*cli/conjure version"
    - from: "tests/run.sh BREW-03 test"
      to: "Formula/conjure.rb"
      via: "static grep for HEAD and branch tokens"
      pattern: "grep.*Formula/conjure.rb"
---

<objective>
Add the BREW regression test block to tests/run.sh and create 13-VALIDATION.md.
The test block provides automated verification for BREW-01 (formula syntax), BREW-02
(CONJURE_HOME env override), BREW-03 (no HEAD/branch in formula), and BREW-04
(workflow contains bump action reference). BREW-01's full brew install and BREW-04's
live tap push are documented as manual-only in VALIDATION.md.

Purpose: Closes the Nyquist gate for Phase 13 — every automated assertion has a test,
and every manual-only assertion has a documented verification procedure.

Output: tests/run.sh with BREW block appended before the summary section;
13-VALIDATION.md following the Phase 12 VALIDATION.md schema.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/13-homebrew-tap/13-CONTEXT.md
@.planning/phases/13-homebrew-tap/13-RESEARCH.md
@.planning/phases/13-homebrew-tap/13-PATTERNS.md
@.planning/phases/13-homebrew-tap/13-01-SUMMARY.md
@.planning/phases/13-homebrew-tap/13-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append BREW test block to tests/run.sh</name>
  <files>tests/run.sh</files>
  <read_first>
    - tests/run.sh lines 1-20 (pass/fail helper definitions and CONJURE_HOME setup — do NOT redefine these)
    - tests/run.sh lines 1077-1090 (OVLY block header — copy verbatim, substitute OVLY with BREW)
    - tests/run.sh lines 1245-1254 (final summary block — new BREW block must go BEFORE line 1248)
    - .planning/phases/13-homebrew-tap/13-RESEARCH.md section "CONJURE_HOME Unit Test Approach" — exact BREW-02 test code
    - .planning/phases/13-homebrew-tap/13-RESEARCH.md section "Code Examples" — BREW-03 and BREW-04 static grep tests
    - .planning/phases/13-homebrew-tap/13-PATTERNS.md section "Temp-dir Lifecycle" — mktemp + trap EXIT lifecycle
  </read_first>
  <action>
    Append a BREW test block to tests/run.sh immediately before the summary block
    (currently starting at line 1248 with the "# Summary" comment). Do not modify
    any existing content — append only.

    The BREW block must follow the exact same structural conventions as the OVLY block
    (lines 1077-1246): separator comment, echo header, individual test assertions using
    pass/fail helpers, cleanup.

    Block structure:

    1. Section separator (copy OVLY format exactly, substituting OVLY for BREW):
       # ──────────────────────────────────────────────────────────────────────────────
       # BREW homebrew tests (BREW-01 through BREW-04)
       # ──────────────────────────────────────────────────────────────────────────────
       echo
       echo "▸ BREW homebrew tests (BREW-01 through BREW-04)"

    2. BREW-01: Formula Ruby syntax check (automated alternative to brew install)
       if ruby -c "$CONJURE_HOME/Formula/conjure.rb" >/dev/null 2>&1; then
         pass "Formula/conjure.rb: valid Ruby syntax (BREW-01)"
       else
         fail "Formula/conjure.rb: Ruby syntax error — run: ruby -c Formula/conjure.rb (BREW-01)"
       fi

    3. BREW-02: CONJURE_HOME env var override unit test (per RESEARCH.md section)
       Temp dir lifecycle — mktemp, immediate trap, fake VERSION, test, explicit rm, trap clear:
       BREW_FAKE="$(mktemp -d)"
       trap 'rm -rf "$BREW_FAKE"' EXIT
       printf '9.8.7\n' > "$BREW_FAKE/VERSION"
       BREW_VER_OUT="$(CONJURE_HOME="$BREW_FAKE" cli/conjure version 2>&1)"
       if printf '%s\n' "$BREW_VER_OUT" | grep -q '9.8.7'; then
         pass "CONJURE_HOME env var overrides default resolution (BREW-02)"
       else
         fail "CONJURE_HOME env var did NOT override — got: $BREW_VER_OUT (BREW-02)"
       fi
       rm -rf "$BREW_FAKE"
       trap - EXIT

    4. BREW-03: No HEAD or branch reference in formula (static grep)
       if grep -qE '\bHEAD\b|\bbranch\b' "$CONJURE_HOME/Formula/conjure.rb" 2>/dev/null; then
         fail "Formula/conjure.rb contains HEAD or branch reference — must use tagged tarball URL (BREW-03)"
       else
         pass "Formula/conjure.rb: no HEAD or branch reference (BREW-03)"
       fi

    5. BREW-04: release.yml references the bump action (static grep)
       if grep -q 'bump-homebrew-formula-action' "$CONJURE_HOME/.github/workflows/release.yml" 2>/dev/null; then
         pass "release.yml references bump-homebrew-formula-action (BREW-04)"
       else
         fail "release.yml missing bump-homebrew-formula-action reference (BREW-04)"
       fi

    Note on BREW-02 test prerequisite: this test relies on the D-03 conditional being
    applied to cli/conjure (completed in Plan 13-01). If the test fails, confirm that
    cli/conjure line 24 is the conditional form before debugging.

    Note on grep gate hygiene: the BREW-03 grep uses `\b` word boundaries to avoid
    false positives on substrings (e.g., "label", "tablename"). The `2>/dev/null`
    guard handles the case where Formula/conjure.rb does not yet exist — the test
    will then fail with the correct failure message.

    Do NOT add a separate BREW-01 manual reminder inside the test block — that belongs
    in VALIDATION.md only.
  </action>
  <verify>
    <automated>bash tests/run.sh 2>&1 | tail -5</automated>
  </verify>
  <done>
    `bash tests/run.sh` exits 0 with FAIL: 0. The BREW block appears before the summary
    section. All four BREW assertions produce pass lines. The BREW_FAKE temp dir is
    cleaned up (no leftover /tmp/tmp.* dirs after the run).
  </done>
</task>

<task type="auto">
  <name>Task 2: Create 13-VALIDATION.md</name>
  <files>.planning/phases/13-homebrew-tap/13-VALIDATION.md</files>
  <read_first>
    - .planning/phases/12-org-overlay/12-VALIDATION.md (full file — the exact format to follow; use the 9-column per-task table schema from Phase 12, not Phase 11's 10-column schema)
    - .planning/phases/13-homebrew-tap/13-PATTERNS.md section "13-VALIDATION.md" — frontmatter pattern, column schema, Manual-Only Verifications table
    - .planning/phases/13-homebrew-tap/13-RESEARCH.md section "Validation Architecture" — test map and Wave 0 gaps
  </read_first>
  <action>
    Create .planning/phases/13-homebrew-tap/13-VALIDATION.md following the Phase 12
    VALIDATION.md format exactly. Use the 9-column per-task verification table
    (no "Threat Ref" column — Phase 12 schema).

    Frontmatter:
      phase: 13
      slug: homebrew-tap
      status: draft
      nyquist_compliant: false
      wave_0_complete: false
      created: 2026-05-26

    Test Infrastructure table (identical to Phase 12):
      Framework: Hand-rolled bash test runner
      Config file: none
      Quick run command: `bash tests/run.sh`
      Full suite command: `bash tests/run.sh`
      Estimated runtime: ~15 seconds

    Per-Task Verification Map (9 columns): Task ID, Plan, Wave, Requirement, Secure
    Behavior, Test Type, Automated Command, File Exists, Status

    Include these rows (derived from RESEARCH.md Validation Architecture table):

    | 13-01-T1 | 13-01 | 1 | BREW-01 | Formula/conjure.rb has valid Ruby syntax | unit (static) | `bash tests/run.sh` (ruby -c check) | Formula/conjure.rb | pending |
    | 13-01-T1 | 13-01 | 1 | BREW-03 | Formula has no HEAD or branch reference | static grep | `bash tests/run.sh` (BREW-03 block) | Formula/conjure.rb | pending |
    | 13-01-T2 | 13-01 | 1 | BREW-02 | CONJURE_HOME env var overrides default resolution | unit | `bash tests/run.sh` (BREW-02 block) | cli/conjure | pending |
    | 13-02-T1 | 13-02 | 2 | BREW-04 | release.yml references bump-homebrew-formula-action | static grep | `bash tests/run.sh` (BREW-04 block) | .github/workflows/release.yml | pending |

    Wave 0 Requirements section:
    - BREW test block in `tests/run.sh` — all 4 automated assertions (created in Plan 13-03 Wave 3)
    - `Formula/conjure.rb` — must exist for ruby -c and BREW-03 tests to pass (created in Plan 13-01 Wave 1)
    - `cli/conjure` D-03 conditional — must be applied for BREW-02 test to pass (Plan 13-01 Wave 1)

    Manual-Only Verifications table:

    | Behavior | Requirement | Why Manual | Test Instructions |
    |----------|-------------|------------|-------------------|
    | `brew install mohandoz/conjure/conjure` succeeds; `conjure --version` exits 0 | BREW-01 | Requires a real brew installation, a published tag, and the tap repo to exist with a valid sha256 | 1. Create mohandoz/homebrew-conjure repo on GitHub with Formula/conjure.rb. 2. Tag and push v0.3.0. 3. Let bump action update sha256. 4. Run: `brew tap mohandoz/conjure && brew install mohandoz/conjure/conjure && conjure --version` |
    | CONJURE_HOME resolves to $(brew --prefix)/share/conjure/ automatically | BREW-02 | Requires actual brew-installed binary in Cellar | After brew install: `conjure version` — output must include the Cellar path |
    | bump-homebrew-formula-action fires and pushes updated formula to tap repo | BREW-04 | Requires real GitHub release event to trigger | Push a new tag (e.g., v0.3.1); verify that mohandoz/homebrew-conjure/Formula/conjure.rb sha256 is updated within ~2 minutes |

    Pre-release Checklist section (specific to this phase):
    - [ ] Create mohandoz/homebrew-conjure repo on GitHub (Formula/conjure.rb with placeholder sha256)
    - [ ] Add HOMEBREW_TAP_GITHUB_TOKEN secret to mohandoz/conjure repo settings (PAT with repo + workflow scopes)
    - [ ] Tag and push v0.3.0 to trigger first release
    - [ ] Verify bump action updates sha256 in tap repo
    - [ ] Run `brew install mohandoz/conjure/conjure && conjure --version` to confirm BREW-01

    Close the document with a note: "The automated BREW assertions in tests/run.sh
    (ruby -c, env override, static greps) provide regression coverage. The manual
    pre-release checklist covers the full end-to-end distribution path."
  </action>
  <verify>
    <automated>grep -c 'BREW-0' .planning/phases/13-homebrew-tap/13-VALIDATION.md</automated>
  </verify>
  <done>
    13-VALIDATION.md exists with correct 7-key frontmatter (phase: 13, slug: homebrew-tap).
    `grep -c 'BREW-0'` outputs at least 4 (one per requirement ID). Manual-only verifications
    table covers BREW-01 full install and BREW-04 live tap push. Pre-release checklist
    includes the tap repo creation and PAT secret setup steps.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tests/run.sh BREW-02 → BREW_FAKE temp dir | Test creates a temp dir with a fake VERSION; cleaned up via trap + explicit rm |
| VALIDATION.md → developer manual steps | Documentation guides manual steps involving external GitHub resources and PAT creation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-06 | Tampering | BREW-02 temp dir left behind | mitigate | Trap EXIT + explicit rm -rf pattern from 13-PATTERNS.md; test verifies via bash tests/run.sh exit 0 |
| T-13-07 | Information Disclosure | HOMEBREW_TAP_GITHUB_TOKEN in VALIDATION.md | accept | VALIDATION.md documents that a PAT is needed but does not contain the token value; token lives only in GitHub Secrets |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | No package manager installs in this plan; tests use bash and ruby only |
</threat_model>

<verification>
After both tasks complete:
1. `bash tests/run.sh` — exits 0, FAIL: 0
2. `bash tests/run.sh 2>&1 | grep -c 'BREW-0'` — outputs at least 4 (one pass line per BREW requirement)
3. `grep -c 'BREW homebrew tests' tests/run.sh` — outputs 1 (block header present)
4. `grep 'bump-homebrew-formula-action' tests/run.sh` — must match (BREW-04 grep test present)
5. `grep 'CONJURE_HOME.*cli/conjure version' tests/run.sh` — must match (BREW-02 test present)
6. `grep -c 'BREW-0' .planning/phases/13-homebrew-tap/13-VALIDATION.md` — outputs at least 4
7. `grep 'nyquist_compliant' .planning/phases/13-homebrew-tap/13-VALIDATION.md` — must match frontmatter key
</verification>

<success_criteria>
tests/run.sh BREW block passes all four automated assertions. 13-VALIDATION.md
documents the full verification contract for Phase 13, including manual-only steps
for BREW-01 (brew install) and BREW-04 (live tap push), and the pre-release checklist
for creating mohandoz/homebrew-conjure and the HOMEBREW_TAP_GITHUB_TOKEN secret.
All four BREW requirements have at least one automated test and one documented
verification path.
</success_criteria>

<output>
Create .planning/phases/13-homebrew-tap/13-03-SUMMARY.md when done
</output>
