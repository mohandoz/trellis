---
phase: 09-3-way-merge
plan: 03
type: execute
wave: 3
depends_on:
  - 09-01
  - 09-02
files_modified:
  - scripts/audit-setup.sh
  - tests/run.sh
autonomous: true
requirements:
  - MERGE-01
  - MERGE-02
  - MERGE-03
  - MERGE-04
  - MERGE-05

must_haves:
  truths:
    - "conjure audit detects <<<<<<< markers in .claude/ and exits non-zero (MERGE-05)"
    - "conjure audit does NOT flag .conjure-conflict-* sidecar files as false positives"
    - "tests/run.sh MERGE-01 passes: clean merge auto-merges non-overlapping edits, exits 0, no sidecar"
    - "tests/run.sh MERGE-02 passes: conflict writes sidecar, leaves original untouched, merge_file_3way returns 1"
    - "tests/run.sh MERGE-03 passes: missing snapshot aborts with correct error message, exits non-zero"
    - "tests/run.sh MERGE-04 passes: settings.json taken upstream unconditionally, no sidecar"
    - "Full test suite bash tests/run.sh exits 0"
  artifacts:
    - path: "scripts/audit-setup.sh"
      provides: "conflict marker detection block before final exit"
      contains: "grep -rl"
    - path: "tests/run.sh"
      provides: "MERGE-01 through MERGE-04 test blocks"
      contains: "MERGE-01"
  key_links:
    - from: "scripts/audit-setup.sh"
      to: ".claude/"
      via: "grep -rl '^<<<<<<<'"
      pattern: "grep.*rl.*<<<<<<<"
    - from: "tests/run.sh"
      to: "lib/merge.sh"
      via: "source CONJURE_HOME/lib/mutate.sh + lib/merge.sh"
      pattern: "source.*lib/merge"
---

<objective>
Add conflict marker detection to scripts/audit-setup.sh (MERGE-05) and add MERGE-01 through MERGE-04 regression test blocks to tests/run.sh (D-07, D-08). This is the final wave — all prior implementation is now exercised by tests and the audit safety net.

Purpose: Operators can run conjure audit to catch unresolved conflicts; developers can run tests/run.sh to verify all four merge scenarios.
Output: audit-setup.sh with grep-based conflict marker check; tests/run.sh with four inline synthetic MERGE test blocks.
</objective>

<execution_context>
@/Users/mohandoz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mohandoz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/mohandoz/u01/innovate/conjure/.planning/ROADMAP.md
@/Users/mohandoz/u01/innovate/conjure/.planning/phases/09-3-way-merge/09-CONTEXT.md
@/Users/mohandoz/u01/innovate/conjure/.planning/phases/09-3-way-merge/09-RESEARCH.md
@/Users/mohandoz/u01/innovate/conjure/.planning/phases/09-3-way-merge/09-PATTERNS.md
@/Users/mohandoz/u01/innovate/conjure/scripts/audit-setup.sh
@/Users/mohandoz/u01/innovate/conjure/tests/run.sh
@/Users/mohandoz/u01/innovate/conjure/.planning/phases/09-3-way-merge/09-01-SUMMARY.md
@/Users/mohandoz/u01/innovate/conjure/.planning/phases/09-3-way-merge/09-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add conflict marker detection to audit-setup.sh (MERGE-05)</name>
  <files>scripts/audit-setup.sh</files>
  <action>
In scripts/audit-setup.sh, locate the final summary block (currently lines 132-136):
  # Summary
  echo
  echo "─────────────────────────────────────"
  echo "PASS: $PASS    WARN: $WARN    FAIL: $FAIL"

Insert the conflict marker check block IMMEDIATELY BEFORE the "# Summary" comment (i.e., it becomes the last check before the summary output). The check must come before the COST and RETIRE blocks too — insert it just before line 132.

Block to insert (printf form — shellcheck-safer than heredoc):

# Conflict markers — detect unresolved 3-way merge conflicts (MERGE-05)
if [ -d .claude ]; then
  CONFLICT_FILES="$(grep -rl '^<<<<<<<' .claude/ 2>/dev/null \
    | grep -v '\.conjure-conflict-' || true)"
  if [ -n "$CONFLICT_FILES" ]; then
    err "Unresolved merge conflicts found in .claude/ — resolve and delete .conjure-conflict-* sidecars"
    printf '%s\n' "$CONFLICT_FILES" | while IFS= read -r cf; do
      [ -z "$cf" ] && continue
      note "  conflict markers: $cf"
    done
  else
    ok ".claude/: no unresolved conflict markers"
  fi
fi

Implementation notes:
- Use err() not warn() for conflict markers — per CONTEXT.md §specifics and PATTERNS.md audit pattern: conflicts are a hard error (err → FAIL++ → exit 2).
- The "| grep -v '\.conjure-conflict-'" filter prevents flagging sidecar files that intentionally contain markers (Pitfall 6 in RESEARCH.md).
- CONFLICT_FILES variable: `local` declarations are not valid at script top-level (audit-setup.sh is not a function). Use unqualified assignment: CONFLICT_FILES="$(...)".
- The printf '%s\n' pipe form avoids heredoc entirely and is shellcheck-friendly.
- After insertion: the block evaluates, increments FAIL via err() if conflicts exist, then falls through to the existing summary echo lines and the final exit block at lines 254-256.
- Do NOT change the final exit block (lines 254-256): `[ "$FAIL" -gt 0 ] && exit 2` — conflict markers correctly produce exit 2 through err().
  </action>
  <verify>
    <automated>cd /Users/mohandoz/u01/innovate/conjure && grep -n 'conjure-conflict' scripts/audit-setup.sh | head -5</automated>
  </verify>
  <done>scripts/audit-setup.sh contains the conflict marker check block using grep -rl with .conjure-conflict- exclusion; uses err() (not warn()); block is before the summary echo lines; shellcheck clean.</done>
</task>

<task type="auto">
  <name>Task 2: Add MERGE-01 through MERGE-04 test blocks to tests/run.sh (D-07, D-08)</name>
  <files>tests/run.sh</files>
  <action>
In tests/run.sh, locate the summary block near the end (lines 606-612):
  # Summary
  echo
  echo "═══════════════════════════════════════════════════════════════════"
  echo "PASS: $PASS    FAIL: $FAIL"
  echo "═══════════════════════════════════════════════════════════════════"
  [ "$FAIL" -eq 0 ]

Insert the four MERGE test blocks IMMEDIATELY BEFORE the "# Summary" comment. The MERGE tests are the last substantive section before the summary.

Insert this entire block before "# Summary":

echo
echo "▸ 3-way merge tests (MERGE-01, MERGE-02, MERGE-03, MERGE-04)"

# Source merge lib once (reused across MERGE-01 and MERGE-02)
# shellcheck disable=SC1090
source "$CONJURE_HOME/lib/mutate.sh"
# shellcheck disable=SC1090
source "$CONJURE_HOME/lib/merge.sh"

# MERGE-01: Clean merge — user and upstream changed different lines
# Expected: merge_file_3way returns 0, merged file has both edits, no sidecar written
MERGE_DIR="$(mktemp -d)"
trap 'rm -rf "$MERGE_DIR"' EXIT
mkdir -p "$MERGE_DIR/.claude/.conjure-templates-0.0.1/skills/testskill"
mkdir -p "$MERGE_DIR/.claude/skills/testskill"
# base (snapshot — original ancestor)
printf 'name: testskill\ndescription: A 30-char minimum test skill here\nline3: base\nline4: base\n' \
  > "$MERGE_DIR/.claude/.conjure-templates-0.0.1/skills/testskill/SKILL.md"
# current (user changed line4)
printf 'name: testskill\ndescription: A 30-char minimum test skill here\nline3: base\nline4: USER_EDIT\n' \
  > "$MERGE_DIR/.claude/skills/testskill/SKILL.md"
# new upstream template (changed line3, left line4 alone)
MERGE_TMPL="$(mktemp)"
printf 'name: testskill\ndescription: A 30-char minimum test skill here\nline3: UPSTREAM_EDIT\nline4: base\n' \
  > "$MERGE_TMPL"
# Reset module-level state before direct lib call
CONJURE_MERGE_CONFLICT_COUNT=0
CONJURE_MERGE_CONFLICT_FILES=""
DRY_RUN=0 merge_file_3way \
  "$MERGE_DIR/.claude/skills/testskill/SKILL.md" \
  "$MERGE_DIR/.claude/.conjure-templates-0.0.1/skills/testskill/SKILL.md" \
  "$MERGE_TMPL" \
  "skills/testskill/SKILL.md" "0.0.1" "0.3.0"
MERGE_RC=$?
rm -f "$MERGE_TMPL"
if [ "$MERGE_RC" -eq 0 ]; then pass "clean merge exits 0 (MERGE-01)"
else fail "clean merge should exit 0, got $MERGE_RC (MERGE-01)"; fi
if grep -q "UPSTREAM_EDIT" "$MERGE_DIR/.claude/skills/testskill/SKILL.md" && \
   grep -q "USER_EDIT" "$MERGE_DIR/.claude/skills/testskill/SKILL.md"; then
  pass "merged file contains both user and upstream edits (MERGE-01)"
else fail "merged file missing expected content (MERGE-01)"; fi
if [ -z "$(find "$MERGE_DIR/.claude" -name '.conjure-conflict-*' 2>/dev/null)" ]; then
  pass "no sidecar written on clean merge (MERGE-01)"
else fail "sidecar unexpectedly present on clean merge (MERGE-01)"; fi
rm -rf "$MERGE_DIR"
trap - EXIT

# MERGE-02: Conflict — user and upstream changed the same line
# Expected: merge_file_3way returns 1, sidecar written with markers, original untouched
MERGE_DIR="$(mktemp -d)"
trap 'rm -rf "$MERGE_DIR"' EXIT
mkdir -p "$MERGE_DIR/.claude/.conjure-templates-0.0.1/skills/testskill"
mkdir -p "$MERGE_DIR/.claude/skills/testskill"
# base (ancestor)
printf 'name: testskill\ndescription: A 30-char minimum test skill here\nconflict_line: base\n' \
  > "$MERGE_DIR/.claude/.conjure-templates-0.0.1/skills/testskill/SKILL.md"
# current (user changed conflict_line)
printf 'name: testskill\ndescription: A 30-char minimum test skill here\nconflict_line: USER_VERSION\n' \
  > "$MERGE_DIR/.claude/skills/testskill/SKILL.md"
# new upstream (also changed conflict_line — genuine conflict)
MERGE_TMPL="$(mktemp)"
printf 'name: testskill\ndescription: A 30-char minimum test skill here\nconflict_line: UPSTREAM_VERSION\n' \
  > "$MERGE_TMPL"
# Reset module-level state before direct lib call
CONJURE_MERGE_CONFLICT_COUNT=0
CONJURE_MERGE_CONFLICT_FILES=""
DRY_RUN=0 merge_file_3way \
  "$MERGE_DIR/.claude/skills/testskill/SKILL.md" \
  "$MERGE_DIR/.claude/.conjure-templates-0.0.1/skills/testskill/SKILL.md" \
  "$MERGE_TMPL" \
  "skills/testskill/SKILL.md" "0.0.1" "0.3.0"
MERGE_RC=$?
rm -f "$MERGE_TMPL"
if [ "$MERGE_RC" -eq 1 ]; then pass "conflict exits 1 (MERGE-02)"
else fail "conflict should exit 1, got $MERGE_RC (MERGE-02)"; fi
# D-05: original file must be untouched (no <<<<<<< markers in original)
if grep -q "USER_VERSION" "$MERGE_DIR/.claude/skills/testskill/SKILL.md" && \
   ! grep -q '<<<<<<<' "$MERGE_DIR/.claude/skills/testskill/SKILL.md"; then
  pass "original file untouched on conflict (MERGE-02 / D-05)"
else fail "original file was modified on conflict — D-05 violation (MERGE-02)"; fi
# Sidecar must exist at expected encoded path
SIDECAR="$MERGE_DIR/.claude/skills/testskill/.conjure-conflict-skills_testskill_SKILL.md"
if [ -f "$SIDECAR" ]; then pass "sidecar written at correct path (MERGE-02)"
else fail "sidecar missing at $SIDECAR (MERGE-02)"; fi
if grep -q '<<<<<<<' "$SIDECAR"; then pass "sidecar contains conflict markers (MERGE-02)"
else fail "sidecar missing conflict markers (MERGE-02)"; fi
rm -rf "$MERGE_DIR"
trap - EXIT

# MERGE-03: Missing snapshot — cli/conjure update --apply aborts with D-01 message
# Expected: exits non-zero, prints "No base snapshot for v..."
MERGE_DIR="$(mktemp -d)"
trap 'rm -rf "$MERGE_DIR"' EXIT
mkdir -p "$MERGE_DIR/.claude"
printf '0.1.0\n' > "$MERGE_DIR/.claude/.conjure-version"
# Intentionally NO .conjure-templates-0.1.0/ directory
# Single invocation captures both output and exit code (do NOT use || true when testing exit code)
MERGE_OUT="$(CONJURE_HOME="$CONJURE_HOME" cli/conjure update --apply "$MERGE_DIR" 2>&1)"
MERGE_RC=$?
if [ "$MERGE_RC" -ne 0 ]; then pass "missing snapshot exits non-zero (MERGE-03)"
else fail "missing snapshot should exit non-zero (MERGE-03)"; fi
if printf '%s\n' "$MERGE_OUT" | grep -q "No base snapshot for v0.1.0"; then
  pass "correct abort message for missing snapshot (MERGE-03)"
else fail "abort message missing 'No base snapshot for v0.1.0' (MERGE-03)"; fi
rm -rf "$MERGE_DIR"
trap - EXIT

# MERGE-04: Generated files take upstream unconditionally (no 3-way merge, no sidecar)
# Expected: settings.json replaced by upstream; no .conjure-conflict-*settings* sidecar
# The stale key "conjure_test_stale_key" cannot appear in any real template — uniquely identifies old content
MERGE_DIR="$(mktemp -d)"
trap 'rm -rf "$MERGE_DIR"' EXIT
mkdir -p "$MERGE_DIR/.claude/.conjure-templates-${CONJURE_VERSION}"
# Stale settings.json with a unique key that no template contains
printf '{"conjure_test_stale_key": "should_be_replaced", "version": "old"}\n' \
  > "$MERGE_DIR/.claude/settings.json"
printf '%s\n' "$CONJURE_VERSION" > "$MERGE_DIR/.claude/.conjure-version"
# Run update --apply
CONJURE_HOME="$CONJURE_HOME" cli/conjure update --apply "$MERGE_DIR" >/dev/null 2>&1
# settings.json must NOT still contain the unique stale key (it was replaced by upstream)
if ! grep -q '"conjure_test_stale_key"' "$MERGE_DIR/.claude/settings.json" 2>/dev/null; then
  pass "settings.json replaced by upstream (stale key gone) (MERGE-04)"
else
  fail "settings.json not replaced by upstream (MERGE-04)"
fi
# No sidecar for settings.json
if [ -z "$(find "$MERGE_DIR/.claude" -name '.conjure-conflict-*settings*' 2>/dev/null)" ]; then
  pass "no conflict sidecar for generated settings.json (MERGE-04)"
else fail "sidecar written for generated settings.json — should take upstream (MERGE-04)"; fi
rm -rf "$MERGE_DIR"
trap - EXIT

# MERGE-05: audit detects <<<<<<< markers in .claude/ and exits non-zero
MERGE_DIR="$(mktemp -d)"
trap 'rm -rf "$MERGE_DIR"' EXIT
mkdir -p "$MERGE_DIR/.claude/skills/testskill"
# Plant a conflict marker in a real skill file (not a sidecar)
printf 'name: testskill\ndescription: A test skill with 30+ characters here\n<<<<<<< your version\nconflict_line: A\n=======\nconflict_line: B\n>>>>>>> upstream\n' \
  > "$MERGE_DIR/.claude/skills/testskill/SKILL.md"
AUDIT_OUT="$(bash "$CONJURE_HOME/scripts/audit-setup.sh" "$MERGE_DIR" 2>&1)"
AUDIT_RC=$?
if [ "$AUDIT_RC" -ne 0 ]; then pass "audit exits non-zero when conflict markers present (MERGE-05)"
else fail "audit should exit non-zero with conflict markers (MERGE-05)"; fi
if printf '%s\n' "$AUDIT_OUT" | grep -q "Unresolved merge conflicts"; then
  pass "audit reports 'Unresolved merge conflicts' (MERGE-05)"
else fail "audit missing 'Unresolved merge conflicts' message (MERGE-05)"; fi
rm -rf "$MERGE_DIR"
trap - EXIT

Implementation notes for the executor:
- The `source lib/mutate.sh` and `source lib/merge.sh` lines at the top of the MERGE section re-source the libs. Since tests/run.sh runs from CONJURE_HOME (line 7: cd "$CONJURE_HOME"), the source paths use $CONJURE_HOME which is already set.
- SC1090 shellcheck disable comments are needed for the dynamic source paths.
- Each MERGE block uses its own mktemp -d + trap EXIT. After each block, call `rm -rf "$MERGE_DIR"` and `trap - EXIT` to clean up before the next block starts a new trap.
- MERGE-03 uses a SINGLE invocation that captures both output and exit code: MERGE_OUT="$(... 2>&1)"; MERGE_RC=$?. Do NOT use || true in the subshell — that would swallow the non-zero exit code. The command-substitution $() preserves the exit code of the last command in the subshell only when || true is NOT appended.
- MERGE-04 test: The unique stale key "conjure_test_stale_key" cannot exist in any conjure template, so if it persists after --apply, the test correctly fails. This fixes the both-branches-pass bug. The update --apply will attempt a real merge; since we created an empty .conjure-templates-VERSION/ dir, the merge loop will find no files to merge, which is acceptable — we only care that settings.json was replaced (the mutate_cp at the start of --apply) and no sidecar exists.
- Module-level state reset before direct lib calls (MERGE-01, MERGE-02): CONJURE_MERGE_CONFLICT_COUNT=0 and CONJURE_MERGE_CONFLICT_FILES="" must be set immediately before each merge_file_3way call that tests these vars, to prevent carry-over from prior test blocks.
  </action>
  <verify>
    <automated>cd /Users/mohandoz/u01/innovate/conjure && grep -c 'MERGE-0[1-5]' tests/run.sh</automated>
  </verify>
  <done>tests/run.sh contains MERGE-01 through MERGE-05 test IDs; bash tests/run.sh passes all MERGE assertions; audit-setup.sh detects <<<<<<< markers and exits non-zero.</done>
</task>

<task type="auto">
  <name>Task 3: Full suite validation and commit</name>
  <files>scripts/audit-setup.sh, tests/run.sh</files>
  <action>
Run the following checks in order. Fix any failures before committing.

1. shellcheck on modified files:
   shellcheck -S error -e SC2164,SC2044,SC2034,SC2155 scripts/audit-setup.sh
   shellcheck -S error -e SC2164,SC2044,SC2034,SC2155 tests/run.sh

2. Verify conflict marker detection works:
   CONFLICT_DIR="$(mktemp -d)"
   mkdir -p "$CONFLICT_DIR/.claude"
   printf '<<<<<<< your version\nA\n=======\nB\n>>>>>>> upstream\n' > "$CONFLICT_DIR/.claude/CONFLICT.md"
   bash scripts/audit-setup.sh "$CONFLICT_DIR" 2>&1 | grep "Unresolved merge conflicts"
   # Must print the error line
   rm -rf "$CONFLICT_DIR"

3. Verify sidecar files are NOT flagged:
   SIDECAR_DIR="$(mktemp -d)"
   mkdir -p "$SIDECAR_DIR/.claude"
   # Sidecar (intentional markers — should not cause audit to fail)
   printf '<<<<<<< markers\nA\n=======\nB\n>>>>>>> end\n' > "$SIDECAR_DIR/.claude/.conjure-conflict-CLAUDE.md"
   # Clean file (no markers)
   printf 'clean content\n' > "$SIDECAR_DIR/.claude/CLAUDE.md"
   bash scripts/audit-setup.sh "$SIDECAR_DIR" 2>&1 | grep -v "Unresolved"
   # Should NOT print "Unresolved merge conflicts" (sidecar excluded)
   rm -rf "$SIDECAR_DIR"

4. Run full test suite:
   bash tests/run.sh 2>&1 | tail -5
   # Must show FAIL: 0

5. Quick MERGE subset:
   bash tests/run.sh 2>&1 | grep -E '(MERGE|PASS|FAIL)' | tail -20

6. Commit:
   git add scripts/audit-setup.sh tests/run.sh
   git commit -m "feat(09): add MERGE-05 audit check + MERGE-01/02/03/04 regression tests"
  </action>
  <verify>
    <automated>cd /Users/mohandoz/u01/innovate/conjure && bash tests/run.sh 2>&1 | tail -3</automated>
  </verify>
  <done>bash tests/run.sh exits 0 (FAIL: 0); shellcheck exits 0 on both modified files; git commit present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .claude/ file content → grep conflict check | Grep reads user's live harness files; output piped to err() for display |
| test mktemp dirs → lib/merge.sh calls | Synthetic test data; isolated per-test directories |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-07 | Information Disclosure | audit grep output lists conflict file paths | accept | File paths are already known to the repo owner running conjure audit on their own project |
| T-09-08 | Denial of Service | grep -rl on large .claude/ | accept | .claude/ is bounded in size by the 25,000 token audit check; grep on text files is fast |
| T-09-09 | Tampering | test block re-sources lib/merge.sh — could affect test state | mitigate | Module-level counters explicitly reset to 0/empty before each direct lib call in test blocks |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | No new package manager invocations in this plan |
</threat_model>

<verification>
1. shellcheck -S error -e SC2164,SC2044,SC2034,SC2155 scripts/audit-setup.sh — exits 0
2. shellcheck -S error -e SC2164,SC2044,SC2034,SC2155 tests/run.sh — exits 0
3. bash tests/run.sh — exits 0, FAIL: 0
4. bash tests/run.sh 2>&1 | grep MERGE — shows PASS lines for MERGE-01 through MERGE-05
5. Conflict detection: bash scripts/audit-setup.sh <dir-with-markers> — exits 2, prints "Unresolved merge conflicts"
6. Sidecar exclusion: bash scripts/audit-setup.sh <dir-with-sidecar-only> — does NOT print "Unresolved merge conflicts" for sidecar files
</verification>

<success_criteria>
- MERGE-05: audit-setup.sh detects <<<<<<< in .claude/ harness files (not sidecars), exits 2
- MERGE-01: merge_file_3way produces auto-merged result with both user+upstream edits, exits 0, no sidecar
- MERGE-02: merge_file_3way writes sidecar with markers, original untouched (D-05), exits 1
- MERGE-03: cli/conjure update --apply aborts with D-01 message when snapshot missing, exits non-zero; single CLI invocation captures both output and exit code correctly
- MERGE-04: settings.json taken upstream unconditionally; stale key "conjure_test_stale_key" absent after --apply; no sidecar written; else branch calls fail() not pass()
- Full bash tests/run.sh: exits 0 (FAIL: 0)
- All files shellcheck clean under project CI flag set
</success_criteria>

<output>
Create .planning/phases/09-3-way-merge/09-03-SUMMARY.md when done.
</output>
