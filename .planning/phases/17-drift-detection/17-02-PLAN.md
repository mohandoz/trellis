---
phase: 17-drift-detection
plan: 02
type: execute
wave: 2
depends_on:
  - 17-01
files_modified:
  - cli/conjure
  - tests/run.sh
autonomous: true
requirements:
  - DRIFT-01
  - DRIFT-02

must_haves:
  truths:
    - "`conjure check` is a recognized subcommand in cli/conjure"
    - "`conjure check --porcelain` emits machine-readable output consumable by scripts"
    - "`conjure check` exits 0 on a freshly-inited harness (round-trip test)"
    - "`conjure check` exits 1 when a kit file is modified in the harness"
    - "`conjure check` exits 1 when a kit file is deleted from the harness"
    - "`conjure check --porcelain` emits 'M .claude/settings.json' when settings.json is modified"
    - "`conjure check --porcelain` emits 'R .claude/hooks/post-edit-format.mjs' when that hook is deleted"
    - "`conjure check --porcelain` exits 0 on a fully-current harness"
    - "All 4 DRIFT regression tests pass in tests/run.sh"
  artifacts:
    - path: "cli/conjure"
      provides: "cmd_check function + dispatch entry + usage line"
      contains: "cmd_check"
    - path: "tests/run.sh"
      provides: "DRIFT-01 and DRIFT-02 regression test section"
      contains: "Drift detection tests"
  key_links:
    - from: "cli/conjure cmd_check"
      to: "scripts/check.sh"
      via: "bash invocation with CONJURE_HOME and CONJURE_PORCELAIN env vars"
      pattern: "CONJURE_PORCELAIN.*bash.*scripts/check\\.sh"
    - from: "tests/run.sh DRIFT section"
      to: "cli/conjure check"
      via: "CLI invocation in mktemp sandbox"
      pattern: "cli/conjure check"
---

<objective>
Wire cmd_check into cli/conjure and add regression tests to tests/run.sh covering exit codes and porcelain output.

Purpose: scripts/check.sh (Plan 01) implements all comparison logic but is not yet reachable from the CLI. This plan adds the thin cmd_check dispatcher in cli/conjure, registers it in the dispatch table and usage string, then validates the full round-trip via 4 regression tests in tests/run.sh. DRIFT-01 and DRIFT-02 are complete only when the tests are green.

Output:
- cli/conjure — cmd_check() function + "check)" dispatch entry + usage line added
- tests/run.sh — DRIFT section with 4 regression tests (no-drift exit 0; modified file exit 1 + porcelain M; removed file exit 1 + porcelain R; porcelain exit 0 on current)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/17-drift-detection/17-CONTEXT.md
@.planning/phases/17-drift-detection/17-RESEARCH.md
@.planning/phases/17-drift-detection/17-01-SUMMARY.md
@cli/conjure
@tests/run.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire cmd_check into cli/conjure</name>
  <files>cli/conjure</files>

  <read_first>
    - cli/conjure — full file; focus on: usage() function (lines ~27-51), cmd_audit() (lines ~142-158), dispatch case block (lines ~323-338)
    - 17-CONTEXT.md — cmd_check implementation approach section; exact env var names (CONJURE_HOME, CONJURE_PORCELAIN)
    - 17-RESEARCH.md — Pattern 1 (cmd_check dispatch) for the exact function signature and bash invocation line
  </read_first>

  <action>
    Make three edits to cli/conjure. Do NOT reformat unrelated sections.

    EDIT 1 — Add usage line for `conjure check`:
    In the usage() heredoc, after the line `conjure update [--check|--apply] [target]`, add:
      conjure check [--porcelain] [target]
    Keep indentation consistent with surrounding usage lines (2-space indent + column alignment).

    EDIT 2 — Add cmd_check function:
    Insert cmd_check immediately after cmd_audit() (before cmd_update()). Follow the exact pattern of cmd_audit():

      cmd_check() {
        local porcelain=0 target="$(pwd)"
        while [ $# -gt 0 ]; do
          case "$1" in
            --porcelain)  porcelain=1 ;;
            --help|-h)    echo "Usage: conjure check [--porcelain] [target]"; return 0 ;;
            *)            target="$1" ;;
          esac
          shift
        done
        CONJURE_HOME="$CONJURE_HOME" CONJURE_PORCELAIN="$porcelain" \
          bash "$CONJURE_HOME/scripts/check.sh" "$target"
      }

    Notes:
    - No cmd_preflight call (per 17-CONTEXT.md: read-only command, no mutations)
    - No `source lib/mutate.sh` (read-only; never calls mutate_*)
    - Pass target as positional arg to scripts/check.sh (not env var)
    - CONJURE_PORCELAIN is passed as env var to check.sh (check.sh reads CONJURE_PORCELAIN internally)

    EDIT 3 — Add dispatch entry in case block:
    In the dispatch case block (after `update) shift; cmd_update "$@" ;;`), add:
      check)           shift; cmd_check "$@"           ;;

    Place it after `update)` and before `refresh-graph)` to maintain alphabetical proximity.

    After the three edits, verify cli/conjure still passes shellcheck:
      shellcheck -S error -e SC2155 cli/conjure
  </action>

  <verify>
    <automated>
      cd /Users/mohandoz/u01/innovate/conjure &&
      grep -q 'cmd_check' cli/conjure &&
      grep -q 'check)' cli/conjure &&
      grep -q 'conjure check \[--porcelain\]' cli/conjure &&
      shellcheck -S error -e SC2155 cli/conjure &&
      echo "PASS: cmd_check wired, dispatch entry present, usage line added, shellcheck clean"
    </automated>
  </verify>

  <acceptance_criteria>
    1. cli/conjure contains a `cmd_check()` function
    2. The dispatch case block contains `check) shift; cmd_check "$@" ;;`
    3. The usage() function contains `conjure check [--porcelain] [target]`
    4. cmd_check passes CONJURE_PORCELAIN env var to scripts/check.sh
    5. cmd_check passes TARGET as positional arg ($1) to scripts/check.sh
    6. shellcheck -S error -e SC2155 cli/conjure exits 0
    7. `cli/conjure help` output includes "check" subcommand
  </acceptance_criteria>

  <done>
    cli/conjure dispatches `conjure check` to scripts/check.sh with correct env vars and target arg; shellcheck passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add DRIFT regression tests to tests/run.sh</name>
  <files>tests/run.sh</files>

  <read_first>
    - tests/run.sh — focus on: BREW section at end (lines ~1340-1380) and the summary block (final ~10 lines); identify the exact insertion point (before summary, after BREW section)
    - tests/lib/sandbox.sh — pass/fail helper signatures; mktemp sandbox setup pattern
    - 17-RESEARCH.md — Pattern 5 (test structure), exact test case code for DRIFT-01a, DRIFT-01b, DRIFT-01c, DRIFT-02
  </read_first>

  <action>
    Append a DRIFT detection test section to tests/run.sh. Insert it between the last BREW test block (ending with `trap - EXIT` after the BREW-04 assertion) and the summary block (`echo "═══..." "PASS: $PASS    FAIL: $FAIL"`). Keep the separator comment style consistent with the existing BREW section.

    The section header:
      # ──────────────────────────────────────────────────────────────────────────────
      # DRIFT detection tests (DRIFT-01, DRIFT-02)
      # ──────────────────────────────────────────────────────────────────────────────
      echo
      echo "▸ Drift detection tests (DRIFT-01, DRIFT-02)"

    DRIFT-01a — fresh init → no drift, exit 0:
    - mktemp -d into DRIFT_DIR
    - trap 'rm -rf "$DRIFT_DIR"' EXIT
    - write "# Test project\n" to $DRIFT_DIR/CLAUDE.md (required by cmd_init)
    - Run: CONJURE_HOME="$CONJURE_HOME" cli/conjure init "$DRIFT_DIR" >/dev/null 2>&1
    - Capture exit code: DRIFT_RC=0; CONJURE_HOME="$CONJURE_HOME" cli/conjure check "$DRIFT_DIR" >/dev/null 2>&1 || DRIFT_RC=$?
    - Assert: [ "$DRIFT_RC" -eq 0 ] → pass/fail "check exits 0 on fully-current harness (DRIFT-01)"
    - rm -rf "$DRIFT_DIR"; trap - EXIT

    DRIFT-01b — modified file (settings.json) → exit 1 + porcelain M line:
    - mktemp -d into DRIFT_DIR
    - trap 'rm -rf "$DRIFT_DIR"' EXIT
    - init as above (printf '# Test project\n' → cli/conjure init)
    - Corrupt: printf 'user-edit\n' >> "$DRIFT_DIR/.claude/settings.json"
    - Capture porcelain output: DRIFT_OUT="$(CONJURE_HOME="$CONJURE_HOME" cli/conjure check --porcelain "$DRIFT_DIR" 2>&1 || true)"
    - Capture exit code: DRIFT_RC=0; CONJURE_HOME="$CONJURE_HOME" cli/conjure check "$DRIFT_DIR" >/dev/null 2>&1 || DRIFT_RC=$?
    - Assert exit 1: pass/fail "check exits 1 when file is modified (DRIFT-01)"
    - Assert porcelain M line: printf '%s\n' "$DRIFT_OUT" | grep -q '^M .claude/settings.json' → pass/fail "--porcelain emits 'M .claude/settings.json' (DRIFT-02)"
    - rm -rf "$DRIFT_DIR"; trap - EXIT

    DRIFT-01c — removed file (post-edit-format.mjs) → exit 1 + porcelain R line:
    - mktemp -d into DRIFT_DIR
    - trap 'rm -rf "$DRIFT_DIR"' EXIT
    - init as above
    - Delete: rm -f "$DRIFT_DIR/.claude/hooks/post-edit-format.mjs"
    - Capture porcelain output: DRIFT_OUT="$(CONJURE_HOME="$CONJURE_HOME" cli/conjure check --porcelain "$DRIFT_DIR" 2>&1 || true)"
    - Capture exit code: DRIFT_RC=0; CONJURE_HOME="$CONJURE_HOME" cli/conjure check "$DRIFT_DIR" >/dev/null 2>&1 || DRIFT_RC=$?
    - Assert exit 1: pass/fail "check exits 1 when kit file is removed from harness (DRIFT-01)"
    - Assert porcelain R line: printf '%s\n' "$DRIFT_OUT" | grep -q '^R .claude/hooks/post-edit-format.mjs' → pass/fail "--porcelain emits 'R' for removed hook (DRIFT-02)"
    - rm -rf "$DRIFT_DIR"; trap - EXIT

    DRIFT-02 — porcelain exit 0 on current harness:
    - mktemp -d into DRIFT_DIR
    - trap 'rm -rf "$DRIFT_DIR"' EXIT
    - init as above
    - PORE_RC=0; CONJURE_HOME="$CONJURE_HOME" cli/conjure check --porcelain "$DRIFT_DIR" >/dev/null 2>&1 || PORE_RC=$?
    - Assert: [ "$PORE_RC" -eq 0 ] → pass/fail "--porcelain exits 0 when harness is current (DRIFT-02)"
    - rm -rf "$DRIFT_DIR"; trap - EXIT

    IMPORTANT: Use `printf '# Test project\n'` not `echo` for the CLAUDE.md content to avoid platform newline differences. Each mktemp sandbox must have its own `trap ... EXIT` / `trap - EXIT` pair to avoid leaked temp dirs on failure. Do not nest traps — reset to `trap - EXIT` after each cleanup rm.

    After adding the section, run the full test suite to confirm it passes:
      bash tests/run.sh
  </action>

  <verify>
    <automated>
      cd /Users/mohandoz/u01/innovate/conjure &&
      grep -q 'Drift detection tests' tests/run.sh &&
      bash tests/run.sh 2>&1 | grep -E 'PASS|FAIL' | tail -3 &&
      bash tests/run.sh 2>&1 | grep 'DRIFT' | grep -c '✓' | grep -v '^0$' &&
      echo "PASS: DRIFT section present and tests passing"
    </automated>
  </verify>

  <acceptance_criteria>
    1. tests/run.sh contains "Drift detection tests (DRIFT-01, DRIFT-02)" section header
    2. All 4 test cases (6 individual assertions) are added (DRIFT-01a, DRIFT-01b, DRIFT-01c, DRIFT-02 porcelain)
    3. `bash tests/run.sh` exits 0 (FAIL count = 0)
    4. The DRIFT section passes all 6 assertions (exit 0 check, exit 1 modified, M line, exit 1 removed, R line, porcelain exit 0)
    5. No temp dirs are leaked (each sandbox uses mktemp + rm -rf + trap reset)
    6. Full test suite still passes (existing BREW, OVLY, etc. tests remain green)
  </acceptance_criteria>

  <done>
    tests/run.sh includes a DRIFT section with 4 test cases covering DRIFT-01 and DRIFT-02; bash tests/run.sh exits 0 with FAIL: 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI flag --porcelain | User-controlled; accepted via case match; safe string comparison only |
| TARGET positional arg | Passed from cmd_check to check.sh; validated inside check.sh |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-04 | Spoofing | cli/conjure check dispatch | accept | Dispatches only to scripts/check.sh; no privilege escalation |
| T-17-05 | Repudiation | tests/run.sh DRIFT section | accept | Tests run locally; no audit log needed for test framework |
| T-17-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs in this plan |
</threat_model>

<verification>
Overall phase checks after both plans are complete:

  cd /Users/mohandoz/u01/innovate/conjure

  # 1. shellcheck on both new files
  shellcheck -S error -e SC2155 scripts/check.sh
  shellcheck -S error -e SC2155 cli/conjure

  # 2. Full test suite green
  bash tests/run.sh

  # 3. CLI help includes check
  cli/conjure help | grep -q 'check'

  # 4. Round-trip smoke test (manual, confirm output)
  CONJURE_HOME=$(pwd) cli/conjure check $(pwd) 2>&1
  CONJURE_HOME=$(pwd) cli/conjure check --porcelain $(pwd) 2>&1
</verification>

<success_criteria>
- `conjure check` is listed in `conjure help` output
- `conjure check [target]` exits 0 on a current harness, exits 1 on any drift
- `conjure check --porcelain` emits `<M|R|A> <path>` lines consumable by AUTPR-01 (Phase 19)
- `bash tests/run.sh` exits 0 with all DRIFT assertions green
- shellcheck passes on both scripts/check.sh and cli/conjure
- No bash 4+ constructs in any new code
</success_criteria>

<output>
Create .planning/phases/17-drift-detection/17-02-SUMMARY.md when done
</output>
