---
phase: 03-sandboxed-per-profile-fixtures
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - tests/fixtures/ts-next/
  - tests/fixtures/java-spring/
  - tests/fixtures/rust-axum/
  - tests/fixtures/go-gin/
  - tests/fixtures/python-fastapi/
  - tests/fixtures/node-nest/
  - tests/fixtures/monorepo/
  - tests/fixtures/polyglot/
  - tests/fixtures/data-science/
autonomous: true
requirements:
  - TEST-01

must_haves:
  truths:
    - "All 9 profile fixture directories exist under tests/fixtures/<profile>/"
    - "Each fixture directory contains CLAUDE.md, the profile manifest stub, and .claude/"
    - "Each fixture's CLAUDE.md starts with the GENERATED header (D-12)"
    - "Each fixture's CLAUDE.md contains the profile marker comment appended by apply.sh"
    - "Each fixture audits green: bash scripts/audit-setup.sh exits 0 for all 9 profiles"
    - "CLAUDE.md line counts are 30–45 lines per profile (all under 100-line cap)"
    - "monorepo fixture contains packages/api/ subdirectory"
    - "No fixture contains graphify-out/ or @imports in CLAUDE.md"
  artifacts:
    - path: "tests/fixtures/ts-next/CLAUDE.md"
      provides: "ts-next profile fixture"
      contains: "GENERATED"
    - path: "tests/fixtures/monorepo/packages/api"
      provides: "monorepo subpackage dir (required by monorepo apply.sh)"
    - path: "tests/fixtures/java-spring/.claude/settings.json"
      provides: "node .mjs hook wiring (SAFE-03)"
      contains: "node"
  key_links:
    - from: "scripts/regen-fixtures.sh"
      to: "tests/fixtures/<profile>/"
      via: "conjure init --profile=<p> then cp -r"
      pattern: "conjure.*init.*--profile"
    - from: "tests/fixtures/<profile>/.claude/settings.json"
      to: "node .mjs hooks"
      via: "SAFE-03 — no bash .sh wiring"
      pattern: "node.*\\.mjs"
---

<objective>
Run scripts/regen-fixtures.sh to generate all 9 per-profile fixture directories under tests/fixtures/
and verify each audits green. This is the bulk content of Phase 3 — 9 committed, hermetic example
projects each reflecting actual `conjure init` output.

Purpose: Committed fixtures are the reference corpus for Phase 4's regression suite. They must be
generated (not hand-crafted) so they always reflect actual init output. Each must audit green to
prove the profile templates produce valid harnesses.

Output: tests/fixtures/{ts-next,java-spring,rust-axum,go-gin,python-fastapi,node-nest,monorepo,polyglot,data-science}/
</objective>

## Phase Goal

**As a** Conjure developer, **I want to** one committed audited fixture per stack profile, **so that**
the regression suite has a stable reference corpus reflecting real `conjure init` output.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-sandboxed-per-profile-fixtures/03-CONTEXT.md
@.planning/phases/03-sandboxed-per-profile-fixtures/03-RESEARCH.md
@.planning/phases/03-sandboxed-per-profile-fixtures/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run regen-fixtures.sh to generate all 9 green profile fixtures</name>
  <files>
    tests/fixtures/ts-next/
    tests/fixtures/java-spring/
    tests/fixtures/rust-axum/
    tests/fixtures/go-gin/
    tests/fixtures/python-fastapi/
    tests/fixtures/node-nest/
    tests/fixtures/monorepo/
    tests/fixtures/polyglot/
    tests/fixtures/data-science/
  </files>

  <read_first>
    - scripts/regen-fixtures.sh — the script created in Plan 01; understand its exit behavior
      so failures are caught (it exits 1 if any fixture fails audit self-check)
    - .planning/phases/03-sandboxed-per-profile-fixtures/03-RESEARCH.md — Critical Finding 1
      (init does NOT create CLAUDE.md), Finding 4 (4 profiles have no preflight.sh), Finding 5
      (java-spring adds a bash .sh hook — acceptable), Finding 6 (monorepo needs packages/api/)
    - .planning/phases/03-sandboxed-per-profile-fixtures/03-01-SUMMARY.md — confirm regen-fixtures.sh
      is complete before running it
  </read_first>

  <action>
    Run `bash scripts/regen-fixtures.sh` from the conjure root (i.e., with CONJURE_HOME=$PWD).
    The script will:
      - Iterate all 9 profiles in order
      - Print `[regen] <profile>` and `[regen] <profile> done` for each
      - Self-validate each fixture via audit-setup.sh and exit 1 if any fails

    If the script exits non-zero, diagnose from the `[regen] WARN:` line which profile failed and
    inspect the generated fixture's CLAUDE.md to identify the cause. Common causes and fixes:
      - "CLAUDE.md missing": seed CLAUDE.md was not written before init — check _write_seed_claude
      - "HARD CAP": seed CLAUDE.md is too long — check seed line count (target: ~22 lines)
      - "monorepo fragment missing": packages/api/ not in seed — check _write_manifest for monorepo
      - Profile fragment not appended: apply.sh guard `[ -f "$TARGET/CLAUDE.md" ]` not satisfied

    After the script exits 0, verify the directory structure for a representative sample:
      - ts-next: has CLAUDE.md, package.json, .claude/, docs/
      - monorepo: has packages/api/ subdir
      - java-spring: .claude/ contains post-edit-format.sh (acceptable legacy hook)

    Do NOT manually edit any file under tests/fixtures/. If a fixture is wrong, fix regen-fixtures.sh
    and re-run. Hand-editing diverges fixtures from real init output (anti-pattern per RESEARCH.md).
  </action>

  <verify>
    <automated>
      bash -c 'for p in ts-next java-spring rust-axum go-gin python-fastapi node-nest monorepo polyglot data-science; do [ -d "tests/fixtures/$p/.claude" ] || { echo "MISSING: tests/fixtures/$p/.claude"; exit 1; }; done && echo "All 9 fixture .claude/ dirs exist"'
    </automated>
  </verify>

  <acceptance_criteria>
    - `tests/fixtures/` contains exactly these 9 subdirectories: ts-next, java-spring, rust-axum,
      go-gin, python-fastapi, node-nest, monorepo, polyglot, data-science
    - Each of the 9 fixture directories contains a `.claude/` subdirectory
    - Each of the 9 fixture directories contains a `CLAUDE.md` file
    - `grep -l 'GENERATED' tests/fixtures/*/CLAUDE.md` lists all 9 fixtures (GENERATED header present)
    - `grep -l 'GENERATED' tests/fixtures/*/CLAUDE.md | wc -l` equals 9
    - `tests/fixtures/monorepo/packages/api` directory exists
    - Each fixture's CLAUDE.md is under 100 lines: `wc -l tests/fixtures/*/CLAUDE.md | grep -v total | awk '{if($1>100) print $0}' | wc -l` equals 0
    - `bash scripts/audit-setup.sh tests/fixtures/ts-next` exits 0 (green audit)
    - `bash scripts/audit-setup.sh tests/fixtures/monorepo` exits 0 (green audit)
    - No fixture contains `@imports`: `grep -rn '^@' tests/fixtures/*/CLAUDE.md 2>/dev/null | wc -l` equals 0
    - No fixture contains `graphify-out/`: `find tests/fixtures -name 'graphify-out' -type d | wc -l` equals 0
  </acceptance_criteria>

  <done>All 9 fixture directories exist under tests/fixtures/, each contains .claude/ + CLAUDE.md with GENERATED header, and bash scripts/audit-setup.sh exits 0 for ts-next and monorepo (representative sample).</done>
</task>

<task type="auto">
  <name>Task 2: Verify all 9 fixtures audit green individually</name>
  <files>tests/fixtures/</files>

  <read_first>
    - scripts/audit-setup.sh — the audit script invoked per fixture; understand exit codes: 0=green,
      1=warn, 2=err; the fixture loop uses this directly (not cli/conjure audit)
    - .planning/phases/03-sandboxed-per-profile-fixtures/03-RESEARCH.md — Finding 8 (exact green
      audit output: PASS: 17, WARN: 0, FAIL: 0); Finding 3 (audit-setup.sh is environment-isolated
      by design — it uses no env vars, only the TARGET dir argument)
  </read_first>

  <action>
    Run audit-setup.sh against each of the 9 fixtures individually and confirm each exits 0.
    Invoke it directly (NOT via cli/conjure audit — preflight would introduce PATH non-determinism):

      for p in ts-next java-spring rust-axum go-gin python-fastapi node-nest monorepo polyglot data-science; do
        bash scripts/audit-setup.sh "tests/fixtures/$p" && echo "GREEN: $p" || echo "FAIL: $p (rc=$?)"
      done

    If any fixture exits non-zero:
      - Exit 1 (WARN): check that fixture's CLAUDE.md line count (`wc -l`) — if 101–200, it is in
        the warn band; if so fix regen-fixtures.sh seed content and re-run regen for that profile
        using `bash scripts/regen-fixtures.sh --profile <p>`
      - Exit 2 (ERR): check for missing CLAUDE.md, @imports, or missing .claude/ directory

    The java-spring fixture will contain a `.claude/hooks/post-edit-format.sh` file alongside the
    five .mjs hooks. This is an acceptable legacy artifact (RESEARCH.md Finding 5) — audit-setup.sh
    only validates .mjs hooks; the .sh file does not cause an audit failure.

    Do NOT modify audit-setup.sh. Do NOT modify fixture files by hand. Fix via regen only.
  </action>

  <verify>
    <automated>
      bash -c 'FAIL=0; for p in ts-next java-spring rust-axum go-gin python-fastapi node-nest monorepo polyglot data-science; do bash scripts/audit-setup.sh "tests/fixtures/$p" >/dev/null 2>&1 && echo "  GREEN: $p" || { echo "  FAIL:  $p"; FAIL=$((FAIL+1)); }; done; [ "$FAIL" -eq 0 ] && echo "All 9 fixtures green" || { echo "$FAIL fixture(s) failed audit"; exit 1; }'
    </automated>
  </verify>

  <acceptance_criteria>
    - `bash scripts/audit-setup.sh tests/fixtures/ts-next` exits 0
    - `bash scripts/audit-setup.sh tests/fixtures/java-spring` exits 0
    - `bash scripts/audit-setup.sh tests/fixtures/rust-axum` exits 0
    - `bash scripts/audit-setup.sh tests/fixtures/go-gin` exits 0
    - `bash scripts/audit-setup.sh tests/fixtures/python-fastapi` exits 0
    - `bash scripts/audit-setup.sh tests/fixtures/node-nest` exits 0
    - `bash scripts/audit-setup.sh tests/fixtures/monorepo` exits 0
    - `bash scripts/audit-setup.sh tests/fixtures/polyglot` exits 0
    - `bash scripts/audit-setup.sh tests/fixtures/data-science` exits 0
    - The automated verify command above exits 0 (all 9 GREEN)
    - `grep -c 'node.*\.mjs' tests/fixtures/ts-next/.claude/settings.json` is greater than 0 (SAFE-03)
  </acceptance_criteria>

  <done>All 9 fixtures audit green (exit 0) when bash scripts/audit-setup.sh is invoked directly against each.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| regen output → committed fixtures | Generated fixtures become the reference corpus; wrong content silently becomes the "truth" |
| fixture .claude/ settings.json → hook wiring | If SAFE-03 regressed, fixtures would embed bash .sh wiring instead of node .mjs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Tampering | Generated fixtures — SAFE-03 regression | mitigate | Accept criteria checks `grep -c 'node.*\.mjs' tests/fixtures/ts-next/.claude/settings.json`; regen-fixtures.sh uses conjure init which produces SAFE-03-compliant wiring |
| T-03-06 | Tampering | Hand-edited fixture files | mitigate | GENERATED header (D-12) signals to contributors not to edit; executor must use regen only, never direct edits |
| T-03-07 | Information Disclosure | audit-setup.sh reading developer $HOME | accept | audit-setup.sh is verified environment-isolated (no $HOME usage, RESEARCH.md Finding 3); sandbox wrap added in Plan 03 for test runner isolation |
| T-03-08 | Denial of Service | graphify-out/ in fixture pollutes audit output | mitigate | Accept criteria asserts `find tests/fixtures -name 'graphify-out' -type d | wc -l` equals 0; regen-fixtures.sh copies only seed content |
| T-03-SC | Tampering | No npm/pip/cargo installs | accept | No new package-manager installs; no slopcheck needed |
</threat_model>

<verification>
After both tasks:
1. `ls tests/fixtures/` shows exactly 9 profile directories (no _broken yet — that is Plan 03)
2. All 9 audit green: the automated verify command in Task 2 exits 0
3. `grep -l 'GENERATED' tests/fixtures/*/CLAUDE.md | wc -l` outputs 9
4. `tests/fixtures/monorepo/packages/api` exists as a directory
5. `grep -c 'node.*\.mjs' tests/fixtures/ts-next/.claude/settings.json` is greater than 0
</verification>

<success_criteria>
- 9 fixture directories committed under tests/fixtures/, each containing .claude/ + CLAUDE.md with GENERATED header
- All 9 fixtures audit green (exit 0) when audit-setup.sh invoked directly
- monorepo fixture has packages/api/ subdir
- No fixture has @imports or graphify-out/
- All CLAUDE.md files are 30–45 lines (well under 100-line cap)
</success_criteria>

<output>
Create `.planning/phases/03-sandboxed-per-profile-fixtures/03-02-SUMMARY.md` when done
</output>
