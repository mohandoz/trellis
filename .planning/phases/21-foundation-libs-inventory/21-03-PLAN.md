---
phase: 21-foundation-libs-inventory
plan: 03
type: execute
wave: 2
depends_on:
  - 21-02
files_modified:
  - lib/snapshot.sh
  - lib/inventory.sh
autonomous: true
requirements:
  - INV-01
  - INV-02
  - INV-03
  - INV-04

must_haves:
  truths:
    - "lib/snapshot.sh snapshot_create copies the target to .conjure-adopt-backups/conjure-adopt-<UTC-ts>/ via raw cp -a (never mutate_cp); snapshot dir is non-empty and contains CLAUDE.md"
    - "lib/snapshot.sh DRY_RUN=1 prints the would-be snapshot path and skips cp -a"
    - "lib/inventory.sh inventory_classify correctly routes each of the 6 bucket paths; unknown for files outside harness dirs (D-03)"
    - "lib/inventory.sh inventory_classify emits ONLY the 6 deterministic buckets and NEVER candidate-skill/candidate-agent/stale-candidate — those are LLM judgment owned by the Phase 23 restructure skill, preserving the split-responsibility invariant (D-02); verifiable: every manifest classification value is a member of {core,skill,agent,planning-doc,reference-doc,unknown}"
    - "lib/inventory.sh inventory_scan respects the 500-file cap with harness-first budget (D-10); sets scan_capped=true + total_found when over cap"
    - "lib/inventory.sh inventory_emit_manifest emits a valid adopt-manifest.json with all required top-level keys; DRY_RUN writes to /tmp"
    - "bash tests/run.sh Phase 21 SC-2, INV-01..INV-04 sections all pass"
  artifacts:
    - path: "lib/snapshot.sh"
      provides: "snapshot_create / snapshot_rollback / snapshot_list; raw cp -a; log_step SNAPSHOT integration"
      exports: ["snapshot_create", "snapshot_rollback", "snapshot_list"]
    - path: "lib/inventory.sh"
      provides: "inventory_scan / inventory_classify / inventory_emit_manifest; 6-bucket classifier; adopt-manifest.json emitter"
      exports: ["inventory_scan", "inventory_classify", "inventory_emit_manifest"]
  key_links:
    - from: "lib/snapshot.sh"
      to: "lib/log.sh"
      via: "log_step SNAPSHOT called after cp -a completes"
      pattern: "log_step.*SNAPSHOT"
    - from: "lib/inventory.sh"
      to: "lib/caps.sh"
      via: "CLAUDE_MD_CAP / SKILL_MD_CAP / AGENT_MD_CAP used for size_cap_exceeded calculation (D-09)"
      pattern: "CLAUDE_MD_CAP\\|SKILL_MD_CAP\\|AGENT_MD_CAP"
    - from: "lib/inventory.sh inventory_emit_manifest"
      to: "lib/mutate.sh"
      via: "mutate_write for manifest output; DRY_RUN writes to /tmp/adopt-manifest-dryrun.json"
      pattern: "mutate_write.*adopt-manifest"
    - from: "lib/inventory.sh inventory_classify"
      to: "CLAUDE.md outbound links"
      via: "D-06: one grep pass to extract ](path) links; file linked from CLAUDE.md → reference-doc"
      pattern: "linked_from.*CLAUDE\\.md"
---

<objective>
Implement lib/snapshot.sh (full timestamped backup primitive) and lib/inventory.sh
(read-only markdown scanner + 6-bucket classifier + adopt-manifest.json emitter).
These are the two most complex files in Phase 21.

lib/snapshot.sh is the "lose nothing" guarantee: it uses raw cp -a (deliberately
bypassing mutate_cp — DRY_RUN must never suppress a backup). It integrates with
log.sh via log_step.

lib/inventory.sh is the CLI↔skill contract generator: it scans all markdown files,
classifies each into one of the 6 deterministic D-01 buckets, respects the 500-file
cap with harness-first budget ordering (D-10), and emits adopt-manifest.json via
mutate_write (DRY_RUN writes to /tmp). This is the largest file this phase.

Purpose: After Plan 03 completes, all Phase 21 library primitives exist. Plan 04
needs only the audit-setup.sh call-site change and test cleanup.

Output:
- lib/snapshot.sh (new)
- lib/inventory.sh (new, largest file this phase)
</objective>

<execution_context>
@/Users/mohandoz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mohandoz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/21-foundation-libs-inventory/21-CONTEXT.md
@.planning/phases/21-foundation-libs-inventory/21-RESEARCH.md
@.planning/phases/21-foundation-libs-inventory/21-PATTERNS.md
@lib/mutate.sh
@lib/merge.sh
@lib/caps.sh
@lib/log.sh
@tests/fixtures/brownfield-simple/CLAUDE.md
@.planning/phases/21-foundation-libs-inventory/21-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement lib/snapshot.sh (per SC-2, SAFE-01 primitive)</name>
  <files>lib/snapshot.sh</files>
  <read_first>
    - lib/merge.sh lines 1-16 — sibling lib header pattern (no shebang, Requires: comment, idempotent state init)
    - tests/lib/sandbox.sh line 50 — cp -r isolation pattern; snapshot_create uses cp -a (preserve symlinks + timestamps per M-4)
    - lib/mutate.sh lines 22-28 — DRY_RUN guard pattern; snapshot_create must NOT use mutate_cp (PATTERNS.md Critical Implementation Notes — deliberate exception)
    - .planning/phases/21-foundation-libs-inventory/21-RESEARCH.md Pattern 3 — snapshot_create with cp -a fallback; DRY_RUN print-only path
    - .planning/phases/21-foundation-libs-inventory/21-RESEARCH.md Pitfall 5 — cp -a vs cp -Rp cross-platform fallback
    - .planning/phases/21-foundation-libs-inventory/21-PATTERNS.md Pattern 3 — snapshot_create full annotated code including DRY_RUN handling
    - .planning/phases/21-foundation-libs-inventory/21-CONTEXT.md D-12 — UTC timestamp format for directory naming: date -u +%Y%m%dT%H%M%SZ
    - lib/log.sh — log_step function signature (log_step <phase> <message>)
    - scripts/audit-setup.sh lines 116-122 — cross-platform stat pattern (macOS -f %m vs Linux -c %Y) for reference
  </read_first>
  <behavior>
    snapshot_create:
    - Test 1: DRY_RUN=1 — call snapshot_create <target> <backup_root>; output contains "[dry-run] would snapshot"; no actual directory created under backup_root; CONJURE_SNAPSHOT_PATH is set to the would-be path
    - Test 2: DRY_RUN=0 live — creates a directory under backup_root named "conjure-adopt-<UTC-ts>" where ts matches pattern [0-9]{8}T[0-9]{6}Z; directory is non-empty
    - Test 3: DRY_RUN=0 live with brownfield-simple fixture — snapshot dir contains CLAUDE.md (core check from ROADMAP success criterion 2)
    - Test 4: DRY_RUN=0 — CONJURE_SNAPSHOT_PATH is set to the created snapshot directory path
    - Test 5: DRY_RUN=0 — log_step SNAPSHOT is called and RESTRUCTURE-LOG.md contains a SNAPSHOT entry (integration with log.sh)
    - Test 6: shellcheck lib/snapshot.sh exits 0

    snapshot_rollback:
    - Test 7: cp -a restore from snapshot_path back to target; log_step ROLLBACK written to RESTRUCTURE-LOG.md

    snapshot_list:
    - Test 8: with 2 snapshots in backup_root, lists both in sorted order (newest first)
  </behavior>
  <action>
    Create lib/snapshot.sh following the merge.sh sibling lib pattern:
    - No shebang; header comment: "lib/snapshot.sh — full timestamped snapshot for Conjure adopt."
    - "Source this file; requires lib/mutate.sh and lib/log.sh already sourced."
    - "POSIX bash 3.2+. No associative arrays, no mapfile, no local -n."
    - Module-level state init: CONJURE_SNAPSHOT_PATH="${CONJURE_SNAPSHOT_PATH:-}"

    snapshot_create <target> <backup_root>:
    - Compute ts using: ts="$(date -u '+%Y%m%dT%H%M%SZ')"
    - snap_dir="${backup_root}/conjure-adopt-${ts}"
    - DRY_RUN guard: if DRY_RUN=1, echo "[dry-run] would snapshot ${target} → ${snap_dir}", set CONJURE_SNAPSHOT_PATH="${snap_dir}", return 0. Do NOT increment CONJURE_DRY_MUTATION_COUNT here — snapshot is the safety primitive, not a counted mutation.
    - Live mode: mkdir -p "${snap_dir}"
    - cp -a "${target}/." "${snap_dir}/" to copy all contents preserving symlinks and timestamps. If cp -a fails (exit code != 0), fall back to cp -Rp "${target}" "${snap_dir}/" (Pitfall 5 cross-platform fallback). If both fail, echo error and return 1.
    - Write snapshot-meta.json to "${snap_dir}/.snapshot-meta.json" using jq -cn. Fields: created_at (ISO 8601 UTC), target (absolute path of target), git_head (from git -C "${target}" rev-parse HEAD 2>/dev/null or empty string), git_stash_list (from git -C "${target}" stash list 2>/dev/null | head -10 or empty string). Use jq --arg for all string fields. Write via mutate_write (live mode: writes to snap_dir; DRY_RUN already returned above so this is always live here).
    - Set CONJURE_SNAPSHOT_PATH="${snap_dir}"
    - Call log_step SNAPSHOT "created at ${snap_dir}" (requires RESTRUCTURE_LOG_PATH to be set by caller before log_step works; if RESTRUCTURE_LOG_PATH is empty, skip log_step silently)

    snapshot_rollback <snapshot_path> <target>:
    - Validate snapshot_path exists; if not, echo error and return 1
    - cp -a "${snapshot_path}/." "${target}/" to restore (same fallback logic as above)
    - Call log_step ROLLBACK "restored from ${snapshot_path}"

    snapshot_list <backup_root>:
    - List all conjure-adopt-* dirs under backup_root sorted newest-first
    - Use: ls -1t "${backup_root}"/conjure-adopt-* 2>/dev/null — printf each line
    - POSIX compatible; no associative arrays

    All file iteration must use mktemp + while IFS= read -r pattern (no process substitution — PATTERNS.md Pattern 4/Critical Notes). No echo -e/-n; use printf.

    Must pass shellcheck with no errors. Use local variables throughout.
  </action>
  <verify>
    <automated>
      shellcheck lib/snapshot.sh
      bash -c '
        source lib/mutate.sh
        source lib/log.sh
        source lib/snapshot.sh
        TGT="$(mktemp -d)"
        cp -r tests/fixtures/brownfield-simple/. "$TGT/"
        BK="$(mktemp -d)"
        RESTRUCTURE_LOG_PATH="$TGT/RESTRUCTURE-LOG.md"
        DRY_RUN=0 snapshot_create "$TGT" "$BK"
        echo "snap_path=$CONJURE_SNAPSHOT_PATH"
        echo "claude_md=$([ -f "${CONJURE_SNAPSHOT_PATH}/CLAUDE.md" ] && echo present || echo MISSING)"
        rm -rf "$TGT" "$BK"
      '
      bash tests/run.sh 2>&1 | grep -E "Phase 21.*SC-2\|Phase 21.*snapshot" | head -10
    </automated>
  </verify>
  <acceptance_criteria>
    - shellcheck lib/snapshot.sh exits 0
    - DRY_RUN=1: output contains "[dry-run] would snapshot"; no directory created; CONJURE_SNAPSHOT_PATH set to would-be path
    - DRY_RUN=0: snapshot dir created at backup_root/conjure-adopt-<ts>; dir non-empty
    - snapshot dir contains CLAUDE.md after copying brownfield-simple fixture (ROADMAP success criterion 2)
    - CONJURE_SNAPSHOT_PATH set to snapshot dir absolute path
    - .snapshot-meta.json present in snapshot dir; jq empty exits 0 on it
    - log_step SNAPSHOT called: RESTRUCTURE-LOG.md contains "SNAPSHOT" entry when RESTRUCTURE_LOG_PATH is set
    - bash tests/run.sh 2>&1 | grep "Phase 21.*SC-2" shows at least 2 pass lines (DRY_RUN + live snapshot)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement lib/inventory.sh (per D-01..D-10, INV-01..INV-04)</name>
  <files>lib/inventory.sh</files>
  <read_first>
    - lib/merge.sh lines 106-123 — POSIX mktemp + while IFS= read -r find loop (must use this pattern; no process substitution)
    - scripts/audit-setup.sh lines 47-68 — per-file wc -l and find -name SKILL.md patterns; wc -l < "$skill" redirect form avoids filename noise
    - .planning/phases/21-foundation-libs-inventory/21-RESEARCH.md Pattern 4 — POSIX bash 3.2+ find loop with mktemp
    - .planning/phases/21-foundation-libs-inventory/21-RESEARCH.md Pattern 6 — inventory_classify full decision tree (D-01 through D-07); exact case statement structure
    - .planning/phases/21-foundation-libs-inventory/21-RESEARCH.md Pattern 7 — finalized adopt-manifest.json schema sample (all D-01/D-05/D-08/D-09 overrides applied)
    - .planning/phases/21-foundation-libs-inventory/21-RESEARCH.md Code Examples section — extract_claude_md_links() + emit_file_entry() concrete implementations
    - .planning/phases/21-foundation-libs-inventory/21-RESEARCH.md Pitfall 2 (D-03 path-first) + Pitfall 3 (harness-first budget) + Pitfall 6 (jq --slurpfile for large manifests)
    - .planning/phases/21-foundation-libs-inventory/21-CONTEXT.md D-03/D-04/D-06/D-07/D-08/D-09/D-10 — each shapes a specific behavior in inventory_classify and inventory_scan
    - lib/caps.sh — CLAUDE_MD_CAP/SKILL_MD_CAP/AGENT_MD_CAP used for size_cap_exceeded computation
    - lib/mutate.sh — mutate_write for manifest output; DRY_RUN path = /tmp/adopt-manifest-dryrun.json
  </read_first>
  <behavior>
    inventory_classify:
    - Test 1: classify "CLAUDE.md" → "core" bucket
    - Test 2: classify ".claude/skills/git/SKILL.md" → "skill" bucket
    - Test 3: classify ".claude/agents/deploy.md" → "agent" bucket
    - Test 4: classify ".planning/21-PLAN.md" → "planning-doc" bucket
    - Test 5: classify "docs/README.md" → "reference-doc" bucket (path-based, D-07)
    - Test 6: classify a random.md outside harness dirs → "unknown" bucket (D-03 conservative)
    - Test 7: a symlink → skipped (return "SKIP:symlink", not classified)
    - Test 8: a file outside .claude/skills/ with skill-like frontmatter → "unknown" (D-03 path-first)
    - Test 9: a file linked from CLAUDE.md via ](path) → "reference-doc" (D-06/D-07)

    inventory_scan:
    - Test 10: scan brownfield-simple fixture; CONJURE_INVENTORY_ITEMS contains entries for CLAUDE.md, skill, agent, planning doc, docs/README.md
    - Test 11: symlink NOT in CONJURE_INVENTORY_ITEMS (skipped by test -L)
    - Test 12: scan 510-file fixture (from generate-large.sh); summary.scan_capped=true; summary.total_found > 500; files[] length ≤ 500
    - Test 13: in 510-file fixture, CLAUDE.md present in files[] (harness-first budget D-10)
    - Test 14: .git/, node_modules/, .conjure-adopt-backups/ excluded from scan

    inventory_emit_manifest:
    - Test 15: DRY_RUN=0 — adopt-manifest.json created at specified path; jq -e '.schema_version' exits 0
    - Test 16: DRY_RUN=1 — no adopt-manifest.json at output path; file written to /tmp/adopt-manifest-dryrun.json instead
    - Test 17: summary bucket counts (core, skill, agent, planning-doc, reference-doc, unknown) match files[] classifications
    - Test 18: size_cap_exceeded=true for a file over its cap limit (D-09)
    - Test 19: size_cap_violations[] populated for files with size_cap_exceeded=true
    - Test 20: restructure_steps[] is empty array at inventory time
    - Test 21: jq validates manifest against adopt-manifest.schema.json (all required fields present)
  </behavior>
  <action>
    Create lib/inventory.sh following the merge.sh sibling lib pattern:
    - No shebang; header: "lib/inventory.sh — read-only markdown scanner + 6-bucket classifier for Conjure adopt."
    - "Source this file; requires lib/mutate.sh, lib/log.sh, lib/caps.sh already sourced."
    - "POSIX bash 3.2+. No associative arrays, no mapfile, no local -n. Read-only: no mutations except manifest write."
    - Module-level state: CONJURE_INVENTORY_ITEMS="" (newline-delimited paths; populated by inventory_scan)

    Function inventory_scan <target>:
    - D-04: scan markdown only (find -name '*.md')
    - Exclusions (RESEARCH.md M-2 + PATTERNS.md Pattern 4):
      find "${target}" -name '*.md' \
        -not -path '*/.git/*' \
        -not -path '*/node_modules/*' \
        -not -path '*/.conjure-adopt-backups/*' \
        -not -path '*/.conjure-archive-*/*'
    - D-08 two-step count: first run find with same exclusions piped to wc -l to get total_found (CONJURE_INVENTORY_TOTAL_FOUND). Do this in a subshell to avoid the ARG_MAX concern; use: total_found=$(find ... | wc -l)
    - D-10 harness-first budget: implement as two separate find passes:
      Pass 1: find "${target}" -name '*.md' -path '*/CLAUDE.md' -not -path '*/.git/*' ... (root CLAUDE.md)
      Pass 2: find "${target}" -name '*.md' \( -path '*/.claude/*' -o -path '*/.planning/*' \) -not ... (harness dirs)
      Pass 3: find "${target}" -name '*.md' -not -path '*/.claude/*' -not -path '*/.planning/*' -not -path '*/CLAUDE.md' ... (all other)
      Accumulate into temp files; deduplicate; concatenate in order Pass1+Pass2+Pass3; take first 500 lines via head -500 for the actual processing list. This ensures harness files are never cut by the cap.
    - For each path in the processing list (mktemp + while IFS= read -r loop per PATTERNS.md Pattern 4):
      - Skip symlinks: test -L "${filepath}" (M-2)
      - Skip binary: LC_ALL=C grep -Pc "\x00" "${filepath}" 2>/dev/null returns > 0 → skip
      - Append to CONJURE_INVENTORY_ITEMS (newline-delimited, same pattern as CONJURE_MERGE_CONFLICT_FILES in merge.sh)
    - Set CONJURE_INVENTORY_TOTAL_FOUND and CONJURE_INVENTORY_SCAN_CAPPED variables for use by emit_manifest

    Function inventory_classify <filepath_abs> <target_abs> <claude_md_links_file>:
    - Derives rel="${filepath_abs#${target_abs}/}"
    - Skip symlinks check first: test -L "${filepath_abs}" → echo "SKIP:symlink"; return 0
    - Path-based decision tree per PATTERNS.md Pattern 6 using case statements (D-03):
      case "${rel}" in
        CLAUDE.md) echo "core"; return 0 ;;
        .claude/skills/*/SKILL.md) echo "skill"; return 0 ;;
        .claude/agents/*.md) echo "agent"; return 0 ;;
        .planning/*) echo "planning-doc"; return 0 ;;
        docs/* | README.md | CHANGELOG.md | CHANGELOG | *.adr.md | ARCHITECTURE.md | CONTRIBUTING.md)
          echo "reference-doc"; return 0 ;;
      esac
    - D-06 CLAUDE.md link check: grep -qxF "${rel}" "${claude_md_links_file}" 2>/dev/null → echo "reference-doc"; return 0
    - Default: echo "unknown"

    Function extract_claude_md_links <target_abs>:
    - Creates a mktemp file; if "${target_abs}/CLAUDE.md" exists, greps for outbound links: grep -oE '\]\([^)]+\)' "${target_abs}/CLAUDE.md" | sed 's/^](\(.*\))$/\1/' | grep -v '^http' > "${links_file}"
    - Echoes the temp file path (caller is responsible for rm -f)
    - If CLAUDE.md does not exist, produces an empty temp file

    Function emit_file_entry <rel_path> <classification> <line_count> <size_bytes> <cap_limit_or_null> <linked_from_json>:
    - Computes size_cap_exceeded: if cap_limit != "null" and line_count > cap_limit → true, else false
    - Uses jq -cn with --arg/--argjson for all fields per RESEARCH.md Code Examples (inject-safe; no shell string concat for JSON)
    - Fields per Pattern 7: path, classification, line_count, size_bytes, size_cap_exceeded, size_cap_limit, linked_from
    - Echoes the JSON object line (one per file for JSONL accumulator)

    Function inventory_emit_manifest <target_abs> <output_path>:
    - Calls inventory_scan if CONJURE_INVENTORY_ITEMS is empty (internal guard)
    - Calls extract_claude_md_links to get links_file (temp)
    - Iterates CONJURE_INVENTORY_ITEMS (mktemp + while IFS= read -r loop) to classify each and emit JSON entries to a JSONL temp file (one JSON object per line per RESEARCH.md Pitfall 6 — avoids ARG_MAX)
    - Accumulates per-bucket counts (core_count, skill_count, etc.) using integer arithmetic
    - Accumulates size_cap_violations array entries in a separate JSONL temp file
    - Determines harness_missing_layers: check if CLAUDE.md exists, .claude/skills/ exists, .claude/agents/ exists, .claude/hooks/ exists; produce array of missing layer names
    - Builds manifest using jq -cn --slurpfile files <items_jsonl> --slurpfile violations <violations_jsonl> with all scalar fields passed via --arg/--argjson. Top-level keys exactly matching Pattern 7: schema_version "1", generated_at (ISO UTC), conjure_version (from VERSION file at CONJURE_HOME/VERSION or "unknown"), target (absolute), snapshot_path "" (empty — filled by adopt.sh in Phase 22), summary object, files array, size_cap_violations array, harness_missing_layers array, restructure_steps []
    - DRY_RUN path: if DRY_RUN=1, use /tmp/adopt-manifest-dryrun.json as output_path (not the caller-supplied path); write via mutate_write
    - Live path: write to output_path via mutate_write (mutate_write handles DRY_RUN internally; but since we've already switched to /tmp path above, the write always happens — both dry and live produce a file; the difference is WHERE it goes)
    - Call log_step INVENTORY "scanned ${CONJURE_INVENTORY_TOTAL_FOUND:-?} files → ${output_path}"
    - Print summary: echo "Inventoried ${total_files} files (${core_count} core, ${skill_count} skill, ...)"
    - If scan was capped: print "N found, scanned 500 — rerun with --full-inventory" (D-08)
    - rm -f all temp files (links_file, items_jsonl, violations_jsonl)

    Size cap limit by bucket: core (CLAUDE.md) = CLAUDE_MD_CAP, skill = SKILL_MD_CAP, agent = AGENT_MD_CAP, planning-doc = null, reference-doc = null, unknown = null.

    All loops must use mktemp + while IFS= read -r pattern (no process substitution — bash 3.2). No echo -e/-n; use printf. Must pass shellcheck.
  </action>
  <verify>
    <automated>
      shellcheck lib/inventory.sh
      bash -c '
        source lib/mutate.sh; source lib/log.sh; source lib/caps.sh; source lib/inventory.sh
        OUT="$(mktemp -d)"
        DRY_RUN=0 CONJURE_SNAPSHOT_PATH="" RESTRUCTURE_LOG_PATH="" \
          inventory_emit_manifest "$(pwd)/tests/fixtures/brownfield-simple" "${OUT}/adopt-manifest.json"
        echo "manifest_exists=$([ -f "${OUT}/adopt-manifest.json" ] && echo yes || echo no)"
        jq -e ".schema_version" "${OUT}/adopt-manifest.json" >/dev/null && echo "schema_version ok"
        jq -e ".summary.scan_capped == false" "${OUT}/adopt-manifest.json" >/dev/null && echo "scan_capped ok"
        jq -r ".summary | to_entries[] | \"\(.key)=\(.value)\"" "${OUT}/adopt-manifest.json"
        rm -rf "$OUT"
      '
      bash tests/run.sh 2>&1 | grep -E "Phase 21.*INV-0[1-4]\|Phase 21.*classify\|Phase 21.*manifest" | head -15
    </automated>
  </verify>
  <acceptance_criteria>
    - shellcheck lib/inventory.sh exits 0
    - inventory_classify "CLAUDE.md" → "core"; ".claude/skills/git/SKILL.md" → "skill"; ".claude/agents/deploy.md" → "agent"; ".planning/21-PLAN.md" → "planning-doc"; "docs/README.md" → "reference-doc"; arbitrary .md outside harness → "unknown" (D-03)
    - symlink-target.md in brownfield-simple skipped (SKIP:symlink); does NOT appear in files[]
    - inventory_emit_manifest on brownfield-simple produces adopt-manifest.json; jq -e '.schema_version' exits 0
    - jq -e '.summary.core == 1' adopt-manifest.json exits 0 (one CLAUDE.md)
    - 510-file fixture: jq -e '.summary.scan_capped == true' and '.summary.total_found > 500' and '(.files | length) <= 500' all exit 0
    - CLAUDE.md present in files[] even in capped scan (harness-first D-10)
    - DRY_RUN=1: manifest written to /tmp/adopt-manifest-dryrun.json; not to the caller-supplied output path
    - size_cap_exceeded=true for a CLAUDE.md > 100 lines; size_cap_violations[] non-empty
    - restructure_steps[] is empty array: jq -e '.restructure_steps == []' exits 0
    - bash tests/run.sh 2>&1 | grep "Phase 21.*INV-01" shows at least 5 pass lines (all 6 bucket classifications)
    - CR-7 perf: inventory_emit_manifest on 510-file fixture completes in < 30 seconds
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| find output → inventory_classify | File paths from find are repo-local; passed as variables never eval'd |
| CLAUDE.md content → link extraction | grep -oE on CLAUDE.md content; output used in grep -qxF comparison, never eval |
| file paths → jq --arg | All path values pass through jq --arg (injection-safe) |
| wc -l output → arithmetic | Stripped with tr -d ' '; used in integer comparison only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-07 | Tampering | path traversal in manifest paths | mitigate | All paths in files[] are relative paths (rel = filepath_abs stripped of target_abs prefix); absolute paths never stored in files[]; find scoped to ${target}; never eval path strings |
| T-21-08 | Tampering | shell injection via filenames with special chars | mitigate | find -print0 not needed here (outputs to temp file, read via while IFS= read -r); always quote "${filepath}" in test -L, wc -l, grep calls; jq --arg for JSON encoding of paths |
| T-21-09 | Tampering | inventory writes to wrong location (DRY_RUN bypass) | mitigate | inventory_emit_manifest uses mutate_write which internally guards DRY_RUN; DRY_RUN=1 redirects to /tmp path before calling mutate_write |
| T-21-10 | Denial | 500-file cap bypassed by symlink farm | accept | Symlinks skipped by test -L before counting toward budget; total_found is raw find count, not post-filter count; no security boundary crossed (local filesystem) |
| T-21-11 | Tampering | snapshot cp -a writes to wrong backup_root | mitigate | snapshot_create derives snap_dir from backup_root + "conjure-adopt-" + ts; backup_root is caller-supplied and documented as .conjure-adopt-backups/; never user-typed string in hot path |
| T-21-SC | Tampering | npm/pip/cargo installs | accept | Plan 03 installs zero packages |
</threat_model>

<verification>
After both tasks complete:
  shellcheck lib/snapshot.sh lib/inventory.sh
  bash -c 'source lib/mutate.sh; source lib/log.sh; source lib/caps.sh; source lib/inventory.sh
           OUT=$(mktemp -d)
           DRY_RUN=0 RESTRUCTURE_LOG_PATH="" CONJURE_SNAPSHOT_PATH="" \
             inventory_emit_manifest tests/fixtures/brownfield-simple "$OUT/m.json"
           jq ".summary" "$OUT/m.json"; rm -rf "$OUT"'
  bash tests/run.sh 2>&1 | grep -E "Phase 21" | grep "✓" | wc -l
  bash tests/run.sh 2>&1 | grep "✗" | grep -v "Phase 21" | wc -l
</verification>

<success_criteria>
- lib/snapshot.sh exists; snapshot_create uses raw cp -a; DRY_RUN honored; CONJURE_SNAPSHOT_PATH set; log_step SNAPSHOT called
- lib/inventory.sh exists; inventory_classify routes all 6 buckets correctly; symlinks skipped; harness-first budget enforced
- inventory_emit_manifest produces adopt-manifest.json with all required keys; size_cap_exceeded correct; scan_capped correct
- Both files pass shellcheck
- bash tests/run.sh Phase 21 SC-2, INV-01..INV-04 sections show passing assertions
- CR-7 perf gate: 510-file fixture inventory completes in < 30s
- Zero pre-existing test regressions
</success_criteria>

<output>
Create .planning/phases/21-foundation-libs-inventory/21-03-SUMMARY.md when done
</output>
