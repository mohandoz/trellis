---
phase: 21-foundation-libs-inventory
plan: 04
type: execute
wave: 3
depends_on:
  - 21-02
  - 21-03
files_modified:
  - scripts/audit-setup.sh
autonomous: true
requirements:
  - ADOPT-03

must_haves:
  truths:
    - "scripts/audit-setup.sh sources lib/caps.sh and uses $CLAUDE_MD_CAP/$SKILL_MD_CAP/$AGENT_MD_CAP instead of the hardcoded literals 100/200/80"
    - "audit-setup.sh behavior is unchanged: same exit codes, same output messages for same inputs"
    - "tests/run.sh Phase 21 SC-5 audit-setup.sh test passes (grep -c CLAUDE_MD_CAP scripts/audit-setup.sh returns > 0)"
    - "bash tests/run.sh completes in < 30 seconds (CR-7 perf gate)"
    - "All Phase 21 test block assertions pass with all four libs in place"
  artifacts:
    - path: "scripts/audit-setup.sh"
      provides: "sources lib/caps.sh; uses cap variables instead of literals"
      contains: "CLAUDE_MD_CAP"
  key_links:
    - from: "scripts/audit-setup.sh"
      to: "lib/caps.sh"
      via: "source CONJURE_HOME/lib/caps.sh near top of file; CONJURE_HOME resolved before source line"
      pattern: "source.*caps\\.sh"
---

<objective>
Final plan: the audit-setup.sh call-site change (extract hardcoded 100/200/80 literals to
use the cap variables from lib/caps.sh, per D-11 and RESEARCH.md §State of the Art).

This plan also serves as the integration gate: with all four libs now in place, any
stub tests in the Phase 21 block that were failing due to missing libraries should now
pass. The ADOPT-03 audit-setup smoke test (SC-5) is verified here.

Purpose: Prevents cap drift — a single change to CLAUDE_MD_CAP in lib/caps.sh now
propagates to audit-setup.sh, adopt.sh (Phase 22), and inventory.sh without
inconsistencies. The call-site change is purely mechanical: no behavior change.

Output:
- scripts/audit-setup.sh (modified — call-site change only; behavior unchanged)
</objective>

<execution_context>
@/Users/mohandoz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mohandoz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/21-foundation-libs-inventory/21-CONTEXT.md
@.planning/phases/21-foundation-libs-inventory/21-RESEARCH.md
@.planning/phases/21-foundation-libs-inventory/21-PATTERNS.md
@scripts/audit-setup.sh
@lib/caps.sh
@tests/run.sh
@.planning/phases/21-foundation-libs-inventory/21-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract cap literals from audit-setup.sh to use lib/caps.sh (per D-11, ADOPT-03/SC-5)</name>
  <files>scripts/audit-setup.sh</files>
  <read_first>
    - scripts/audit-setup.sh lines 1-30 — top of file structure: set -uo pipefail, TARGET resolution, CONJURE_HOME lazy-set pattern at lines 179-180 using ": ${CONJURE_HOME:=...}"
    - scripts/audit-setup.sh lines 23-30 — CLAUDE.md cap check: line 26 uses literal 100 and 200
    - scripts/audit-setup.sh lines 47-68 — Skills section: line 54 uses literal 200
    - scripts/audit-setup.sh lines 70-82 — Agents section: line 78 uses literal 80
    - .planning/phases/21-foundation-libs-inventory/21-PATTERNS.md "audit-setup.sh call-site change" section — exact before/after showing source line placement and CONJURE_HOME resolution guard
    - .planning/phases/21-foundation-libs-inventory/21-RESEARCH.md §Code Examples "audit-setup.sh call-site change" — the BEFORE/AFTER code excerpt
    - .planning/phases/21-foundation-libs-inventory/21-PATTERNS.md Critical Implementation Notes "CONJURE_HOME resolution in audit-setup.sh" — CONJURE_HOME must be set before the source call; add ': "${CONJURE_HOME:="$(cd "$(dirname "$0")/.." && pwd)"}"' immediately before the source line
    - lib/caps.sh — verify the exported variable names: CLAUDE_MD_CAP, SKILL_MD_CAP, AGENT_MD_CAP
  </read_first>
  <action>
    Make three targeted changes to scripts/audit-setup.sh; do NOT change any other code or behavior:

    CHANGE 1 — Add CONJURE_HOME resolution + source lib/caps.sh near top of file.
    Insert immediately after the "set -uo pipefail" line (line 6) and before the TARGET and cd lines:

      : "${CONJURE_HOME:="$(cd "$(dirname "$0")/.." && pwd)"}"
      # shellcheck source=lib/caps.sh
      source "${CONJURE_HOME}/lib/caps.sh"

    This ensures CONJURE_HOME is set before caps.sh is sourced, regardless of how
    audit-setup.sh is called. The : "${CONJURE_HOME:=...}" pattern is already used
    later in the file (lines 179, 247) — move it to the top so caps.sh can source.

    CHANGE 2 — Replace three literal cap values with cap variables:
    - Line 26: change '100' to "${CLAUDE_MD_CAP}" and '200' to "${SKILL_MD_CAP}" in the
      CLAUDE.md line-count check:
        if [ "$LINES" -le "${CLAUDE_MD_CAP}" ]; then ok ...
        elif [ "$LINES" -le "${SKILL_MD_CAP}" ]; then warn ...   (the hard cap comparison)
        else err "CLAUDE.md: $LINES lines (HARD CAP exceeded — trim)"
    - Line 54: change '>200' to '> "${SKILL_MD_CAP}"' in the skill size check
    - Line 78: change '>80' to '> "${AGENT_MD_CAP}"' in the agent size check

    CHANGE 3 — If the existing CONJURE_HOME lazy-set at lines 179-180 and 247 now duplicates
    the one added at the top, the later ones become harmless no-ops (the := guard means
    they only set if CONJURE_HOME is unset, which it won't be after the top-of-file line).
    Leave the later ones in place — they are belt-and-suspenders and do no harm.

    No other changes. No behavior changes. Exit codes (0/1/2) remain identical.
    Must pass shellcheck after the edit.
  </action>
  <verify>
    <automated>
      shellcheck scripts/audit-setup.sh
      grep -v '^#' scripts/audit-setup.sh | grep -c 'CLAUDE_MD_CAP'
      grep -v '^#' scripts/audit-setup.sh | grep -c '\[ "\$LINES" -le 100'
      bash scripts/audit-setup.sh tests/fixtures/brownfield-simple 2>&1 | tail -5; echo "rc=$?"
      bash tests/run.sh 2>&1 | grep -E "audit.*caps|SC-5.*audit|CLAUDE_MD_CAP.*audit" | head -5
    </automated>
  </verify>
  <acceptance_criteria>
    - shellcheck scripts/audit-setup.sh exits 0 (no new errors introduced)
    - grep -v '^#' scripts/audit-setup.sh | grep -c 'CLAUDE_MD_CAP' returns value >= 1 (variable used in non-comment code)
    - grep -v '^#' scripts/audit-setup.sh | grep -c '\[ "\$LINES" -le 100' returns 0 (literal 100 removed from cap comparison)
    - grep -v '^#' scripts/audit-setup.sh | grep -c '\[ "\$LINES" -gt 200' returns 0 (literal 200 removed from skill cap comparison, per D-11)
    - grep -v '^#' scripts/audit-setup.sh | grep -c '\[ "\$LINES" -gt 80' returns 0 (literal 80 removed)
    - bash scripts/audit-setup.sh tests/fixtures/brownfield-simple exits 0 (behavior unchanged; fixture still passes)
    - bash tests/run.sh exits with rc <= 1 (no new failures; WARN is acceptable since conjure kit has no CLAUDE.md)
    - bash tests/run.sh 2>&1 | grep "audit-setup.sh ran" contains "✓" (audit self-test still passes)
    - bash tests/run.sh 2>&1 | grep "Phase 21.*SC-5.*audit" shows "✓" pass for the audit-setup.sh cap variable check
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    All four Phase 21 libs complete: lib/caps.sh, lib/log.sh, lib/mutate.sh (mutate_archive),
    lib/snapshot.sh, lib/inventory.sh. adopt-manifest.schema.json finalized. scripts/audit-setup.sh
    updated. brownfield-simple fixture present. Phase 21 test block complete.
  </what-built>
  <how-to-verify>
    Run the full test suite and check all Phase 21 assertions pass:

    1. Full suite (should complete in < 30 seconds — CR-7):
       time bash tests/run.sh 2>&1 | tail -20

    2. Phase 21 assertion summary:
       bash tests/run.sh 2>&1 | grep "Phase 21"

    3. No pre-existing regressions:
       bash tests/run.sh 2>&1 | grep "✗" | grep -v "Phase 21"

    4. Schema validation:
       jq empty adopt-manifest.schema.json && echo "schema JSON valid"

    5. Manual end-to-end smoke (optional but recommended):
       bash -c '
         source lib/mutate.sh
         source lib/log.sh
         source lib/caps.sh
         source lib/inventory.sh
         OUT="$(mktemp -d)"
         DRY_RUN=0 RESTRUCTURE_LOG_PATH="" CONJURE_SNAPSHOT_PATH="" \
           inventory_emit_manifest "$(pwd)/tests/fixtures/brownfield-simple" "${OUT}/adopt-manifest.json"
         jq ".summary" "${OUT}/adopt-manifest.json"
         rm -rf "$OUT"
       '

    Expected results:
    - time bash tests/run.sh completes in < 30 seconds wall time
    - All Phase 21 test sections show passing assertions (✓) for SC-5, SC-1, SC-2, INV-01..INV-04, SAFE-03, SC-4, CR-7
    - grep "✗" | grep -v "Phase 21" returns empty (no regressions)
    - jq .summary on brownfield-simple manifest shows core=1, at least one skill and agent
  </how-to-verify>
  <resume-signal>Type "approved" if all Phase 21 tests pass, or describe which assertions are failing</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CONJURE_HOME path → source | CONJURE_HOME resolved from dirname $0 (script location); not user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-12 | Tampering | CONJURE_HOME path injection via env var | mitigate | ": ${CONJURE_HOME:=...}" only sets if unset; the :=fallback uses dirname "$0" which is script-relative, not user-controllable; sourced file (caps.sh) is known-good codebase file |
| T-21-13 | Tampering | audit-setup.sh behavior change (cap values change) | accept | Call-site change only — CLAUDE_MD_CAP=100/SKILL_MD_CAP=200/AGENT_MD_CAP=80 in lib/caps.sh are identical to the literals they replace; behavior is identical; regression tested by fixture audit loop |
| T-21-SC | Tampering | npm/pip/cargo installs | accept | Plan 04 installs zero packages |
</threat_model>

<verification>
Final phase gate (run after human-verify checkpoint approves):
  time bash tests/run.sh 2>&1 | tail -5
  bash tests/run.sh 2>&1 | grep "✗" | grep -v "Phase 21" | wc -l
  bash tests/run.sh 2>&1 | grep "Phase 21" | grep "✓" | wc -l
  jq '.properties.files.items.properties.classification.enum' adopt-manifest.schema.json
  shellcheck lib/caps.sh lib/log.sh lib/mutate.sh lib/snapshot.sh lib/inventory.sh scripts/audit-setup.sh
</verification>

<success_criteria>
- scripts/audit-setup.sh sources lib/caps.sh and uses $CLAUDE_MD_CAP/$SKILL_MD_CAP/$AGENT_MD_CAP; no literal 100/200/80 in cap comparisons
- audit-setup.sh behavior unchanged: brownfield-simple fixture still exits 0; self-test still passes
- shellcheck passes on all modified files
- bash tests/run.sh Phase 21 block: all assertions pass for SC-5, SC-1, SC-2, INV-01..INV-04, SAFE-03, SC-4, CR-7
- Full suite completes in < 30 seconds (CR-7)
- Zero pre-existing test regressions
</success_criteria>

<output>
Create .planning/phases/21-foundation-libs-inventory/21-04-SUMMARY.md when done
</output>
