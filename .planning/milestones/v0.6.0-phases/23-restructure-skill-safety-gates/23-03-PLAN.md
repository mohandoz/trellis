---
phase: 23-restructure-skill-safety-gates
plan: 03
type: execute
wave: 2
depends_on: ["23-01", "23-02"]
files_modified:
  - templates/skills/restructure/SKILL.md
  - templates/skills/restructure/gates/approve.sh
  - scripts/init-project.sh
autonomous: true
requirements: [RESTR-01, RESTR-02, RESTR-03]
nyquist_compliant: true

must_haves:
  truths:
    - "After conjure adopt, .claude/skills/restructure/SKILL.md is present in the target, its frontmatter declares allowed-tools: [Read, Bash], and the file is ≤200 lines (criterion 1)"
    - "The whole skill dir (SKILL.md + gates/*.sh) lands in the target via the init-project.sh scaffold loop (criterion 1)"
    - "The skill orchestration narrates: read manifest → extract invariants → propose via conjure adopt --update-manifest (stdin) → run GATE A + GATE B before approval → per-class /dev/tty approve/skip/edit → conjure adopt --apply-step on approve → archive steps LAST with decision-scan individual confirm"
    - "The approval driver exits 2 on a non-TTY stdin (never auto-approves, D-12) and logs ONE summary line per bulk bucket (D-09)"
    - "The skill never calls Write or Edit on a project file — every mutation routes through conjure adopt (RESTR-02 chokepoint)"
  artifacts:
    - path: "templates/skills/restructure/SKILL.md"
      provides: "the human-gated restructure skill runbook (orchestration prose only, ≤200 lines, D-01/16)"
      min_lines: 40
      contains: "allowed-tools: [Read, Bash]"
    - path: "templates/skills/restructure/gates/approve.sh"
      provides: "per-class /dev/tty approve/skip/edit driver with non-TTY exit-2 guard + bulk summary log (D-09/10/12)"
      contains: "exit 2"
    - path: "scripts/init-project.sh"
      provides: "scaffolds restructure into target .claude/skills/ during conjure adopt (D-16)"
      contains: "restructure"
  key_links:
    - from: "scripts/init-project.sh"
      to: "templates/skills/restructure/"
      via: "the line-59 tooling-skills scaffold loop (mutate_cp whole dir → gates/ land too)"
      pattern: "restructure"
    - from: "templates/skills/restructure/SKILL.md"
      to: "conjure adopt --update-manifest / --apply-step"
      via: "Bash invocation of the Phase 22 seam (stdin op JSON), never Write/Edit"
      pattern: "conjure adopt"
    - from: "templates/skills/restructure/gates/approve.sh"
      to: "/dev/tty"
      via: "read -r ... < /dev/tty with [ -t 0 ] non-TTY exit-2 guard"
      pattern: "/dev/tty"
---

<objective>
Ship the skill itself: a thin `templates/skills/restructure/SKILL.md` (≤200 lines, orchestration prose only, frontmatter `allowed-tools: [Read, Bash]`), the per-class `approve/skip/edit` `/dev/tty` approval driver (`gates/approve.sh`), and the one-token `scripts/init-project.sh` scaffold-loop edit that makes the skill (and its gates) land in any target during `conjure adopt`. This turns the Wave 0 scaffold/criterion-1 + non-TTY-exit-2 + group-by + archive-last assertions green.

The skill is the PROPOSER half of the v0.6.0 split-responsibility model. It reads `adopt-manifest.json` + the oversized CLAUDE.md (Read), proposes ops + staged content via `conjure adopt --update-manifest` (stdin — Pitfall 2: the env var is NOT forwarded), runs GATE A (verify-invariants) + GATE B (audit-staged) BEFORE the user sees a proposal (D-14), presents per-class grouped approvals over `/dev/tty` (D-09), and applies on approve via `conjure adopt --apply-step` (the RESTR-02 chokepoint through lib/mutate.sh). Archive steps are sequenced LAST (D-15) with decision-scan individual confirmation (D-11). All logic lives in `gates/*.sh`; SKILL.md narrates the runbook (D-01, Pitfall 4 — keeps it ≤200 lines).

Purpose: deliver the installed, operational, human-gated restructure skill (RESTR-01) with the Read+Bash chokepoint (RESTR-02) and per-step grouped approval (RESTR-03), riding the shipped Phase 22 seam.
Output: SKILL.md + approve.sh + the init-project.sh scaffold edit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-restructure-skill-safety-gates/23-CONTEXT.md
@.planning/phases/23-restructure-skill-safety-gates/23-RESEARCH.md
@.planning/phases/23-restructure-skill-safety-gates/23-PATTERNS.md
@.planning/phases/23-restructure-skill-safety-gates/23-VALIDATION.md

# Wave 0/1 contracts this plan consumes:
@.planning/phases/23-restructure-skill-safety-gates/23-01-SUMMARY.md
@.planning/phases/23-restructure-skill-safety-gates/23-02-SUMMARY.md

# The shipped seam + the scaffold edit point + the SKILL.md template format + the /dev/tty model:
@.planning/phases/22-conjure-adopt-cli-core-rollback/22-03-SUMMARY.md
@scripts/init-project.sh
@templates/skills/_anatomy/SKILL.md
@templates/skills/release/SKILL.md
@scripts/resolve.sh
@tests/fixtures/_adopt-restructure-steps/adopt-manifest.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: approve.sh — per-class /dev/tty approve/skip/edit driver (D-09/10/12, O-3) + init-project.sh scaffold edit (D-16)</name>
  <files>templates/skills/restructure/gates/approve.sh, scripts/init-project.sh</files>
  <read_first>
    - scripts/resolve.sh:32-82 (the EXACT non-TTY guard `if ! { [ -t 0 ] || [ "${CONJURE_FORCE_INTERACTIVE:-0}" = "1" ]; }; then ... exit 2; fi`; the fd-3 walk `exec 3< "$tmpfile"` so the inner `read < /dev/tty` keeps reading; the verb-loop with re-prompt-on-unknown and re-prompt-on-edit; NOTE: do NOT copy resolve.sh:68's `${EDITOR:-vi}` — O-3 says edit re-drafts via the skill, not $EDITOR).
    - .planning/phases/23-restructure-skill-safety-gates/23-PATTERNS.md (lines 299-330 the interactive approval shared pattern + the O-3 edit mechanics: edit = skill re-drafts via --update-manifest + re-runs gates, NO $EDITOR; lines 230-256 the init-project.sh:59 one-token scaffold edit + A1 RESOLVED mutate_cp cp -r ships gates/).
    - scripts/init-project.sh:58-64 (the tooling-skills loop — the ONLY line to change: add `restructure` to the `for skill in ...` list at line 59).
    - .planning/phases/23-restructure-skill-safety-gates/23-RESEARCH.md (lines 237-258 Pattern 4 per-class grouped approval over the 6-bucket classification; lines 330-341 the group-by jq; lines 304-308 Pitfall 2 stdin-not-env for --update-manifest).
    - lib/log.sh (`log_step <phase> <message>` — the DRY_RUN-safe summary-line logger for bulk ops, D-09/SAFE-07; never `echo >>`).
    - adopt-manifest.schema.json + tests/fixtures/_adopt-restructure-steps/adopt-manifest.json (the `classification` 6-bucket enum core/skill/agent/planning-doc/reference-doc/unknown + restructure_steps[] shape the driver groups over).
    - tests/run.sh (the Phase 23 Wave 0 non-TTY exit-2 assert + the bulk-summary-log assert this driver must satisfy).
  </read_first>
  <action>
    Create `templates/skills/restructure/gates/approve.sh`: the per-class grouped approval driver the skill invokes via Bash. Header + `set -uo pipefail` + usage/exit-code comment (0 = all buckets processed, 2 = non-TTY / hard fail). Resolve `CONJURE_HOME` and source `lib/log.sh` with a `# shellcheck source=/dev/null` directive (for `log_step`). Take the manifest path as `$1` (default `adopt-manifest.json`) and the target as `$2` (default `pwd`).

    Non-TTY guard FIRST (D-12, copy resolve.sh:32-37 in spirit): `if ! { [ -t 0 ] || [ "${CONJURE_FORCE_INTERACTIVE:-0}" = "1" ]; }; then echo "restructure: stdin is not a TTY — interactive approval required" >&2; exit 2; fi`. NEVER auto-approve.

    Group-by + per-class prompt (D-09, RESEARCH lines 330-341): iterate the 6 buckets in NON-archive order `core skill agent planning-doc reference-doc unknown`, emitting a per-bucket prompt only for non-empty buckets (`count="$(jq -r --arg b "$bucket" '[.files[]|select(.classification==$b)]|length' "$MANIFEST")"; [ "${count:-0}" -gt 0 ] || continue`). For each non-empty bucket: print the bucket summary (count + up to 5 sample paths via a jq + `head -5`), then a verb loop reading from `/dev/tty`: `read -r -p "  [a]pprove / [s]kip / [e]dit: " choice < /dev/tty`. Cases (mirror adopt.sh:367-380 + resolve.sh:52-80): `a|approve)` → for each proposed step id in the bucket, `conjure adopt --apply-step "$id"` (routes through mutate.sh, RESTR-02), then `log_step RESTRUCTURE "approved $bucket bucket — applied N steps"` (ONE summary line for the whole bucket, D-09), `break`; `s|skip)` → `log_step RESTRUCTURE "skipped $bucket bucket"`, `break`; `e|edit)` → the O-3 mechanic: print the staging path + a message that the skill should re-draft this bucket's staged content and re-run gates, then signal the skill to re-draft via --update-manifest and RE-RUN GATE A + GATE B before re-prompting (NO `$EDITOR`; NO `break` so it re-prompts after the re-draft); `*)` → `echo "  enter a, s, or e"` (NO break — no default, D-14). If iterating a tmpfile of bucket/step ids, use the fd-3 pattern (`exec 3< "$tmpfile"` ... `<&3`) so the inner `/dev/tty` read is not consumed (resolve.sh:41-42,82).

    Archive ordering: the driver processes archive ops LAST (D-15); for each archive candidate the skill runs `gates/decision-scan.sh` first — an `individual` signal pulls that file OUT of the bulk bucket into a single `/dev/tty` confirm (D-11). Keep the driver's responsibility to the prompt loop + apply + log; the SKILL.md narrates calling decision-scan and passing the resulting individual-vs-bulk lists.

    Edit `scripts/init-project.sh` line 59 (D-16, the ONLY change to this file): add `restructure` to the tooling-skills `for skill in ...` list, before `_anatomy` (PATTERNS.md line 238): `for skill in code-graph docs-lookup web-research ast-search repo-pack sql-explorer restructure _anatomy; do`. `mutate_cp` at line 61 copies the whole dir recursively (cp -r, lib/mutate.sh:42-46) so `SKILL.md` + `gates/*.sh` all land (A1 RESOLVED). adopt.sh:734 already runs `init-project.sh existing "$TARGET"`, so the skill scaffolds during `conjure adopt` — no second copy step.

    approve.sh: bash 3.2-safe (no mapfile/assoc arrays); `exit 2` never `exit 1`; build any op JSON injection-safe with `jq -n --arg` (never string-interpolate into JSON — PATTERNS.md line 351).
  </action>
  <acceptance_criteria>
    - `bash templates/skills/restructure/gates/approve.sh tests/fixtures/_adopt-restructure-steps/adopt-manifest.json < /dev/null; echo "rc=$?"` → `rc=2` (non-TTY → exit 2, D-12) with a "stdin is not a TTY" message on stderr.
    - `grep -n 'restructure' scripts/init-project.sh` → the line-59 tooling-skills loop now includes `restructure` (before `_anatomy`).
    - `shellcheck -S error -e SC2164,SC2044,SC2034,SC2155 templates/skills/restructure/gates/approve.sh scripts/init-project.sh` → clean.
    - `grep -v '^#' templates/skills/restructure/gates/approve.sh | grep -c 'exit 1'` → 0.
    - `grep -c 'EDITOR' templates/skills/restructure/gates/approve.sh` → 0 (O-3: no $EDITOR launch; edit re-drafts via the skill).
    - The Wave 0 non-TTY-exit-2 assertion passes: the Phase 23 lines in `bash tests/run.sh` matching the approval/non-TTY assert show no ✗.
  </acceptance_criteria>
  <verify>
    <automated>shellcheck -S error -e SC2164,SC2044,SC2034,SC2155 templates/skills/restructure/gates/approve.sh scripts/init-project.sh && [ "$(grep -v '^#' templates/skills/restructure/gates/approve.sh | grep -c 'exit 1')" -eq 0 ] && [ "$(grep -c 'EDITOR' templates/skills/restructure/gates/approve.sh)" -eq 0 ] && grep -q 'restructure' scripts/init-project.sh && { bash templates/skills/restructure/gates/approve.sh tests/fixtures/_adopt-restructure-steps/adopt-manifest.json < /dev/null; [ $? -eq 2 ]; } && echo OK</automated>
  </verify>
  <done>approve.sh exits 2 on non-TTY stdin (never auto-approves), presents per-class grouped prompts, applies via conjure adopt --apply-step, logs one bulk summary line per bucket, uses NO $EDITOR (O-3 re-draft model), is shellcheck-clean with zero `exit 1`; init-project.sh line 59 now scaffolds `restructure` (whole dir incl. gates/) during conjure adopt.</done>
</task>

<task type="auto">
  <name>Task 2: SKILL.md — the thin restructure runbook (≤200 lines, allowed-tools: [Read, Bash], D-01/04/14/15/16)</name>
  <files>templates/skills/restructure/SKILL.md</files>
  <read_first>
    - templates/skills/_anatomy/SKILL.md (the authoring rulebook: ≤200 lines line 36, tables over prose line 38, cite file:line line 39, include forbidden actions line 40, subfolders for attached scripts lines 94-96; the frontmatter form lines 10-17 incl. the valid YAML-list `allowed-tools`).
    - templates/skills/release/SKILL.md (a concrete 62-line skill to mirror for structure/length; frontmatter name+description; cross-references at the tail).
    - .planning/phases/23-restructure-skill-safety-gates/23-PATTERNS.md (lines 35-76 the SKILL.md pattern: frontmatter D-16 difference — this is the FIRST skill to declare allowed-tools; body = orchestration prose only, no bash loops; lines 70-76 what the body MUST be).
    - .planning/phases/23-restructure-skill-safety-gates/23-RESEARCH.md (lines 121-159 the system-architecture diagram = the exact flow the SKILL.md narrates; lines 176-192 Pattern 1 Read+Bash chokepoint incl. the stdin op-JSON example + the staging-file-write-via-Bash caveat; lines 260-268 the anti-patterns to forbid in the body; lines 316-320 Pitfall 4 keep it thin).
    - .planning/phases/23-restructure-skill-safety-gates/23-CONTEXT.md (D-01..D-16 — the locked behavior the runbook must describe; D-04 stdin seam, D-10 verb semantics, D-14 gate order, D-15 archive-last).
    - .planning/phases/23-restructure-skill-safety-gates/23-02-SUMMARY.md (the exact gate-helper names + arg signatures + exit-code contracts the SKILL.md tells the user to invoke).
    - tests/fixtures/_adopt-restructure-steps/adopt-manifest.json (the manifest shape the skill reads: files[].classification, size_cap_exceeded, restructure_steps[]).
  </read_first>
  <action>
    Create `templates/skills/restructure/SKILL.md` as orchestration prose ONLY (D-01 — NO bash loops/normalization; logic lives in gates/*.sh). Keep it ≤200 lines (criterion 1 / Pitfall 4) — target ~120-160 lines, using tables for the gate catalog and the verb semantics (per _anatomy). Do NOT put fenced bash implementation in the body; describe invocations as prose ("run `gates/verify-invariants.sh <staging> <INVARIANTS.txt>`; if it exits non-zero, STOP and show the user the missing list").

    Frontmatter (D-16, PATTERNS.md lines 48-57): `name: restructure`; a Claude-matchable `description:` with concrete trigger phrases (e.g. "Restructure an oversized CLAUDE.md + doc sprawl into a ≤100-line core + extracted skills/refs/archive. Invoke when the user asks to restructure, condense, or adopt a brownfield harness."); `allowed-tools: [Read, Bash]` (the chokepoint — physically excludes Write/Edit on project files, RESTR-02). Optionally `disallowed-tools: [Write, Edit]` (belt-and-suspenders, discretionary).

    Body sections (narrate the RESEARCH lines 121-159 flow):
      1. What this skill is for + the hard rule: NEVER call Write/Edit on a project file; every mutation routes through `conjure adopt --apply-step` (RESTR-02). State that staged CONTENT is written to `.conjure-adopt-state/staging/<file>` via a Bash redirect (transient adopt-state, not a project file — D-04, Pattern 1 caveat), and the proposed OP is registered via `printf '%s' "$op" | conjure adopt --update-manifest` (STDIN, Pitfall 2 — the env var is NOT forwarded through the CLI).
      2. Inputs table: `adopt-manifest.json` (summary + files[] with classification + size_cap_exceeded), the oversized CLAUDE.md, flagged docs (read via Read).
      3. Step sequence: (a) read manifest; (b) `gates/extract-invariants.sh` → candidates → LLM confirms into `.conjure-adopt-state/INVARIANTS.txt` (D-03/05/06, canonical tokens O-2); (c) draft condensed CLAUDE.md / extracted skills / archive list; stage + `--update-manifest` each op; (d) PRE-APPROVAL GATES (D-14, BOTH before the user sees the proposal): GATE A `gates/verify-invariants.sh` (non-zero → STOP, show missing invariants, criterion 4) + GATE B `gates/audit-staged.sh` (non-zero → STOP, show audit output, criterion 5); (e) per-class approval via `gates/approve.sh` (D-09/10/12 — approve/skip/edit over /dev/tty, non-TTY → exit 2); (f) ARCHIVE steps LAST (D-15) — run `gates/decision-scan.sh` on each candidate; `individual` → single confirm (D-11), else bulk per-class.
      4. A gate catalog table: helper | purpose | block condition | criterion.
      5. A verb-semantics table: approve (apply via --apply-step) | skip (leave as-is) | edit (re-draft + re-run gates, NO $EDITOR — O-3).
      6. Forbidden actions (per _anatomy line 40 + RESEARCH anti-patterns lines 260-268): never Write/Edit a project file; never pass op JSON via the env var (stdin only); never audit/verify AFTER apply (gates run on staging, before approval); never one-prompt-per-file at scale (per-class only); never archive before write/extract; never `exit 1` in a helper.
      7. Cross-reference tail (mirror release/SKILL.md): point to `gates/*.sh` and the Phase 22 `conjure adopt` seam.
    Cite file:line for factual claims (e.g. the seam at scripts/adopt.sh; the buckets at adopt-manifest.schema.json). Ensure `name:` and `description:` are present (audit-setup.sh:61/64) and the description is >30 chars (audit-setup.sh:66 warn).
  </action>
  <acceptance_criteria>
    - `templates/skills/restructure/SKILL.md` exists; `[ "$(wc -l < templates/skills/restructure/SKILL.md)" -le 200 ]` → true (criterion 1).
    - `head -10 templates/skills/restructure/SKILL.md | grep -q '^name:'` AND `... | grep -q '^description:'` → both match (audit frontmatter gate).
    - `grep -q 'allowed-tools:.*Read.*Bash' templates/skills/restructure/SKILL.md` → match (RESTR-02 chokepoint declared).
    - The body contains NO fenced bash logic blocks implementing loops/normalization (D-01): `grep -c '```bash' templates/skills/restructure/SKILL.md` is 0 OR any fenced block is a single illustrative invocation line, not a loop (manual review per _anatomy line 42).
    - The body references each gate helper by name: `grep -qE 'verify-invariants|audit-staged|decision-scan|extract-invariants|approve' templates/skills/restructure/SKILL.md` → match.
    - The body references the seam: `grep -q 'conjure adopt --apply-step' AND grep -q 'conjure adopt --update-manifest'` → both match; and `grep -qi 'never.*write\|never.*edit\|Read, Bash' SKILL.md` documents the chokepoint.
    - Scaffolded into a sandbox via `init-project.sh existing`, the file is present at `.claude/skills/restructure/SKILL.md` with the gates dir alongside (criterion 1) — the Wave 0 scaffold assertion in tests/run.sh passes.
    - `bash tests/run.sh` Phase 23 scaffold + criterion-1 assertions show no ✗.
  </acceptance_criteria>
  <verify>
    <automated>test -f templates/skills/restructure/SKILL.md && [ "$(wc -l < templates/skills/restructure/SKILL.md)" -le 200 ] && head -10 templates/skills/restructure/SKILL.md | grep -q '^name:' && head -10 templates/skills/restructure/SKILL.md | grep -q '^description:' && grep -q 'allowed-tools:.*Read.*Bash' templates/skills/restructure/SKILL.md && grep -q 'conjure adopt --apply-step' templates/skills/restructure/SKILL.md && grep -q 'conjure adopt --update-manifest' templates/skills/restructure/SKILL.md && echo OK</automated>
  </verify>
  <done>SKILL.md exists ≤200 lines with name+description+`allowed-tools: [Read, Bash]` frontmatter, narrates the full read→extract→propose→GATE A+B→per-class approve→archive-last flow as orchestration prose (no bash loops, D-01), references every gate helper + the conjure adopt seam (stdin), lists the forbidden actions, and scaffolds cleanly into a target with its gates dir. Criterion 1 satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| restructure skill (Read+Bash) → project files | the skill must NEVER write a project file directly; all mutation crosses into `conjure adopt --apply-step` → lib/mutate.sh |
| skill → conjure adopt --update-manifest | op JSON crosses via stdin; validated by the shipped op-allowlist + required-field check |
| approval driver → /dev/tty | human consent crosses here; non-TTY must never be auto-approved |
| scaffold (init-project.sh) → target .claude/ | the whole skill dir is copied into an untrusted target repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-11 | Tampering | skill bypasses the chokepoint via Write/Edit on a project file | mitigate | `allowed-tools: [Read, Bash]` (physical) + optional `disallowed-tools: [Write, Edit]`; SKILL.md forbidden-actions section; criterion-1 frontmatter assertion verifies the declaration (RESTR-02) |
| T-23-12 | EoP / Repudiation | approval auto-proceeds in a non-interactive context without human consent | mitigate | approve.sh non-TTY guard `exit 2` (D-12, mirrors resolve.sh/adopt.sh); Wave 0 non-TTY-exit-2 assertion enforces it |
| T-23-13 | Tampering | malformed/garbled op JSON corrupts the manifest | mitigate | op JSON built injection-safe via `jq -n --arg`; the shipped `--update-manifest` jq-validates `{id,op,status}` + op-allowlist + atomic temp+mv (Phase 22, adopt.sh:400/435) |
| T-23-14 | Tampering (loss) | archive runs before write/extract or bulk-archives an undocumented decision | mitigate | SKILL.md sequences archive LAST (D-15) and routes each candidate through decision-scan → individual confirm (D-11, criterion 6) |
| T-23-15 | Information Disclosure | gates run AFTER apply (too late to block) | mitigate | SKILL.md mandates GATE A + GATE B on the STAGING file BEFORE the approval prompt (D-13/14); forbidden-actions calls out post-apply auditing |
| T-23-16 | Tampering | op JSON passed via the env var silently read as empty stdin (Pitfall 2) → wrong/no op | mitigate | SKILL.md mandates `printf '%s' "$op" \| conjure adopt --update-manifest` (stdin only); documents the env var is NOT forwarded through the CLI |
| T-23-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — zero external packages (RESEARCH Package Legitimacy Audit, line 113-115); ships bash + a markdown SKILL.md only. No slopcheck required |
</threat_model>

<verification>
- SKILL.md ≤200 lines (criterion 1) with name+description+`allowed-tools: [Read, Bash]` frontmatter.
- SKILL.md narrates the full flow as prose, references every gate helper + the stdin seam, lists forbidden actions, sequences archive last.
- approve.sh: non-TTY → exit 2; shellcheck-clean; zero `exit 1`; no `$EDITOR`.
- init-project.sh line 59 includes `restructure` (before `_anatomy`).
- Scaffold drive: `init-project.sh existing` lands SKILL.md + gates/*.sh in the target (criterion 1).
- Full `bash tests/run.sh`: every Phase 23 assertion green (scaffold, criterion 1, non-TTY exit 2, group-by, archive-last); no pre-existing regression; FAIL 0.
- `grep -v '^#' templates/skills/restructure/gates/*.sh scripts/init-project.sh | grep -c 'exit 1'` == 0.
- SKILL.md frontmatter audit clean (`conjure audit` / audit-setup.sh on a scaffolded target shows no skill frontmatter error).
</verification>

<success_criteria>
The restructure skill is installed and operational: scaffolded into any target during `conjure adopt` (criterion 1), declares the `[Read, Bash]` chokepoint (RESTR-02), narrates the human-gated propose→GATE A+B→per-class approve→archive-last flow riding the shipped Phase 22 seam (RESTR-01), and enforces per-class grouped approval with non-TTY exit-2 and one bulk summary log line (RESTR-03). All deterministic Phase 23 assertions are green; the interactive approve/skip/edit loop is the documented manual/expect UAT (criterion 2).
</success_criteria>

<output>
Create `.planning/phases/23-restructure-skill-safety-gates/23-03-SUMMARY.md` when done.
</output>
