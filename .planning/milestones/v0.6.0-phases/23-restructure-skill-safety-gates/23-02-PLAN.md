---
phase: 23-restructure-skill-safety-gates
plan: 02
type: execute
wave: 1
depends_on: ["23-01"]
files_modified:
  - templates/skills/restructure/gates/verify-invariants.sh
  - templates/skills/restructure/gates/audit-staged.sh
  - templates/skills/restructure/gates/extract-invariants.sh
  - templates/skills/restructure/gates/decision-scan.sh
autonomous: true
requirements: [RESTR-04, RESTR-05, RESTR-06]
nyquist_compliant: true

must_haves:
  truths:
    - "A proposed condensed CLAUDE.md missing a required invariant token causes verify-invariants.sh to exit 2 and print the missing tokens BEFORE any approval prompt (criterion 4)"
    - "A reflowed/case-mangled but content-complete CLAUDE.md still passes verify-invariants.sh (normalized substring, D-07)"
    - "A proposed CLAUDE.md containing an @import line causes audit-staged.sh to exit 2 with the audit output (criterion 5)"
    - "A proposed CLAUDE.md exceeding CLAUDE_MD_CAP (>100 lines) causes audit-staged.sh to block (exit 2)"
    - "An archive candidate containing decision vocabulary (decided/we chose/rationale/do not/never) is flagged by decision-scan.sh for individual confirmation; a clean doc is bulk-eligible (criterion 6)"
    - "extract-invariants.sh produces a non-empty canonical-token candidate list from a source CLAUDE.md, writing ONLY under .conjure-adopt-state/ (never a project file)"
    - "Every gate helper hard-fails with exit 2, never exit 1; all four are shellcheck-clean at error severity"
  artifacts:
    - path: "templates/skills/restructure/gates/verify-invariants.sh"
      provides: "GATE A — deterministic normalized-substring invariant verifier (D-05/07/08)"
      contains: "exit 2"
    - path: "templates/skills/restructure/gates/audit-staged.sh"
      provides: "GATE B — single-file @import/cap-breach audit via temp-dir conjure-audit shim (D-13/O-1)"
      contains: "audit-setup.sh"
    - path: "templates/skills/restructure/gates/extract-invariants.sh"
      provides: "constraint-extraction pre-pass → INVARIANTS candidates under .conjure-adopt-state/ (D-03/06)"
    - path: "templates/skills/restructure/gates/decision-scan.sh"
      provides: "decision-vocabulary scan escalating archive candidates to individual confirm (D-11)"
  key_links:
    - from: "templates/skills/restructure/gates/audit-staged.sh"
      to: "scripts/audit-setup.sh"
      via: "temp-dir shim seeds staged file as CLAUDE.md + minimal .claude/ then runs the real conjure audit"
      pattern: "audit-setup.sh"
    - from: "templates/skills/restructure/gates/verify-invariants.sh"
      to: ".conjure-adopt-state/INVARIANTS.txt"
      via: "reads the LLM-confirmed invariant token list as the needle set"
      pattern: "INVARIANTS"
---

<objective>
Build the four deterministic bash gate helpers the restructure skill invokes via Bash (D-01/D-02): the two pre-approval validation gates (verify-invariants = GATE A, audit-staged = GATE B; both run BEFORE the user sees a proposal per D-14), plus the extract-invariants pre-pass and the decision-scan archive guard. These turn the Wave 0 gate-helper assertions from graceful-red to green.

All four are net-new algorithm bodies wrapped in SHIPPED conventions (exit 2 never exit 1, lib/caps.sh constants, the audit-setup.sh exit 0/1/2 contract). They write ONLY transient files under `.conjure-adopt-state/` — never a project file (the RESTR-02 chokepoint; project mutation is the skill's job via `conjure adopt --apply-step` in Wave 2).

Purpose: deliver the deterministic safety backstop that blocks invalid LLM proposals (dropped invariants, @imports, cap breaches, undocumented-decision archives) before any human approval (RESTR-04/05/06, criteria 4/5/6).
Output: `templates/skills/restructure/gates/{verify-invariants,audit-staged,extract-invariants,decision-scan}.sh`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-restructure-skill-safety-gates/23-CONTEXT.md
@.planning/phases/23-restructure-skill-safety-gates/23-RESEARCH.md
@.planning/phases/23-restructure-skill-safety-gates/23-PATTERNS.md
@.planning/phases/23-restructure-skill-safety-gates/23-VALIDATION.md

# Wave 0 contract these gates satisfy:
@.planning/phases/23-restructure-skill-safety-gates/23-01-SUMMARY.md

# Convention + analog sources:
@scripts/audit-setup.sh
@lib/caps.sh
@scripts/resolve.sh
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GATE A + GATE B — verify-invariants.sh + audit-staged.sh (the two pre-approval gates, D-05/07/08/13/14, O-1/O-2)</name>
  <files>templates/skills/restructure/gates/verify-invariants.sh, templates/skills/restructure/gates/audit-staged.sh</files>
  <read_first>
    - .planning/phases/23-restructure-skill-safety-gates/23-PATTERNS.md (lines 79-122 verify-invariants spec: normalize() + 3.2-safe while-loop + missing-list BLOCK + exit-code convention from audit-setup.sh:18-21,296-298; lines 125-175 audit-staged spec: the temp-dir shim, CONJURE_HOME resolution, the two named conditions at audit-setup.sh:27-39).
    - .planning/phases/23-restructure-skill-safety-gates/23-RESEARCH.md (lines 194-218 Pattern 2 bash-3.2-safe sketch; lines 220-235 Pattern 3 audit shim; lines 298-302 Pitfall 1; lines 310-314 Pitfall 3 canonical tokens; lines 383-391 O-1/O-2).
    - scripts/audit-setup.sh:1-48 (the exit 0/1/2 contract, the CLAUDE.md cap-breach branch at :28-33, the `grep -q '^@'` @import check at :35-39, the `.claude/` absent → exit 2 at :48, the `cd "$TARGET"` dir-scope at :12-13, CONJURE_HOME default at :8).
    - lib/caps.sh (CLAUDE_MD_CAP=100, SKILL_MD_CAP=200 — source for any direct cap check).
    - scripts/resolve.sh:9-17 (CONJURE_HOME resolution + `set -uo pipefail` + the `# shellcheck source=/dev/null` directive idiom for dynamic source).
    - tests/fixtures/_restructure-gates/* (the Wave 0 fixtures these gates are asserted against: INVARIANTS.txt, with-invariant.md, missing-invariant.md, reflowed-invariant.md, with-import.md, oversized.md, clean-doc.md).
  </read_first>
  <behavior>
    - verify-invariants.sh `<staging-claude.md> <INVARIANTS.txt>`:
      - with-invariant.md (contains every token) → rc 0, no missing list.
      - missing-invariant.md (drops `exit 2`) → rc 2, stderr lists the missing token(s).
      - reflowed-invariant.md (case+whitespace mangled, content-complete) → rc 0 (normalized substring, D-07).
      - empty/blank invariant lines in INVARIANTS.txt are skipped (no false-positive miss).
    - audit-staged.sh `<staging-file>`:
      - with-import.md (`^@` line) → exit 2 + the audit output surfaced (criterion 5, D-13).
      - clean-doc.md (≤100 lines, no @import) → exit 0.
      - oversized.md (>100 lines, cap breach for a proposed CLAUDE.md) → exit 2.
      - resolves CONJURE_HOME to the KIT (not the target) so it finds scripts/audit-setup.sh.
  </behavior>
  <action>
    Create `templates/skills/restructure/gates/verify-invariants.sh` implementing GATE A per PATTERNS.md lines 94-115: a `#!/usr/bin/env bash` + `set -uo pipefail` header; usage comment with exit codes (0=all present, 2=missing); arg-parse `STAGING_CLAUDE="$1"` `INVARIANTS_TXT="$2"` (exit 2 with a usage message if either is absent/unreadable). Define `normalize() { tr '[:upper:]' '[:lower:]' | tr -s '[:space:]' ' ' | sed 's/^ //; s/ $//'; }`. Build `HAYSTACK="$(normalize < "$STAGING_CLAUDE")"` (the whole proposed file collapsed to one normalized line). Loop the invariant tokens with the bash-3.2-safe newline-delimited `missing=""` accumulator (NO mapfile/assoc arrays): for each non-empty line, `needle="$(printf '%s' "$inv" | normalize)"`, `case "$HAYSTACK" in *"$needle"*) ;; *) missing="${missing}${inv}"$'\n' ;; esac`. If `$missing` is non-empty: print `✗ restructure: proposed CLAUDE.md is missing required invariants:` + `printf '  - %s\n' $missing` to stderr, then `exit 2` (D-08 BLOCK — NEVER exit 1). Else `exit 0`.

    Create `templates/skills/restructure/gates/audit-staged.sh` implementing GATE B per O-1 (PATTERNS.md lines 144-171): header + `set -uo pipefail`; resolve `CONJURE_HOME="${CONJURE_HOME:-...}"` — note the caller (the skill, in a TARGET repo) must export CONJURE_HOME pointing at the KIT since `$(dirname "$0")/..` from `.claude/skills/restructure/gates/` is NOT the kit; default to a best-effort kit-relative resolution and document that the skill exports CONJURE_HOME (PATTERNS.md lines 168-171). Arg-parse `staged="$1"` (exit 2 if missing/unreadable). Source `lib/caps.sh` for `CLAUDE_MD_CAP` (default 100) via `# shellcheck source=/dev/null`.

    **Run the real audit to SURFACE output, but decide the BLOCK on two NAMED, deterministic conditions — NOT on the audit return code.** Rationale (checker B1): `conjure audit` returns rc=1 for unrelated harness-completeness WARNs (missing `.claudeignore`, `docs/`, `settings.json`, etc.) even on a perfectly clean CLAUDE.md in a minimal temp dir, so keying the gate on `rc>=1` would block EVERY clean proposal and make the clean-pass criterion unsatisfiable. RESTR-05/criterion 5 names exactly two block conditions: `@import` lines and cap breaches.

    Implement the temp-dir shim (faithful to RESTR-05 "run through conjure audit"): `tmp="$(mktemp -d)"`, `trap 'rm -rf "$tmp"' EXIT`, `cp "$staged" "$tmp/CLAUDE.md"`, `mkdir -p "$tmp/.claude"` (audit-setup.sh:48 exits 2 without `.claude/`), then `audit_out="$(bash "$CONJURE_HOME/scripts/audit-setup.sh" "$tmp" 2>&1)"; audit_rc=$?`. Surface `audit_out` to stderr (do NOT swallow it — criterion 5 requires the user sees WHY it blocked).

    Then apply this exact BLOCK decision table (the gate's own exit code, independent of `audit_rc`):
      - Condition 1 — `@import`: `if grep -q '^@' "$staged"; then` print the surfaced audit output + a `✗ restructure: proposed CLAUDE.md contains @import (forbidden eager-load)` line → `exit 2`.
      - Condition 2 — cap breach: `lines=$(grep -c '' "$staged")` (line count); `if [ "$lines" -gt "${CLAUDE_MD_CAP:-100}" ]; then` print the surfaced audit output + `✗ restructure: proposed CLAUDE.md is $lines lines (> ${CLAUDE_MD_CAP} cap)` → `exit 2`.
      - Otherwise → `exit 0` (clean; the surfaced WARN-band audit output is informational only and MUST NOT block).
    Keep `exit 2` for blocks, never `exit 1`. The audit shim provides the human-readable WHY (criterion 5); the deterministic grep/line-count provides the satisfiable block decision. If the shim is infeasible at runtime (audit-setup.sh missing), the two named-condition checks (grep `^@` + line-count vs `CLAUDE_MD_CAP`) still stand alone as the fallback — they are the authoritative block logic regardless.

    Both scripts: source any dynamic file with a `# shellcheck source=/dev/null` directive (resolve.sh:16); `chmod +x` is handled by the scaffold copy, but ensure the shebang is present. Use only POSIX bash 3.2 constructs. Add a one-line usage comment block at the top of each (path, usage, exit codes) mirroring audit-setup.sh:1-4.
  </action>
  <acceptance_criteria>
    - `bash templates/skills/restructure/gates/verify-invariants.sh tests/fixtures/_restructure-gates/with-invariant.md tests/fixtures/_restructure-gates/INVARIANTS.txt; echo $?` → 0.
    - `bash templates/skills/restructure/gates/verify-invariants.sh tests/fixtures/_restructure-gates/missing-invariant.md tests/fixtures/_restructure-gates/INVARIANTS.txt 2>&1; echo "rc=$?"` → prints the missing token(s) on stderr AND `rc=2`.
    - `bash templates/skills/restructure/gates/verify-invariants.sh tests/fixtures/_restructure-gates/reflowed-invariant.md tests/fixtures/_restructure-gates/INVARIANTS.txt; echo $?` → 0 (normalized match survives reflow/case).
    - `CONJURE_HOME="$(pwd)" bash templates/skills/restructure/gates/audit-staged.sh tests/fixtures/_restructure-gates/with-import.md; echo $?` → 2, and stderr contains the audit `@import` message.
    - `CONJURE_HOME="$(pwd)" bash templates/skills/restructure/gates/audit-staged.sh tests/fixtures/_restructure-gates/clean-doc.md; echo $?` → 0.
    - `CONJURE_HOME="$(pwd)" bash templates/skills/restructure/gates/audit-staged.sh tests/fixtures/_restructure-gates/oversized.md; echo $?` → 2.
    - `shellcheck -S error -e SC2164,SC2044,SC2034,SC2155 templates/skills/restructure/gates/verify-invariants.sh templates/skills/restructure/gates/audit-staged.sh` → clean (rc 0).
    - `grep -v '^#' templates/skills/restructure/gates/verify-invariants.sh templates/skills/restructure/gates/audit-staged.sh | grep -c 'exit 1'` → 0.
    - The corresponding Wave 0 assertions in tests/run.sh's Phase 23 section now PASS: `bash tests/run.sh 2>&1 | grep -iE 'verify-invariants|audit-staged' | grep -c '✗'` → 0.
  </acceptance_criteria>
  <verify>
    <automated>shellcheck -S error -e SC2164,SC2044,SC2034,SC2155 templates/skills/restructure/gates/verify-invariants.sh templates/skills/restructure/gates/audit-staged.sh && [ "$(grep -v '^#' templates/skills/restructure/gates/verify-invariants.sh templates/skills/restructure/gates/audit-staged.sh | grep -c 'exit 1')" -eq 0 ] && bash templates/skills/restructure/gates/verify-invariants.sh tests/fixtures/_restructure-gates/with-invariant.md tests/fixtures/_restructure-gates/INVARIANTS.txt && ! bash templates/skills/restructure/gates/verify-invariants.sh tests/fixtures/_restructure-gates/missing-invariant.md tests/fixtures/_restructure-gates/INVARIANTS.txt 2>/dev/null && CONJURE_HOME="$(pwd)" bash templates/skills/restructure/gates/audit-staged.sh tests/fixtures/_restructure-gates/clean-doc.md && ! CONJURE_HOME="$(pwd)" bash templates/skills/restructure/gates/audit-staged.sh tests/fixtures/_restructure-gates/with-import.md 2>/dev/null && echo OK</automated>
  </verify>
  <done>verify-invariants.sh passes present/reflowed and blocks (exit 2 + missing list) on a dropped token; audit-staged.sh passes clean files and blocks (exit 2 + surfaced output) on @import and cap-breach (>CLAUDE_MD_CAP) files; both are shellcheck-clean at error severity, contain zero `exit 1`, and flip their Wave 0 assertions to green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: extract-invariants.sh + decision-scan.sh (the pre-pass + the archive guard, D-03/06/11, O-2)</name>
  <files>templates/skills/restructure/gates/extract-invariants.sh, templates/skills/restructure/gates/decision-scan.sh</files>
  <read_first>
    - .planning/phases/23-restructure-skill-safety-gates/23-PATTERNS.md (lines 178-201 decision-scan spec: the 5 D-11 terms, scope-to-archive-candidates-only, `grep -iw` over-match guard, over-flag-is-safe direction; lines 205-227 extract-invariants spec: tmpfile/find analog from resolve.sh:19-24, the D-06 signal-grep seed, write ONLY under .conjure-adopt-state/).
    - .planning/phases/23-restructure-skill-safety-gates/23-RESEARCH.md (lines 343-350 the signal-grep seed; lines 322-326 Pitfall 5 decision-scan over-matching; line 468 Security V12 — helpers write only under .conjure-adopt-state/).
    - scripts/resolve.sh:19-24 (the mktemp tmpfile + find-into-sorted-file analog) + scripts/audit-setup.sh:139-152 (the grep-and-flag per-file note loop analog).
    - tests/fixtures/_restructure-gates/* (decision-doc.md, clean-doc.md, and a source CLAUDE.md for extraction — reuse with-invariant.md or the fixture's CLAUDE.md).
    - .conjure-adopt-state path convention: D-03 says INVARIANTS.txt + candidates live under .conjure-adopt-state/ (transient, beside staging/), NOT a project file.
  </read_first>
  <behavior>
    - extract-invariants.sh `<source-claude.md> [state-dir]`:
      - emits a non-empty candidates file (default `.conjure-adopt-state/INVARIANTS.candidates`) containing canonical-token signal lines grepped from the source (must/never/always/exit 2/@import/≤N/named-backtick tokens), one per line.
      - writes ONLY under the state dir (never a project file); exits 0 on success, exit 2 on unreadable source / un-writable state dir.
    - decision-scan.sh `<archive-candidate-file>`:
      - decision-doc.md (matches a D-11 term) → signals "individual" (documented exit code/stdout token).
      - clean-doc.md (no D-11 term) → signals "bulk".
      - scope is the single candidate passed in (not a full-tree grep); over-flagging is the SAFE direction.
  </behavior>
  <action>
    Create `templates/skills/restructure/gates/extract-invariants.sh` per PATTERNS.md lines 205-227: header + `set -uo pipefail` + usage/exit-code comment. Arg `SRC_CLAUDE="$1"` (exit 2 if unreadable); `STATE_DIR="${2:-.conjure-adopt-state}"`; `mkdir -p "$STATE_DIR"` (exit 2 if it can't be created). VALIDATE the state dir is under `.conjure-adopt-state` (Security V12: helpers write only there — refuse a path that resolves outside it; reuse a `case "$STATE_DIR" in *.conjure-adopt-state*) ;; *) exit 2 ;; esac` guard or a resolve-and-prefix check). Run the D-06 signal grep producing canonical tokens (O-2): `grep -niE 'must|never|always|forbidden|required|do not|exit 2|@import|≤[0-9]+|[0-9]+ lines|`+'`'+`[^`+'`'+`]+`+'`'+`' "$SRC_CLAUDE" > "$STATE_DIR/INVARIANTS.candidates"` (scope to the single source file — full-tree grep is slow per PITFALLS). The LLM later refines these candidates into the confirmed `.conjure-adopt-state/INVARIANTS.txt` (D-05); this helper produces the deterministic checklist. Exit 0 (the candidates file existing + non-empty is success; an empty result on a constraint-free file is still exit 0 with an empty candidates file — the LLM may add tokens). Document in the usage comment that the OUTPUT is candidates, not the final INVARIANTS.txt (the LLM confirms; verify-invariants reads the confirmed file).

    Create `templates/skills/restructure/gates/decision-scan.sh` per PATTERNS.md lines 178-201: header + `set -uo pipefail` + usage/exit-code comment defining the signal contract (DECIDE and DOCUMENT it explicitly — e.g. `exit 0` = clean/bulk-eligible, `exit 1` is FORBIDDEN by convention, so use `exit 0` for bulk and a distinct signal for individual; recommended: print `individual` (or `bulk`) to stdout AND set exit code 0 for bulk / a non-zero-but-not-1 code is awkward — INSTEAD use stdout token as the contract and always `exit 0` on a successful scan, reserving `exit 2` for unreadable input). Arg `candidate="$1"` (exit 2 if unreadable). Run `grep -iE 'decided|we chose|rationale|do not|never' "$candidate"` (the 5 D-11 terms, case-insensitive); consider `grep -iwE` word-boundary on the bare-word terms (`never`, `do not`) to cut noise per Pitfall 5 — but DO NOT relax below the 5 terms (over-flag is safe, CR-6 HIGH). If matched: print `individual` to stdout (and optionally the matching lines to stderr for the skill to show the user); if no match: print `bulk`. Always `exit 0` on a successful scan; `exit 2` only on unreadable input. The skill reads the stdout token to decide bulk-vs-individual routing (D-11).

    Both scripts: bash 3.2-safe; `# shellcheck source=/dev/null` for any dynamic source; usage comment block at top. NEVER write a project file — extract-invariants writes only under .conjure-adopt-state/; decision-scan is read-only.
  </action>
  <acceptance_criteria>
    - `T="$(mktemp -d)"; mkdir -p "$T/.conjure-adopt-state"; bash templates/skills/restructure/gates/extract-invariants.sh tests/fixtures/_restructure-gates/with-invariant.md "$T/.conjure-adopt-state"; echo $?` → 0 and `[ -s "$T/.conjure-adopt-state/INVARIANTS.candidates" ]` → true; `grep -q 'exit 2' "$T/.conjure-adopt-state/INVARIANTS.candidates"` → matches (the source token was captured).
    - extract-invariants.sh refuses to write outside .conjure-adopt-state: invoking with a `STATE_DIR` of `/tmp/not-adopt-state` → exit 2 (no file written there).
    - `bash templates/skills/restructure/gates/decision-scan.sh tests/fixtures/_restructure-gates/decision-doc.md` → stdout contains `individual`; rc 0.
    - `bash templates/skills/restructure/gates/decision-scan.sh tests/fixtures/_restructure-gates/clean-doc.md` → stdout contains `bulk`; rc 0.
    - `shellcheck -S error -e SC2164,SC2044,SC2034,SC2155 templates/skills/restructure/gates/extract-invariants.sh templates/skills/restructure/gates/decision-scan.sh` → clean.
    - `grep -v '^#' templates/skills/restructure/gates/extract-invariants.sh templates/skills/restructure/gates/decision-scan.sh | grep -c 'exit 1'` → 0.
    - The corresponding Wave 0 assertions now PASS: `bash tests/run.sh 2>&1 | grep -iE 'extract-invariants|decision-scan' | grep -c '✗'` → 0.
  </acceptance_criteria>
  <verify>
    <automated>shellcheck -S error -e SC2164,SC2044,SC2034,SC2155 templates/skills/restructure/gates/extract-invariants.sh templates/skills/restructure/gates/decision-scan.sh && [ "$(grep -v '^#' templates/skills/restructure/gates/extract-invariants.sh templates/skills/restructure/gates/decision-scan.sh | grep -c 'exit 1')" -eq 0 ] && bash templates/skills/restructure/gates/decision-scan.sh tests/fixtures/_restructure-gates/decision-doc.md | grep -q individual && bash templates/skills/restructure/gates/decision-scan.sh tests/fixtures/_restructure-gates/clean-doc.md | grep -q bulk && echo OK</automated>
  </verify>
  <done>extract-invariants.sh produces a non-empty canonical-token candidates file under .conjure-adopt-state/ only (refusing paths outside it); decision-scan.sh signals `individual` for decision-vocabulary docs and `bulk` for clean docs; both are shellcheck-clean, contain zero `exit 1`, and flip their Wave 0 assertions to green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| skill (Read+Bash) → gate helpers | the skill invokes the helpers via Bash; the helpers receive untrusted LLM-proposed staging content as input |
| gate helper → filesystem | helpers may write ONLY transient files under `.conjure-adopt-state/`; any write outside it is a chokepoint leak |
| LLM-proposed content → invariant/audit gates | proposed CLAUDE.md is untrusted until both gates pass (D-14) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-05 | Tampering (info-integrity) | LLM silently drops a security/compliance invariant during condensation | mitigate | verify-invariants.sh BLOCKs (exit 2) before approval, printing the missing tokens (D-05/08, criterion 4); canonical-token extraction (O-2) survives paraphrase (CR-1 HIGH) |
| T-23-06 | Tampering | LLM re-introduces an `@import` eager-load foot-gun while "fixing" CLAUDE.md | mitigate | audit-staged.sh runs the real conjure audit on the staged file and blocks on `^@` (always a hard err) before approval (D-13, criterion 5, CR-5) |
| T-23-07 | Tampering (loss) | bulk-archiving a file holding an active, undocumented decision | mitigate | decision-scan.sh flags the 5 D-11 terms → individual confirm; over-flag is the safe direction (CR-6 HIGH); never relaxed below the 5 terms |
| T-23-08 | Tampering / EoP | a gate helper writes a project file outside .conjure-adopt-state/ (chokepoint leak) | mitigate | extract-invariants.sh validates STATE_DIR is under `.conjure-adopt-state` and exits 2 otherwise; decision-scan + the two gates are read-only; acceptance criterion asserts no out-of-state-dir write |
| T-23-09 | Repudiation | a gate fails with `exit 1` instead of `exit 2`, violating the project convention | mitigate | every hard fail uses `exit 2`; `grep -v '^#' ... grep -c 'exit 1'` == 0 is an acceptance criterion + the per-wave convention gate |
| T-23-10 | Denial of Service | audit-staged.sh leaves temp dirs behind on every invocation | mitigate | `trap 'rm -rf "$tmp"' EXIT` cleans the shim temp dir; shellcheck-clean confirms no leaked traps |
| T-23-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — zero external packages (RESEARCH Package Legitimacy Audit, line 113-115); pure bash + jq/grep/sed/tr/awk all verified present. No slopcheck required |
</threat_model>

<verification>
- All four gate helpers shellcheck-clean at error severity: `shellcheck -S error -e SC2164,SC2044,SC2034,SC2155 templates/skills/restructure/gates/*.sh`.
- Convention gate: `grep -v '^#' templates/skills/restructure/gates/*.sh | grep -c 'exit 1'` == 0.
- verify-invariants: present → rc 0, missing → rc 2 + list, reflowed → rc 0.
- audit-staged: @import → rc 2 + output, clean → rc 0, oversized → rc 2.
- decision-scan: decision-doc → individual, clean → bulk.
- extract-invariants: non-empty candidates under .conjure-adopt-state/ only; refuses out-of-state paths.
- The Phase 23 Wave 0 gate-helper assertions flip from red to green: `bash tests/run.sh 2>&1 | grep -iE 'verify-invariants|audit-staged|extract-invariants|decision-scan' | grep -c '✗'` == 0.
- No regression: full `bash tests/run.sh` shows no pre-existing assertion flipping to ✗.
</verification>

<success_criteria>
The four deterministic gate helpers exist, pass their fixture-driven contracts (invariant present/missing/reflowed; audit clean/@import/oversized; decision individual/bulk; extraction non-empty + state-dir-confined), are shellcheck-clean at error severity, contain zero `exit 1`, write no project files, and turn every Wave 0 gate-helper assertion green. The two pre-approval gates (verify-invariants, audit-staged) implement the D-14 BLOCK-before-approval contract (criteria 4 & 5); decision-scan implements the D-11 archive guard (criterion 6).
</success_criteria>

<output>
Create `.planning/phases/23-restructure-skill-safety-gates/23-02-SUMMARY.md` when done.
</output>
