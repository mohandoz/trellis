---
phase: 24-integration-tests-argus-fixture
plan: 02
type: execute
wave: 2
depends_on:
  - 24-01
files_modified:
  - tests/run.sh
autonomous: true
requirements: []   # verification phase — no REQ-* IDs; gated by the 5 ROADMAP success criteria
must_haves:
  truths:
    - "conjure adopt --dry-run on the 500-file argus fixture completes in <30s and writes zero files to the fixture dir (criterion 1)"
    - "A live adopt then --rollback produces zero diff (per-file sha256 + diff -r with D-03 excludes empty) (criterion 2)"
    - "A second adopt on an already-adopted fixture makes zero mutations and reports 'nothing to scaffold' (criterion 3)"
    - "SIGKILL after snapshot before scaffold → non-TTY re-run exits 2 with recovery options; CONJURE_ADOPT_ROLLBACK=1 re-run restores cleanly (criterion 4)"
    - "A symlink fixture file is absent from manifest files[]; an @import staged CLAUDE.md is blocked (exit 2) by audit-staged and never written to the target CLAUDE.md (criterion 5)"
    - "The full suite stays green with the new Phase 24 assertions and never regresses below the 429 baseline"
  artifacts:
    - path: "tests/run.sh"
      provides: "▸ Phase 24 test block — 5 criterion sections behind a P24_ARGUS_OK guard, inserted after :3280"
      contains: "▸ Phase 24"
  key_links:
    - from: "tests/run.sh ▸ Phase 24 block"
      to: "tests/fixtures/_brownfield-argus/generate-argus.sh"
      via: "P24_ARGUS_GEN presence guard + bash \"$P24_ARGUS_GEN\" \"$target\" per section"
      pattern: "P24_ARGUS"
    - from: "tests/run.sh ▸ Phase 24 block"
      to: "scripts/adopt.sh"
      via: "DRY_RUN / CONJURE_ADOPT_ROLLBACK env-var invocation of the shipped pipeline under test"
      pattern: "P22_ADOPT_SH"
    - from: "tests/run.sh ▸ Phase 24 SIGKILL section"
      to: "state.json .current_step"
      via: "bounded poll on current_step ∈ {snapshot,inventory} before kill -9"
      pattern: "current_step"
---

<objective>
Add the `▸ Phase 24` block to `tests/run.sh` (after :3280, before the gh-stub
cleanup at :3282 / Summary at :3285), asserting all five ROADMAP success criteria
end-to-end against the `_brownfield-argus` fixture. Mirror the green, battle-tested
`▸ Phase 22` section idioms (mktemp sandboxes, set/reset EXIT-trap discipline,
`p22_sha`, `diff -r` with D-03 excludes, the SIGKILL background-launch + bounded-poll
harness) pointed at the 500-file argus fixture.

Purpose: This is the verification deliverable — it PROVES the shipped Phases 21–23
pipeline holds together at 500-file scale. Every assertion traces to a 24-VALIDATION.md
criterion. Behind a `P24_ARGUS_OK` presence guard so it reports graceful RED if the
generator (Plan 01) is absent.

Output: the `▸ Phase 24` block in `tests/run.sh` (MODIFY), all sections GREEN, the
full suite never regressing below the 429 baseline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-integration-tests-argus-fixture/24-RESEARCH.md
@.planning/phases/24-integration-tests-argus-fixture/24-VALIDATION.md
@.planning/phases/24-integration-tests-argus-fixture/24-01-PLAN.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ▸ Phase 24 block foundation + C1 (dry-run perf/zero-write) + C2 (rollback zero-diff) + C3 (idempotent re-run)</name>
  <files>tests/run.sh</files>
  <read_first>
    - tests/run.sh:2357-2391 (the Phase 22 block PREAMBLE to mirror: the section-banner comment style, `P22_ADOPT_SH`/`P22_ADOPT_OK` presence-guard pattern, `P22_FIXTURE` path, the `p22_adopt()` wrapper, and the cross-platform `p22_sha()` helper — Phase 24 reuses `P22_ADOPT_SH` and `p22_sha` directly and adds a parallel `P24_ARGUS_GEN`/`P24_ARGUS_OK` guard).
    - tests/run.sh:2393-2449 (Phase 22 dry-run section — the EXACT C1 template: mktemp target, trap, run with `DRY_RUN=1`, assert `git status --porcelain` clean fallback + `find -name adopt-manifest.json | wc -l` == 0 + `find -name .conjure-adopt-state` == 0; copy the zero-write assertions).
    - tests/run.sh:2322-2355 (Phase 21 perf gate — the `date +%s` delta pattern: `P21_START=$(date +%s)` ... `P21_END=$(date +%s)`; `P21_ELAPSED=$((END-START))`; `[ "$ELAPSED" -lt 30 ]`. Reuse the 30s ceiling VERBATIM — do NOT tighten; research measured ~6s on 512 files).
    - tests/run.sh:2554-2611 (Phase 22 rollback section — the EXACT C2 template: `P22_RB_TARGET`/`P22_RB_PRE`/`P22_RB_HASHES` mktemp trio with the hash file OUTSIDE both trees; record per-file sha256 via `p22_sha`; live adopt then `CONJURE_ADOPT_ROLLBACK=1 ... --rollback`; per-file sha256 == before; `[ROLLBACK]` in RESTRUCTURE-LOG.md; `diff -r` with the 5 D-03 `-x` excludes empty).
    - tests/run.sh:2885-2910 (Phase 22 self-copy section — the `rm -rf "$target/.conjure-adopt-state"` between-runs idiom at :2896 that makes a second adopt a clean idempotent scaffold instead of a recovery prompt — needed for C3).
    - 24-RESEARCH.md "## Code Examples" — the idempotent re-run assertion block (`grep -Eq 'Scaffolded:[[:space:]]*0[[:space:]]+layer'` + `jq -r '.created | length'` == 0 + the optional "nothing to scaffold" grep) and the "Note on second-run mechanics" (clear `.conjure-adopt-state` between runs).
    - 24-RESEARCH.md "## Architecture Patterns" Pattern 1 (sandbox + set/reset EXIT-trap skeleton), Pattern 2 (`P24_ARGUS_OK` presence guard for graceful RED), Pattern 4 (zero-diff rollback verification), and "## Common Pitfalls" Pitfall 2 (perf 30s ceiling, no tightening) + Pitfall 4 (hash file OUTSIDE both trees).
    - 24-VALIDATION.md "## The 5 Criteria → Observable Test Points" rows C1, C2, C3 (the exact observable signals each section must assert).
    - tests/run.sh:3278-3292 (the insertion point: the Phase 24 block goes AFTER `:3280` "End Phase 23 test block" and BEFORE the gh-stub cleanup at `:3282` and the Summary at `:3285`).
    - CLAUDE.md Constraints (POSIX bash 3.2+; `exit 2` never `exit 1`; no new deps; mirror existing run.sh idioms).
  </read_first>
  <action>
    Insert a new `▸ Phase 24` block into `tests/run.sh` immediately after the "End Phase 23 test block" banner at :3280 and before the gh-stub cleanup loop at :3282. Open with a section-banner comment block (mirror the Phase 22 banner at :2361-2369) explaining this is the v0.6.0 E2E verification against `_brownfield-argus`, behind a `P24_ARGUS_OK` guard. Then:
    Define the shared preamble (mirror :2371-2391): `P24_ARGUS_GEN="$CONJURE_HOME/tests/fixtures/_brownfield-argus/generate-argus.sh"`, `P24_ARGUS_OK=0`, `[ -f "$P24_ARGUS_GEN" ] && P24_ARGUS_OK=1`. Reuse the EXISTING `P22_ADOPT_SH` and `p22_sha` (they are defined earlier in the same script and in scope) — do not redefine them. Every section must ALSO require `P22_ADOPT_OK -eq 1` AND `P24_ARGUS_OK -eq 1`; if either is missing, `fail "... generator/adopt.sh missing — Plan 01 / Wave 1 must create ... first"` so the suite reports graceful RED instead of crashing.
    Section C1 (`▸ Phase 24 — dry-run perf + zero writes (criterion 1)`): mktemp target + trap; `bash "$P24_ARGUS_GEN" "$target"` to materialize 500 files; capture `START=$(date +%s)`; run `DRY_RUN=1 CONJURE_HOME="$CONJURE_HOME" bash "$P22_ADOPT_SH" "$target"`; `END=$(date +%s)`; assert `$((END-START)) -lt 30` (pass with the measured delta, fail if >= 30). Assert zero writes: `find "$target" -name adopt-manifest.json | wc -l | tr -d ' '` == 0 AND `find "$target" -name '.conjure-adopt-state' | wc -l | tr -d ' '` == 0 (non-git fallback per O-2 — the sandbox is not a git repo). `rm -rf` + `trap - EXIT`.
    Section C2 (`▸ Phase 24 — live adopt + rollback zero-diff (criterion 2)`): mirror :2554-2611 exactly, scaled to argus. `P24_RB_TARGET`/`P24_RB_PRE` = two mktemp dirs, `P24_RB_HASHES` = a mktemp FILE OUTSIDE both trees (Pitfall 4). Generate argus into BOTH the target and the pristine pre copy (run the generator twice, or generate once then `cp -r`; generating twice is non-deterministic only for the symlink/content if randomized — generate once into PRE then `cp -aR` PRE → TARGET to guarantee identical pre-state, OR generate into target then `cp -aR` target/. → PRE; use `cp -aR` to preserve the symlink). Record per-file sha256 of every target file (skip `.git`) into `P24_RB_HASHES`. Live adopt (`DRY_RUN=0`), then rollback (`DRY_RUN=0 CONJURE_ADOPT_ROLLBACK=1 ... --rollback`). Assert: per-file sha256 == recorded before (mismatch count == 0); `[ROLLBACK]` in RESTRUCTURE-LOG.md; `diff -r` with the 5 D-03 excludes (`.conjure-adopt-backups`, `.conjure-archive-*`, `RESTRUCTURE-LOG.md`, `adopt-manifest.json`, `.conjure-adopt-state`) empty. Cleanup + `trap - EXIT`.
    Section C3 (`▸ Phase 24 — idempotent re-run (criterion 3)`): mktemp target + trap; generate argus; first live adopt; snapshot `cp -aR` the post-run-1 target to a pristine `RUN1_AFTER` mktemp OUTSIDE the target (for the run1-vs-run2 diff); `rm -rf "$target/.conjure-adopt-state"` (the :2896 idiom) so the second run is a clean idempotent scaffold not a recovery prompt; capture the second-run stdout. Assert: stdout matches `Scaffolded:[[:space:]]*0[[:space:]]+layer` (signal a); `jq -r '.created | length' "$target/.conjure-adopt-state/state.json"` == 0 (signal b); `diff -r` (excl D-03) between RUN1_AFTER and the post-run-2 target empty (signal c, zero mutations); stdout matches `nothing to scaffold` (signal d — depends on the Plan 01 O-1 deviation). Cleanup + `trap - EXIT`.
    Use only POSIX/system tools already used in run.sh (find/wc/tr/jq/diff/date/grep/sha256 via p22_sha). No new deps. No `exit 1`.
  </action>
  <verify>
    <automated>bash tests/run.sh 2>&1 | grep -E '▸ Phase 24|✗' ; echo "---count---" ; bash tests/run.sh 2>&1 | grep -c '▸ Phase 24'</automated>
  </verify>
  <acceptance_criteria>
    - `bash tests/run.sh 2>&1 | grep -c '▸ Phase 24'` >= 3 (C1, C2, C3 section headers present after this task).
    - C1: a dry-run on the argus fixture passes both the `<30s` (`date +%s` delta) assertion and the zero-write assertions (no `adopt-manifest.json` and no `.conjure-adopt-state` under the target).
    - C2: the rollback section passes per-file sha256 == before, the `[ROLLBACK]`-in-log assertion, and the `diff -r` (excl D-03) empty assertion.
    - C3: the idempotent-re-run section passes the `Scaffolded:  0 layer files` assertion, `created | length` == 0, the run1-after-vs-run2-after `diff -r` (excl D-03) empty assertion, AND the literal `nothing to scaffold` assertion (consuming the Plan 01 deviation).
    - No `✗` lines appear for any `▸ Phase 24` section.
    - `bash tests/run.sh` total is `FAIL: 0` and `PASS` is >= 429 + the new C1/C2/C3 assertions (no regression).
    - `grep -v '^#' tests/run.sh | grep -c 'exit 1'` is unchanged from baseline (no `exit 1` introduced).
    - The block sits between `:3280` (End Phase 23) and the gh-stub cleanup — `grep -n '▸ Phase 24' tests/run.sh` shows a line number > 3280 and the gh-stub cleanup loop still precedes the Summary.
  </acceptance_criteria>
  <done>The `▸ Phase 24` block exists after :3280 with C1/C2/C3 sections all GREEN against the 500-file argus fixture, behind a `P24_ARGUS_OK` guard, with no `exit 1` and no suite regression.</done>
</task>

<task type="auto">
  <name>Task 2: ▸ Phase 24 C4 (SIGKILL recovery) + C5 (symlink-skip + @import-block) + full-suite no-regression gate</name>
  <files>tests/run.sh</files>
  <read_first>
    - tests/run.sh:2692-2747 (Phase 22 SIGKILL section — the EXACT C4 harness: background-launch `bash "$P22_ADOPT_SH" "$target" >/dev/null 2>&1 &`, capture `$!`, bounded poll, `kill -9`, `wait`, then a non-TTY re-run with `< /dev/null`, assert `rc == 2` + `grep -qi 'last completed:'` + the three recovery flags `--rollback`/`--resume`/`--start-fresh`).
    - 24-RESEARCH.md "## Architecture Patterns" Pattern 3 (the PRECISE C4 kill window: poll `jq -r '.current_step // ""' "$target/.conjure-adopt-state/state.json"` and break when it is `snapshot` or `inventory` — snapshot done, scaffold not yet; this is more precise than the Phase 22 backups-dir poll) + "## Common Pitfalls" Pitfall 3 (the pipeline order is snapshot → inventory → scaffold, so "after snapshot before scaffold" spans inventory — poll `current_step ∈ {snapshot,inventory}`).
    - 24-RESEARCH.md "## Validation Architecture" C4 row: the automatable path is the non-TTY exit-2 + an explicit `CONJURE_ADOPT_ROLLBACK=1` re-run → `diff -r` (excl D-03) zero-diff; the interactive `[r]/[c]/[s]` prompt is MANUAL-ONLY (PTY, already UAT'd in 22/23) — do NOT attempt to automate the interactive prompt; assert only the non-TTY half + the rollback-restores-cleanly half.
    - 24-RESEARCH.md "## Code Examples" — the symlink-skip assertion (`jq -e --arg p 'docs/linked.md' '.files[]?|select(.path==$p)'` must NOT match → symlink absent from files[]) and the @import-block assertion (`audit-staged.sh` on a staged `@import` CLAUDE.md exits 2; the target CLAUDE.md never gains an `^@` line).
    - templates/skills/restructure/gates/audit-staged.sh (the SHIPPED gate under test — exits 2 on a leading `@` line) and tests/run.sh:2982-2990 (the Phase 23 audit-staged invocation idiom to mirror for the @import sub-check).
    - 24-VALIDATION.md "## The 5 Criteria → Observable Test Points" rows C4, C5 + "## Manual-Only Verifications" (the C4 interactive prompt is the documented manual-only sub-check).
    - tests/run.sh:2912-2914 (the "End Phase 22 test block" banner style) — Phase 24 must close with a matching "End Phase 24 test block" banner before the existing gh-stub cleanup at :3282.
    - CLAUDE.md Constraints (POSIX bash 3.2+; `exit 2` never `exit 1`; no new deps).
  </read_first>
  <action>
    Append two more sections to the `▸ Phase 24` block (after the C3 section from Task 1), then close the block with an "End Phase 24 test block" banner, all still BEFORE the gh-stub cleanup at the (now-shifted) original :3282.
    Section C4 (`▸ Phase 24 — SIGKILL recovery after snapshot (criterion 4)`): mktemp target + trap; generate argus; snapshot a pristine `cp -aR` PRE copy OUTSIDE the target (for the post-rollback zero-diff). **Use the :2706 background-launch/kill/non-TTY-re-run STRUCTURE, but REPLACE its poll condition** — the Phase 22 section polls on `.conjure-adopt-backups` dir existence (line 2707); do NOT copy that looser check. Instead use the Pattern-3 `current_step` poll (more precise: snapshot done, scaffold not yet). Background-launch `DRY_RUN=0 CONJURE_HOME="$CONJURE_HOME" bash "$P22_ADOPT_SH" "$target" >/dev/null 2>&1 &`; capture PID. Bounded poll: in a `for _i in $(seq 1 200)` loop, read `_step="$(jq -r '.current_step // ""' "$target/.conjure-adopt-state/state.json" 2>/dev/null || true)"`; `case "$_step" in snapshot|inventory) break ;; esac`; `kill -0 "$PID" || break`; `sleep 0.05`. Then `kill -9 "$PID"`; `wait "$PID" 2>/dev/null || true`. **Anti-flake (warning): wrap the launch+poll+kill in a small relaunch loop (up to 3 attempts)** — the 500-file argus snapshot is the wide, expensive step so the in-window catch is reliable, but on a fast/loaded runner the process could transition past `inventory` between polls; if the kill landed out-of-window (process finished OR step was scaffold/audit), clear `.conjure-adopt-state`/`.conjure-adopt-backups` and relaunch; only after 3 out-of-window attempts record a fail with the last observed step (so a legitimate timing slip retries rather than going false-RED). Then the non-TTY recovery re-run (mirror :2720-2743): `OUT="$(DRY_RUN=0 CONJURE_HOME="$CONJURE_HOME" bash "$P22_ADOPT_SH" "$target" < /dev/null 2>&1)"`; `RC=$?`; assert `RC == 2`, `grep -qi 'last completed:'`, and all three flags `--rollback`/`--resume`/`--start-fresh` present. Then drive rollback automatically (`DRY_RUN=0 CONJURE_ADOPT_ROLLBACK=1 CONJURE_HOME="$CONJURE_HOME" bash "$P22_ADOPT_SH" --rollback "$target"`) and assert `diff -r` (excl D-03) between PRE and target empty — "choosing rollback restores the fixture cleanly". Add a `# NOTE:` comment that the interactive `[r]/[c]/[s]` prompt is manual-only (PTY, UAT'd in 22/23) and that the relaunch loop guards an inherent kill-window timing race. Cleanup + `trap - EXIT`.
    Section C5 (`▸ Phase 24 — symlink skip + @import pre-write block (criterion 5)`): mktemp target + trap; generate argus (which creates the real `ln -s docs/linked.md` and the `with-import.md` @import seed). (5a symlink) Live adopt; then assert the symlink path is ABSENT from manifest files[]: `jq -e --arg p 'docs/linked.md' '.files[]?|select(.path==$p)' "$target/adopt-manifest.json"` must return non-zero (pass when the select finds nothing). Use the actual relative path the generator used for the symlink (match Plan 01's `docs/linked.md`). (5b @import block) Stage the @import seed as a proposed CLAUDE.md: `mkdir -p "$target/.conjure-adopt-state/staging"`; copy the generator's `with-import.md` content (or `printf '# CLAUDE\n@.claude/skills/x/SKILL.md\n'`) to `"$target/.conjure-adopt-state/staging/CLAUDE.md"`; run `CONJURE_HOME="$CONJURE_HOME" bash "$CONJURE_HOME/templates/skills/restructure/gates/audit-staged.sh" "$target/.conjure-adopt-state/staging/CLAUDE.md" >/dev/null 2>&1`; assert `$? == 2` (blocked before approval). (5c never written) Assert the target's actual `CLAUDE.md` does NOT contain an `^@` line (`grep -q '^@' "$target/CLAUDE.md"` → fail if found, pass if absent) — the gate blocked, so the staged @import was never applied. Cleanup + `trap - EXIT`.
    Close the block with the "End Phase 24 test block" banner (mirror :2912-2914), positioned before the gh-stub cleanup loop. Finally, run the FULL suite and confirm `FAIL: 0` with `PASS` at the new higher count (>= 429 + all Phase 24 assertions). No new deps; no `exit 1`.
  </action>
  <verify>
    <automated>bash tests/run.sh 2>&1 | grep -E '▸ Phase 24|✗' ; echo "---summary---" ; bash tests/run.sh 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `bash tests/run.sh 2>&1 | grep -c '▸ Phase 24'` == 5 (all five criterion sections present).
    - C4: the SIGKILL section passes the in-window kill assertion (polled `current_step` was snapshot or inventory), the non-TTY re-run `rc == 2` + `last completed:` + three-flags assertions, AND the `CONJURE_ADOPT_ROLLBACK=1` re-run → `diff -r` (excl D-03) empty assertion. A `# NOTE:` documents the interactive prompt as manual-only.
    - C5: the symlink path is absent from manifest `files[]` (5a), `audit-staged.sh` on the staged @import exits 2 (5b), and the target `CLAUDE.md` has no `^@` line (5c).
    - No `✗` lines appear for any `▸ Phase 24` section across the full run.
    - `bash tests/run.sh` ends `FAIL: 0` with `PASS` >= 429 plus all new Phase 24 assertions (no regression; the existing 429 stay green).
    - `grep -v '^#' tests/run.sh | grep -c 'exit 1'` unchanged from baseline (no `exit 1` introduced).
    - The "End Phase 24 test block" banner precedes the gh-stub cleanup loop (`for _s in $GH_HIDE_STUBS`) which still precedes the Summary.
    - `shellcheck -S error -e SC2164,SC2044,SC2034,SC2155 tests/run.sh` exits 0.
  </acceptance_criteria>
  <done>All five `▸ Phase 24` sections (C1–C5) are GREEN against the argus fixture; C4's interactive prompt is documented manual-only while the non-TTY exit-2 + auto-rollback path is automated; the block is closed and positioned before the gh-stub cleanup; the full suite is `FAIL: 0` with no regression below 429 and shellcheck-clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Test-harness phase only. Sections spawn + `kill -9` a local background bash process and write to `mktemp -d` dirs that are trap-cleaned. No external input, no auth, no network, no crypto, no new attack surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-SC | Tampering | external package installs | accept | Zero external packages added (CLAUDE.md `dependencies: {}` empty; no npm/pip/cargo install). Slopcheck has nothing to audit. |
| T-24-02 | Denial of Service | SIGKILL background-launch + bounded-poll harness | accept | Self-contained: the test spawns and kills its own background process within a bounded `seq` poll (no blind sleep, no unbounded loop), then `wait`s and `rm -rf`s the sandbox. No external process targeted. |

> Per 24-RESEARCH.md "## Security Domain": `security_enforcement` absent from config; no ASVS category applies to a self-contained bash test block. The pipeline's own security gates (path-traversal, op-allowlist, protected-dir) were built and tested in Phases 22/23 (14/14 threats closed) and are exercised — not modified — by these assertions.
</threat_model>

<verification>
- `bash tests/run.sh 2>&1 | grep -c '▸ Phase 24'` == 5 (all five criterion sections).
- `bash tests/run.sh` ends `FAIL: 0` with `PASS` >= 429 + the new Phase 24 assertions (no regression).
- `shellcheck -S error -e SC2164,SC2044,SC2034,SC2155 tests/run.sh` exits 0.
- `grep -v '^#' tests/run.sh | grep -c 'exit 1'` unchanged from baseline.
- The `▸ Phase 24` block sits after :3280 and before the gh-stub cleanup / Summary.
</verification>

<success_criteria>
- All five ROADMAP success criteria (C1–C5) have GREEN automated assertions against the 500-file `_brownfield-argus` fixture in the `▸ Phase 24` block.
- C4's interactive `[r]/[c]/[s]` prompt is correctly documented manual-only (PTY-verified in 22/23); the non-TTY exit-2 path + the `CONJURE_ADOPT_ROLLBACK=1` auto-rollback zero-diff are automated.
- The full suite is `FAIL: 0` with no regression below the 429 baseline, shellcheck-clean, with no new test dependency.
</success_criteria>

<output>
Create `.planning/phases/24-integration-tests-argus-fixture/24-02-SUMMARY.md` when done.
Record the final PASS count (>= 429 + new Phase 24 assertions), confirm `FAIL: 0`, and note the C4 interactive prompt as the documented manual-only sub-check.
</output>
