---
phase: 24-integration-tests-argus-fixture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/fixtures/_brownfield-argus/generate-argus.sh
  - scripts/adopt.sh
autonomous: true
requirements: []   # verification phase — no REQ-* IDs; gated by the 5 ROADMAP success criteria
must_haves:
  truths:
    - "A generator script materializes a 500-file brownfield-argus fixture (500 .md + a real ln -s symlink + an oversized CLAUDE.md + an @import staged seed) into a passed target dir"
    - "The argus fixture dir is _-prefixed so it is excluded from the generic tests/fixtures/[^_]*/ sweep loops"
    - "An idempotent re-run with zero scaffolded layers emits the literal phrase 'nothing to scaffold' (ROADMAP criterion 3)"
    - "The 429 existing assertions stay green and shellcheck stays clean after the report() deviation"
  artifacts:
    - path: "tests/fixtures/_brownfield-argus/generate-argus.sh"
      provides: "500-file brownfield fixture generator (bulk .md + real symlink + oversized CLAUDE.md + @import seed)"
      min_lines: 30
    - path: "scripts/adopt.sh"
      provides: "report() with the criterion-3 'nothing to scaffold' deviation when created_count is 0"
      contains: "nothing to scaffold"
  key_links:
    - from: "tests/fixtures/_brownfield-argus/generate-argus.sh"
      to: "<target-dir>"
      via: "materializes 500 .md + ln -s symlink + oversized CLAUDE.md + @import seed into the passed dir"
      pattern: "ln -s"
    - from: "scripts/adopt.sh report()"
      to: "stdout adoption report"
      via: "conditional echo when created_count == 0"
      pattern: "nothing to scaffold"
---

<objective>
Build the two prerequisites the `▸ Phase 24` E2E test block (Plan 02) consumes:
(1) the `_brownfield-argus` fixture generator that materializes a representative
500-file brownfield repo into a passed target dir, and (2) the single-line
criterion-3 `report()` deviation in `scripts/adopt.sh` so an idempotent re-run
emits the literal ROADMAP phrase "nothing to scaffold".

Purpose: Phase 24 verifies the SHIPPED `conjure adopt` + restructure pipeline
(Phases 21–23) end-to-end. The generator is the fixture under test; the `report()`
deviation is the ONLY product-code touch this phase makes (O-1, justified by
ROADMAP criterion 3's exact wording). Both must exist and be correct before the
test block can go GREEN.

Output: `tests/fixtures/_brownfield-argus/generate-argus.sh` (NEW), a 1-line
deviation in `scripts/adopt.sh` `report()` (MODIFY).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-integration-tests-argus-fixture/24-RESEARCH.md
@.planning/phases/24-integration-tests-argus-fixture/24-VALIDATION.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write tests/fixtures/_brownfield-argus/generate-argus.sh (500-file fixture generator)</name>
  <files>tests/fixtures/_brownfield-argus/generate-argus.sh</files>
  <read_first>
    - tests/fixtures/brownfield-simple/generate-large.sh (the generator shape to MIRROR — usage check, mkdir -p DEST, while-loop printf into doc-NNN.md, final echo). Note it `exit 1`s on the usage-arg error: a fixture generator is NOT a hook, but to honor the project "exit 2 never exit 1" lock, use `exit 2` on the missing-arg error instead.
    - 24-RESEARCH.md "## Standard Stack" (the `ln -s` row: the existing brownfield-simple symlink name is unreliable across cp/git/Windows; the argus symlink MUST be created with a genuine `ln -s` at generation time) + "### Recommended Project Structure" (the _brownfield-argus layout: generate-argus.sh + generated bulk + ln -s symlink + oversized CLAUDE.md + @import seed).
    - 24-RESEARCH.md "## Code Examples" — the symlink-skip example (`ln -s real.md "$P24_TARGET/docs/linked.md"`) and the @import staged-content example (`printf '# CLAUDE\n@.claude/skills/x/SKILL.md\n'`) show the exact distinctive files the generator must produce.
    - 24-RESEARCH.md "## Common Pitfalls" Pitfall 1 (the `_`-prefix sweep — the dir is `_brownfield-argus`, already locked) + the "Committing the symlink file" anti-pattern (create with `ln -s`, never commit a symlink).
    - 24-VALIDATION.md "## Planning Resolutions" Fixture strategy bullet (500 .md + real ln -s + oversized CLAUDE.md + @import seed into a passed target dir).
    - CLAUDE.md Constraints (POSIX bash 3.2+: no associative arrays / mapfile / `local -n`; `exit 2` never `exit 1`; shellcheck gate `-S error -e SC2164,SC2044,SC2034,SC2155`; no new deps).
  </read_first>
  <action>
    Create `tests/fixtures/_brownfield-argus/generate-argus.sh`, mirroring `generate-large.sh`'s shape (shebang `#!/usr/bin/env bash`, `set -uo pipefail`, a `TARGET="${1:-}"` usage guard that prints to stderr and `exit 2` on missing arg — NOT exit 1 per the project lock, `mkdir -p`, a `while [ "$i" -le N ]` printf loop, a final summary echo). It accepts one positional `<target-dir>` and materializes a representative brownfield repo into it (the dir is created if absent). Materialize, all into the passed target dir:
    (1) An oversized/sprawling `CLAUDE.md` at the target root — well over the 100-line cap (e.g. a header plus a generated body of 120+ lines) so the adopt report has a meaningful before-line count and the fixture is genuinely "grown-messy". A plain printf loop building the body is fine.
    (2) ~500 bulk `.md` files under a subdir (e.g. `docs/` and/or `generated-docs/`), each a few lines, via a `while`/`printf` loop with zero-padded names (mirror generate-large.sh's `printf '%03d'`). Pick a count >= 500 to satisfy the "500-file" criterion (e.g. 500–510; document the exact count in the final echo).
    (3) A REAL symlink markdown file via `ln -s` — first create a regular target (e.g. `docs/real.md`), then `ln -s real.md "$TARGET/docs/linked.md"` (a relative symlink target keeps it portable). This is criterion 5's symlink-skip source; it MUST be a genuine `ln -s`, never a committed regular file.
    (4) An `@import` staged seed: a markdown file whose first content line is an `@`-import (e.g. `printf '# Proposed CLAUDE\n@.claude/skills/x/SKILL.md\n' > "$TARGET/with-import.md"`). This is criterion 5's @import-block source — Plan 02 stages it into `.conjure-adopt-state/staging/CLAUDE.md` and runs it through the audit-staged gate.
    Add inline `# shellcheck` directives matching the project style if any flagged-by-default rule would otherwise trip (the CI gate is `-S error -e SC2164,SC2044,SC2034,SC2155`). Keep it dependency-free (printf/mkdir/ln/while only). Do NOT commit any generated bulk file or the symlink — the script materializes everything at test time; only the script itself is committed under `tests/fixtures/_brownfield-argus/`.
  </action>
  <verify>
    <automated>T="$(mktemp -d)"; bash tests/fixtures/_brownfield-argus/generate-argus.sh "$T" >/dev/null 2>&1; rc=$?; n="$(find "$T" -name '*.md' | wc -l | tr -d ' ')"; sym="$([ -L "$T/docs/linked.md" ] && echo yes || echo no)"; imp="$(grep -lc '^@' "$T"/*.md 2>/dev/null | head -1)"; claude_lines="$(wc -l < "$T/CLAUDE.md" | tr -d ' ')"; echo "rc=$rc md=$n symlink=$sym claude_lines=$claude_lines"; [ "$rc" -eq 0 ] && [ "$n" -ge 500 ] && [ "$sym" = "yes" ] && [ "$claude_lines" -gt 100 ] && grep -q '^@' "$T"/with-import.md; echo "exit:$?"; rm -rf "$T"</automated>
  </verify>
  <acceptance_criteria>
    - `bash tests/fixtures/_brownfield-argus/generate-argus.sh "$(mktemp -d)"` exits 0.
    - `find <target> -name '*.md' | wc -l` >= 500 after generation.
    - `[ -L <target>/docs/linked.md ]` is true (a REAL symlink, created via `ln -s`).
    - `wc -l < <target>/CLAUDE.md` > 100 (oversized CLAUDE.md present).
    - The `@import` seed file's first content line matches `^@` (`grep -q '^@' <target>/with-import.md`).
    - `shellcheck -S error -e SC2164,SC2044,SC2034,SC2155 tests/fixtures/_brownfield-argus/generate-argus.sh` exits 0.
    - `grep -v '^#' tests/fixtures/_brownfield-argus/generate-argus.sh | grep -c 'exit 1'` == 0 (no `exit 1` in executable lines).
    - The dir name is exactly `_brownfield-argus` (leading underscore — excluded from the `tests/fixtures/[^_]*/` sweep loops at run.sh:326/368/390).
  </acceptance_criteria>
  <done>The generator script exists, is shellcheck-clean, contains no `exit 1`, and materializes a >=500-file fixture with a real `ln -s` symlink, an oversized (>100-line) CLAUDE.md, and an `@import` seed file into any passed target dir.</done>
</task>

<task type="auto">
  <name>Task 2: Add the criterion-3 "nothing to scaffold" report() deviation in scripts/adopt.sh (O-1)</name>
  <files>scripts/adopt.sh</files>
  <read_first>
    - scripts/adopt.sh:222-245 (the `report()` function — `created_count` is computed at :233 from `jq -r '.created | length' "$STATE_PATH"`, and the "Scaffolded:" line is printed at :238 as `echo "  Scaffolded:  ${created_count} layer files"`). This is the ONE line to deviate.
    - 24-VALIDATION.md "## Planning Resolutions" O-1 (do BOTH: keep asserting `Scaffolded: 0 layer files` AND make a minimal 1-line `report()` deviation so the literal "nothing to scaffold" is emitted when `created[]` is empty; document as a deviation).
    - 24-RESEARCH.md "## Open Questions" Q1 (the literal string "nothing to scaffold" exists nowhere in the codebase today; the recommendation is a 1-line conditional in `report()`) + "## State of the Art" (the criterion-3 phrase row).
    - CLAUDE.md Constraints (this is the ONLY product-code touch the phase makes; keep it truly minimal — 1 conditional echo; do not refactor report() otherwise).
  </read_first>
  <action>
    In `report()` in `scripts/adopt.sh`, make the SMALLEST possible deviation so that when `created_count` is 0 the report emits the literal phrase "nothing to scaffold". Keep the existing "Scaffolded:  ${created_count} layer files" line intact (the Plan 02 test still asserts `Scaffolded:  0 layer files` for criterion 3 signal a). Add a single conditional echo immediately after the Scaffolded line: when `[ "${created_count:-0}" -eq 0 ]`, also echo a line containing the literal token "nothing to scaffold" (e.g. `echo "  Scaffolded:  nothing to scaffold"` as an ADDITIONAL line, leaving the count line above it). This is a documented O-1 deviation in an otherwise test-only phase, justified by ROADMAP criterion 3's exact wording. Do NOT change any other behavior in report() or anywhere else in adopt.sh. Record this deviation explicitly in the plan SUMMARY.
  </action>
  <verify>
    <automated>FX="$(mktemp -d)"; bash tests/fixtures/_brownfield-argus/generate-argus.sh "$FX" >/dev/null 2>&1; DRY_RUN=0 CONJURE_HOME="$PWD" bash scripts/adopt.sh "$FX" >/dev/null 2>&1; rm -rf "$FX/.conjure-adopt-state"; OUT="$(DRY_RUN=0 CONJURE_HOME="$PWD" bash scripts/adopt.sh "$FX" 2>&1)"; printf '%s\n' "$OUT" | grep -qi 'nothing to scaffold' && echo "DEVIATION_OK" || echo "DEVIATION_MISSING"; printf '%s\n' "$OUT" | grep -Eq 'Scaffolded:[[:space:]]*0[[:space:]]+layer' && echo "COUNT_LINE_OK"; rm -rf "$FX"</automated>
  </verify>
  <acceptance_criteria>
    - On an idempotent re-run (state cleared between runs, second run scaffolds 0 layers), `report()` stdout contains the literal phrase "nothing to scaffold" (`grep -qi 'nothing to scaffold'`).
    - The original `Scaffolded:  0 layer files` count line is still present on the zero-scaffold run (`grep -Eq 'Scaffolded:[[:space:]]*0[[:space:]]+layer'`) — the deviation ADDS a line, it does not replace the count line.
    - The deviation is a single conditional echo; `git diff scripts/adopt.sh` shows only the report() change (no other behavior touched).
    - `shellcheck -S error -e SC2164,SC2044,SC2034,SC2155 scripts/adopt.sh` exits 0.
    - `grep -v '^#' scripts/adopt.sh | grep -c 'exit 1'` == 0 (no `exit 1` introduced).
    - Full suite stays green: `bash tests/run.sh` ends `PASS: 429    FAIL: 0` (no regression — the deviation is additive and does not affect any existing Phase 21/22/23 assertion).
  </acceptance_criteria>
  <done>`scripts/adopt.sh` `report()` emits "nothing to scaffold" when `created_count` is 0 via a single additive conditional echo, the count line is unchanged, shellcheck is clean, and all 429 existing assertions remain green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Test-harness + fixture-generator phase only. No external input, no auth, no network, no crypto, no new attack surface. The generator writes to a passed `mktemp -d` target dir; the report() deviation is a local stdout echo. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-SC | Tampering | external package installs | accept | Zero external packages added (CLAUDE.md locks `dependencies: {}` empty; no npm/pip/cargo install in this plan). Slopcheck has nothing to audit. |
| T-24-01 | Tampering | scripts/adopt.sh report() deviation | mitigate | Deviation is a single additive conditional echo; the 429-green full-suite run is an acceptance criterion proving no behavioral regression. No new input path; output-only. |

> Per 24-RESEARCH.md "## Security Domain": `security_enforcement` absent from config; no ASVS category applies to a self-contained bash test/fixture phase. The pipeline's own security gates (path-traversal, op-allowlist, protected-dir) were built and tested in Phases 22/23 (14/14 threats closed) and are not re-litigated here.
</threat_model>

<verification>
- `shellcheck -S error -e SC2164,SC2044,SC2034,SC2155 tests/fixtures/_brownfield-argus/generate-argus.sh scripts/adopt.sh` exits 0.
- `bash tests/run.sh` ends `PASS: 429    FAIL: 0` (Plan 01 adds no test assertions itself; the deviation must not regress the existing suite).
- Generator produces >=500 .md, a real `ln -s` symlink, an oversized (>100-line) CLAUDE.md, and an `@import` seed in any passed target dir.
- `report()` emits "nothing to scaffold" on a zero-scaffold run while preserving the `Scaffolded:  0 layer files` count line.
</verification>

<success_criteria>
- `tests/fixtures/_brownfield-argus/generate-argus.sh` exists, is shellcheck-clean, has no `exit 1`, and materializes the 500-file argus fixture (bulk .md + real symlink + oversized CLAUDE.md + @import seed) into a passed dir.
- `scripts/adopt.sh` `report()` emits the literal "nothing to scaffold" on a zero-scaffold run via a single additive conditional echo (O-1 deviation, documented in SUMMARY).
- The 429 existing assertions remain green; no new test dependency introduced.
</success_criteria>

<output>
Create `.planning/phases/24-integration-tests-argus-fixture/24-01-SUMMARY.md` when done.
Document the `scripts/adopt.sh` `report()` change explicitly as an O-1 deviation (the only product-code touch in this verification phase) and confirm the 429-green no-regression.
</output>
