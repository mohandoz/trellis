---
phase: 21-foundation-libs-inventory
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/fixtures/brownfield-simple/CLAUDE.md
  - tests/fixtures/brownfield-simple/.claude/skills/git/SKILL.md
  - tests/fixtures/brownfield-simple/.claude/agents/deploy.md
  - tests/fixtures/brownfield-simple/.claude/settings.json
  - tests/fixtures/brownfield-simple/docs/README.md
  - tests/fixtures/brownfield-simple/docs/ARCHITECTURE.md
  - tests/fixtures/brownfield-simple/docs/RUNBOOK.md
  - tests/fixtures/brownfield-simple/.planning/21-PLAN.md
  - tests/fixtures/brownfield-simple/generate-large.sh
  - tests/fixtures/brownfield-simple/EXPECT
  - adopt-manifest.schema.json
  - tests/run.sh
autonomous: true
requirements:
  - INV-01
  - INV-02
  - INV-03
  - INV-04
  - SAFE-03
  - ADOPT-03

must_haves:
  truths:
    - "tests/fixtures/brownfield-simple/ contains CLAUDE.md, a skill, an agent, docs/README.md, docs/ARCHITECTURE.md, docs/RUNBOOK.md, .claude/settings.json, .planning/21-PLAN.md, and a symlink"
    - "tests/fixtures/brownfield-simple/generate-large.sh creates 510+ .md files when run"
    - "adopt-manifest.schema.json is valid JSON Schema draft-07 with all required keys"
    - "Phase 21 test block exists in tests/run.sh with section headers for every Req ID"
    - "brownfield-simple fixture passes audit-setup.sh with exit 0"
  artifacts:
    - path: "tests/fixtures/brownfield-simple/CLAUDE.md"
      provides: "core-bucket file for classification unit tests; 15-line valid doc"
    - path: "tests/fixtures/brownfield-simple/generate-large.sh"
      provides: "script that creates 510+ synthetic .md files for INV-03 cap test"
    - path: "adopt-manifest.schema.json"
      provides: "JSON Schema draft-07 validating all manifest fields from RESEARCH.md Pattern 7"
    - path: "tests/run.sh"
      provides: "Phase 21 test block scaffolded with MISSING/stub markers for D-01..D-13 behaviors"
  key_links:
    - from: "tests/fixtures/brownfield-simple/generate-large.sh"
      to: "tests/run.sh INV-03 cap test"
      via: "invoked inside Phase 21 test block at test time"
      pattern: "generate-large\\.sh"
    - from: "adopt-manifest.schema.json"
      to: "tests/run.sh SC-4"
      via: "jq validation command in Phase 21 test block"
      pattern: "adopt-manifest\\.schema\\.json"
---

<objective>
Wave 0 test infrastructure: create the brownfield-simple fixture, the synthetic cap
generator, the adopt-manifest.schema.json (JSON Schema draft-07), and scaffold the
Phase 21 test block in tests/run.sh. Every subsequent plan depends on these being
present so unit tests can run immediately after implementation.

Purpose: Nyquist validation is enabled — tests must exist before or alongside the code
under test. This plan satisfies the Wave 0 requirement from 21-VALIDATION.md.

Output:
- tests/fixtures/brownfield-simple/ (9 files + 1 symlink)
- adopt-manifest.schema.json (draft-07 JSON Schema)
- Phase 21 test block appended to tests/run.sh (stubs that FAIL until libs exist)
</objective>

<execution_context>
@/Users/mohandoz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mohandoz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/21-foundation-libs-inventory/21-CONTEXT.md
@.planning/phases/21-foundation-libs-inventory/21-RESEARCH.md
@.planning/phases/21-foundation-libs-inventory/21-PATTERNS.md
@.planning/phases/21-foundation-libs-inventory/21-VALIDATION.md
@tests/run.sh
@tests/lib/sandbox.sh
@tests/fixtures/ts-next/CLAUDE.md
@scripts/audit-setup.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create brownfield-simple fixture and adopt-manifest.schema.json</name>
  <files>
    tests/fixtures/brownfield-simple/CLAUDE.md
    tests/fixtures/brownfield-simple/.claude/skills/git/SKILL.md
    tests/fixtures/brownfield-simple/.claude/agents/deploy.md
    tests/fixtures/brownfield-simple/.claude/settings.json
    tests/fixtures/brownfield-simple/docs/README.md
    tests/fixtures/brownfield-simple/docs/ARCHITECTURE.md
    tests/fixtures/brownfield-simple/docs/RUNBOOK.md
    tests/fixtures/brownfield-simple/.planning/21-PLAN.md
    tests/fixtures/brownfield-simple/generate-large.sh
    tests/fixtures/brownfield-simple/EXPECT
    adopt-manifest.schema.json
  </files>
  <read_first>
    - tests/fixtures/ts-next/CLAUDE.md — fixture CLAUDE.md format (generated header, Constraints, Tech Stack sections)
    - tests/run.sh lines 323-380 — fixture audit loop; brownfield-simple will be automatically picked up and must exit 0
    - scripts/audit-setup.sh lines 23-82 — what audit-setup.sh checks: CLAUDE.md ≤100 lines, no @imports, skills have name:/description: frontmatter, agents ≤80 lines; lines 109-110: warns if docs/ARCHITECTURE.md or docs/RUNBOOK.md missing; line 85: warns if .claude/settings.json missing
    - .planning/phases/21-foundation-libs-inventory/21-RESEARCH.md Pattern 7 — finalized adopt-manifest.json schema sample (D-01/D-05/D-08/D-09 applied)
    - .planning/phases/21-foundation-libs-inventory/21-PATTERNS.md "brownfield-simple fixture must contain" section — exact file list and symlink requirement
    - .planning/phases/21-foundation-libs-inventory/21-CONTEXT.md D-01/D-08/D-09 — bucket names, size_cap_exceeded naming, scan_capped naming
  </read_first>
  <action>
    Create tests/fixtures/brownfield-simple/ with these exact files:

    CLAUDE.md: 15-line valid core doc (follows ts-next fixture format; no @imports; header comment "BROWNFIELD-SIMPLE fixture"; a Constraints section, a Technology Stack section; total ≤15 lines). Must pass audit-setup.sh CLAUDE.md checks.

    .claude/skills/git/SKILL.md: valid skill frontmatter (name: git, description: at least 30 chars), body ≤200 lines. Must pass audit-setup.sh skill checks.

    .claude/agents/deploy.md: minimal agent doc ≤80 lines. Must pass audit-setup.sh agent size check.

    .claude/settings.json: minimal valid JSON object (required so audit-setup.sh finds .claude/settings.json and does not warn; audit-setup.sh line 85 warns if missing). Minimal: {"hooks":{}}.

    docs/README.md: short reference doc (5+ lines). This is the reference-doc bucket target.

    docs/ARCHITECTURE.md: minimal stub (3+ lines, e.g. "# Architecture\n\nBrownfield-simple fixture.\n"). Required so audit-setup.sh line 109 does not emit the "docs/ARCHITECTURE.md missing" warning and exit 1.

    docs/RUNBOOK.md: minimal stub (3+ lines, e.g. "# Runbook\n\nBrownfield-simple fixture.\n"). Required so audit-setup.sh line 110 does not emit the "docs/RUNBOOK.md missing" warning and exit 1.

    .planning/21-PLAN.md: short planning doc (3+ lines). This is the planning-doc bucket target.

    symlink-target: create a symlink pointing at docs/README.md for M-2 skip testing. Use: ln -s docs/README.md tests/fixtures/brownfield-simple/symlink-target.md

    generate-large.sh: executable bash script that, when run with a target directory argument, creates 510+ .md files in that directory under a subdirectory called generated-docs/. Files named doc-001.md through doc-510.md with 3 lines each. Script must be executable. Used by the INV-03 cap test at runtime, NOT committed as actual fixture files.

    EXPECT: golden-file assertions for the fixture audit loop (TEST-03). Must contain patterns that audit-setup.sh outputs for a valid harness: "CLAUDE.md:.*lines", "skills/", "agents/". With docs/ARCHITECTURE.md, docs/RUNBOOK.md, and .claude/settings.json all present, audit-setup.sh will exit 0 (no warnings for missing docs or settings). EXPECT patterns must match what audit-setup.sh actually prints.

    adopt-manifest.schema.json: JSON Schema draft-07 file at repo root. Required keys to validate: schema_version (string), generated_at (string, format date-time), conjure_version (string), target (string), snapshot_path (string), summary (object with properties: total_files integer, scan_capped boolean, total_found integer, core integer, skill integer, agent integer, planning-doc integer, reference-doc integer, unknown integer), files (array of objects: path string, classification enum ["core","skill","agent","planning-doc","reference-doc","unknown"], line_count integer, size_bytes integer, size_cap_exceeded boolean, size_cap_limit oneOf [integer, null], linked_from array of strings), size_cap_violations (array of objects: path string, line_count integer, cap integer, overage integer), harness_missing_layers (array of strings), restructure_steps (array). Use "$schema": "http://json-schema.org/draft-07/schema#". Set "additionalProperties" judiciously — allow additional properties at top level to accommodate future Phase 22 additions (snapshot_path, at_imports_detected). All required array fields must be present. Classification enum must be exactly the D-01 six values. size_cap_limit must accept null (oneOf: [{type: integer}, {type: null}]). Per D-09: field is named size_cap_exceeded (not cap_exceeded).
  </action>
  <verify>
    <automated>
      bash scripts/audit-setup.sh tests/fixtures/brownfield-simple 2>&1 | tail -5; echo "rc=$?"
      jq empty adopt-manifest.schema.json && echo "schema JSON valid"
      test -L tests/fixtures/brownfield-simple/symlink-target.md && echo "symlink exists"
      bash tests/fixtures/brownfield-simple/generate-large.sh /tmp/bf-large-test-$$ && ls /tmp/bf-large-test-$$/generated-docs/ | wc -l | tr -d ' ' && rm -rf /tmp/bf-large-test-$$
    </automated>
  </verify>
  <acceptance_criteria>
    - bash scripts/audit-setup.sh tests/fixtures/brownfield-simple exits 0 (all checks green, no warnings from missing docs or settings.json)
    - jq empty adopt-manifest.schema.json exits 0 (valid JSON)
    - jq '.properties.files.items.properties.classification.enum | length' adopt-manifest.schema.json outputs 6
    - jq '.properties.files.items.properties.size_cap_exceeded' adopt-manifest.schema.json contains "size_cap_exceeded" (not cap_exceeded)
    - test -L tests/fixtures/brownfield-simple/symlink-target.md exits 0 (symlink exists)
    - generate-large.sh run produces at least 510 .md files in generated-docs/ subdirectory
    - tests/run.sh fixture audit loop picks up brownfield-simple and passes (bash tests/run.sh 2>&1 | grep "brownfield-simple" shows "fixture audit green")
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Scaffold Phase 21 test block in tests/run.sh</name>
  <files>
    tests/run.sh
  </files>
  <read_first>
    - tests/run.sh lines 254-294 — mutate_rm unit test block pattern (subshell DRY_RUN=1 tests + live tests; pass/fail helpers; source lib/mutate.sh)
    - .planning/phases/21-foundation-libs-inventory/21-VALIDATION.md — Per-Task Verification Map; every Req ID row must have a corresponding test stub in the block
    - .planning/phases/21-foundation-libs-inventory/21-RESEARCH.md §Validation Architecture — test command examples and assertion patterns
    - .planning/phases/21-foundation-libs-inventory/21-PATTERNS.md "Phase 21 test block must follow this exact pattern" section
  </read_first>
  <action>
    Append a Phase 21 test block to tests/run.sh AFTER the existing test sections. The block must follow the identical structural pattern as the mutate_rm unit tests (lines 254-294):

    Section headers (echo lines): one per requirement group — "Phase 21 — lib/caps.sh (SC-5)", "Phase 21 — lib/log.sh (ADOPT-03/SC-1)", "Phase 21 — lib/snapshot.sh (SC-2)", "Phase 21 — lib/inventory.sh (INV-01..INV-04)", "Phase 21 — mutate_archive (SAFE-03)", "Phase 21 — audit-setup.sh caps (SC-5)", "Phase 21 — manifest schema (SC-4)", "Phase 21 — perf gate (CR-7)".

    For each Req ID in 21-VALIDATION.md, write a concrete stub test that:
    1. Attempts to source the not-yet-existing lib file (e.g., source lib/caps.sh)
    2. If sourcing fails, immediately fails with a message like "lib/caps.sh not found — Wave 1 must create it first (SC-5)" and skips remaining sub-tests for that section
    3. If sourcing succeeds, runs the specific assertion from 21-VALIDATION.md

    Key test stubs to include (all use source + assertion pattern; fail gracefully if lib missing):

    SC-5 / caps.sh:
    - Source lib/caps.sh; assert CLAUDE_MD_CAP = 100 via: [ "${CLAUDE_MD_CAP}" = "100" ]
    - Assert SKILL_MD_CAP = 200, AGENT_MD_CAP = 80 similarly

    ADOPT-03/SC-1 / log.sh:
    - DRY_RUN=1 subshell: source lib/mutate.sh; source lib/log.sh; call log_step TEST "hello"; assert output contains "[dry-run] would write" (per D-11, log.sh uses mutate_write which honors DRY_RUN)
    - Live mode: create mktemp dir with RESTRUCTURE-LOG.md path; DRY_RUN=0; call log_init and log_step; assert RESTRUCTURE-LOG.md contains "[" and the phase and message text; grep -c "^\[" must equal expected entry count (Pitfall 1 newline check)

    SC-2 / snapshot.sh:
    - DRY_RUN=1: source libs; call snapshot_create on brownfield-simple fixture path; assert output contains "[dry-run] would snapshot"; assert no actual directory was created
    - DRY_RUN=0 live: use mktemp for target and backup_root; cp -r fixture into target; call snapshot_create; assert $CONJURE_SNAPSHOT_PATH is non-empty; assert snapshot dir exists; assert snapshot contains CLAUDE.md

    INV-01 / inventory.sh classify:
    - Source libs; call inventory_classify on each file type from brownfield-simple and assert correct bucket: CLAUDE.md → core, .claude/skills/git/SKILL.md → skill, .claude/agents/deploy.md → agent, .planning/21-PLAN.md → planning-doc, docs/README.md → reference-doc
    - Assert a file outside harness dirs (e.g., a temp .md file in /tmp) → unknown (D-03)

    INV-02 / inventory.sh emit_manifest:
    - Run inventory_scan + inventory_emit_manifest on brownfield-simple (in a sandbox tmp copy); assert adopt-manifest.json created; assert jq -e '.schema_version' outputs non-null; assert jq -e '.summary.scan_capped == false'; assert jq -e '.files | length > 0'
    - Assert summary bucket counts match: jq -e '.summary.core == 1' (one CLAUDE.md)

    INV-03 / cap tests:
    - Symlink skip: run inventory on fixture containing symlink-target.md; assert the symlink path does NOT appear in files[] via jq
    - 500-file cap: use generate-large.sh to create 510+ files in a temp dir (with a minimal CLAUDE.md); run inventory_scan; assert jq -e '.summary.scan_capped == true'; assert jq -e '.summary.total_found > 500'; assert jq -e '(.files | length) <= 500'
    - Harness-first budget: in the 510-file fixture, assert jq -e '.files[] | select(.path == "CLAUDE.md") | .classification == "core"' succeeds (CLAUDE.md always included per D-10)

    INV-04 / size_cap_exceeded:
    - Create a temp CLAUDE.md with 105 lines (> CLAUDE_MD_CAP=100); run inventory; assert jq -e '.files[] | select(.path == "CLAUDE.md") | .size_cap_exceeded == true'
    - Assert jq -e '.size_cap_violations | length > 0'

    SAFE-03 / mutate_archive:
    - DRY_RUN=1: source lib/mutate.sh; call mutate_archive on a temp file; assert output contains "[dry-run] would archive"; assert counter incremented; assert original file still exists
    - Live mode: create src file; create archive_root; call mutate_archive; assert src no longer exists; assert dest file exists at path-preserving location; assert .archive-ledger file exists with a line containing src path
    - sha256 mismatch abort: create src, create dest manually in archive path, corrupt dest; call mutate_archive and capture return code; assert return code != 0; assert src still exists (never deleted unverified content — D-13)

    SC-4 / schema:
    - jq 'empty' adopt-manifest.schema.json exits 0
    - Validate RESEARCH.md Pattern 7 sample JSON against schema (inline the minimal sample as a heredoc temp file and use jq to check required keys match schema)

    SC-5 / audit-setup.sh caps:
    - grep -v '^#' scripts/audit-setup.sh | grep -c 'CLAUDE_MD_CAP' returns value > 0 after Plan 04 modifies the file (this assertion will fail until Plan 04 completes; stub it to fail gracefully with "audit-setup.sh not yet updated — Plan 04 required")

    CR-7 / perf gate:
    - Section header "Phase 21 — perf gate (CR-7)"; use the generate-large.sh fixture (510 files) in a temp dir with a CLAUDE.md; time the inventory_emit_manifest call; assert completion in < 30 seconds. Use: START=$(date +%s); ... ; END=$(date +%s); ELAPSED=$((END-START)); [ "$ELAPSED" -lt 30 ]

    The entire Phase 21 block must be self-contained: each section sources the relevant lib files and uses mktemp for isolation. Sections that depend on files not yet created (libs from Plans 02-04) must fail gracefully (skip-with-fail, not crash) so the existing 302+ tests are not broken.

    IMPORTANT: Do NOT break existing test infrastructure. Append only. Do not modify any existing test sections.
  </action>
  <verify>
    <automated>
      bash tests/run.sh 2>&1 | grep -E "✓|✗" | tail -20
      bash tests/run.sh 2>&1 | grep -c "Phase 21"
      bash tests/run.sh 2>&1 | grep "brownfield-simple" | head -5
    </automated>
  </verify>
  <acceptance_criteria>
    - bash tests/run.sh exits without crash (rc 0 or 1 are both acceptable since Phase 21 libs don't exist yet; rc 2 means a hard failure)
    - bash tests/run.sh 2>&1 | grep "Phase 21" counts at least 8 section headers (one per requirement group)
    - bash tests/run.sh 2>&1 | grep "brownfield-simple" contains "fixture audit green: brownfield-simple"
    - All existing non-Phase-21 tests continue to pass (bash tests/run.sh 2>&1 | grep "✗" shows only Phase 21 failures, not pre-existing failures)
    - Phase 21 section stubs fail gracefully with informative messages (not bash errors/stack traces) when libs are absent
    - grep -c "INV-01\|INV-02\|INV-03\|INV-04\|SAFE-03\|ADOPT-03\|SC-1\|SC-2\|SC-4\|SC-5\|CR-7" tests/run.sh returns value >= 10 (all Req IDs covered)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture files → test harness | Fixture file contents are authored; no untrusted input crosses here |
| generate-large.sh → filesystem | Script writes to a caller-supplied temp directory; path must be quoted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | Tampering | generate-large.sh target path | mitigate | Script accepts target dir as $1; always quote "$1" in all cp/mkdir/touch calls; fail if $1 is empty |
| T-21-02 | Tampering | tests/run.sh Phase 21 block — mktemp paths | mitigate | All test temp dirs created via mktemp -d; always quoted in subsequent commands; cleaned up via rm -rf |
| T-21-SC | Tampering | npm/pip/cargo installs | accept | Phase 01 installs zero packages; no package manager invocations |
</threat_model>

<verification>
Run after both tasks complete:
  bash tests/run.sh 2>&1 | tail -5
  jq empty adopt-manifest.schema.json && echo "schema ok"
  bash scripts/audit-setup.sh tests/fixtures/brownfield-simple 2>&1 | tail -5; echo "rc=$?"
  test -L tests/fixtures/brownfield-simple/symlink-target.md && echo "symlink present"
</verification>

<success_criteria>
- tests/fixtures/brownfield-simple/ exists with all 9 required files + symlink
- generate-large.sh produces 510+ .md files when run
- adopt-manifest.schema.json is valid JSON Schema draft-07 with the 6-bucket classification enum
- Phase 21 test block in tests/run.sh covers all Req IDs from 21-VALIDATION.md
- brownfield-simple fixture passes audit-setup.sh (exit 0)
- bash tests/run.sh does not crash; all pre-existing tests still pass
</success_criteria>

<output>
Create .planning/phases/21-foundation-libs-inventory/21-01-SUMMARY.md when done
</output>
